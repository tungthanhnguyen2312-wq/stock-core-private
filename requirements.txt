# Stock Lookup Producer — CORE runtime dependencies (tier hermetic core / CI)
# Chỉ gồm package mà lõi thực sự import: pipeline offline, feature/quant engine, canonical
# daily operation, publication contracts và toàn bộ test hermetic.
#
# Cài tái lập (giống CI, khuyến nghị):
#   python -m pip install -r requirements.txt -r requirements-test.txt -c constraints.txt
#
# Provider runtime TÙY CHỌN nằm ở requirements-providers.txt (vnstock/vnai cho lane bổ trợ
# KBS/VCI của Daily, anthropic cho ai_analyzer.py gọi API thật). Production provider path VẪN
# cần các package đó; core CI thì không. Xem docs/CI_AND_DEPENDENCY_TIERS.md.

pandas>=2.3
numpy>=2.2
requests>=2.33
pyarrow>=24.0
openpyxl>=3.1            # private local-only portfolio workbook import

# pandas_ta               # chỉ cần cho `vn_indicators.py full` (~130 chỉ báo mở rộng, hiếm dùng);
#                           đã bọc try/except trong code — bỏ qua êm nếu chưa cài, bỏ comment nếu cần
