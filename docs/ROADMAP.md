# Stock Lookup — Architecture & Roadmap

**DNSE intraday history rate-limit resilience and resume integrity V1 (2026-09-06):**
`DNSE_INTRADAY_HISTORY_RATE_LIMIT_RESILIENCE_AND_RESUME_INTEGRITY_V1 = COMPLETE /
SOURCE_CORRECTION_ONLY` at local checkpoint `e01c06f`. This is a bounded corrective gate for
the retained Phase A DNSE Trades acquisition only: retryable failed roots can resume under a
scope lock, finite retry/backoff/pacing, and write-ahead raw-page integrity, while successes and
confirmed empties remain immutable checkpoint facts. Direct checkpoint accounting sets the next
retry-only universe at 21,897 roots (21,891 rate limited, 1 connection failure, 5 read timeouts)
and preserves 3,003 already-successful roots. It does not acquire data or authorize a retry;
Phase B, secondary anchor semantics, canonical-materialization changes, G1/ADTV, provider,
liquidity, sizing, PIT, RAW_AS_TRADED, or authority promotion remain out of scope. Next gate:
`OWNER_REVIEW_FOR_TARGETED_PHASE_A_RETRY`. Evidence:
`operations-review/dnse-intraday-history-rate-limit-resilience-resume-integrity-v1-20260906/`.

**Canonical Trades reconciler page-selection indexing performance fix V1 (2026-09-06):**
`CANONICAL_TRADES_RECONCILER_PAGE_SELECTION_INDEXING_PERFORMANCE_FIX_V1 = COMPLETE` at source
checkpoint `c4409e5`. This bounded corrective milestone removes the per-unit full session-directory
scan that made Anti-Gravity's reconciliation validation stall. The new invocation-local, sorted,
exact-symbol raw-page index preserves full candidate provenance/payload/page validation and all
original/repair, empty, failed, duplicate, and deterministic ordering semantics. The retained
baseline reconciliation terminates normally and matches the accepted 40-session output exactly:
66,400 logical units, 209,193 selected pages, 28,545 confirmed-empty units, and 27 explicit
remaining failures. It does not acquire Phase-A data or alter any liquidity, sizing, provider,
PIT, RAW_AS_TRADED, policy, database, Dashboard, publication, deployment, or authority boundary.
Evidence: `operations-review/canonical-trades-reconciler-page-selection-indexing-performance-fix-v1-20260906/`.
The only next gate is `OWNER_REVIEW_FOR_ANTIGRAVITY_PHASE_A_RESUME`; Phase A/Phase B remain
Anti-Gravity work.

**Canonical Trades toolchain restoration and governed session calendar fix V1 (2026-09-06):**
`CANONICAL_TRADES_TOOLCHAIN_RESTORATION_AND_GOVERNED_SESSION_CALENDAR_FIX_V1 = COMPLETE` at local
checkpoint `6dce959`. Current main now carries the minimum Task-160 reconciliation and shadow
materialization toolchain, manually ported from the unmerged historical candidate without merging
or cherry-picking its branch. The reconciler/materializer preserve terminal logical-unit state,
empty evidence, failures, board/time/source lineage, content identities, and idempotence. The
matched-liquidity runner now uses one explicit governed analytical session ledger rather than its
observed Canonical Trades partitions. Therefore 2026-08-25 and 2026-09-04 are valid targets but
remain data-missing after the 2026-08-11 retained endpoint; ADTV20/60 is still zero with no future
leak or authority promotion. The 65-session required union is confirmed. Next gate is owner review
for Anti-Gravity data resumption only. Evidence:
`operations-review/canonical-trades-toolchain-restoration-session-calendar-fix-v1-20260906/`.

**Market-wide historical matched liquidity and ADTV foundation V1 (2026-09-05):**
`MARKET_WIDE_HISTORICAL_MATCHED_LIQUIDITY_AND_ADTV_FOUNDATION_V1 = COMPLETE /
PARTIAL_BY_EVIDENCE` at local checkpoint `8582810`, under explicit
`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_MARKET_WIDE_HISTORICAL_MATCHED_LIQUIDITY_AND_ADTV_FOUNDATION_V1`.
The retained primary canonical DNSE Trades corpus is now governed as a 40-session, 18,109,141-row
input with unit completeness and source identity, `G1` regular-board scope, and no board-family
mixing. Existing exact-reconciled G1 value cells (13,196) are reusable only at their exact daily
scope; generic matched-share volume remains unqualified. Since the corpus stops on 2026-08-11,
both required replay targets (2026-08-25 and 2026-09-04) fail closed: 0 exact ADV20/ADV60/ADTV20/
ADTV60 across the 1,683-ticker current governed universe. The only product wiring is disabled-by-
default, self-verifying evidence-context pass-through; no provider, daily operation, database,
PIT/RAW_AS_TRADED, execution, participation, slippage, sizing, ranking, score, target, policy,
publication, or deployment authority changed. Evidence:
`operations-review/market-wide-historical-matched-liquidity-adtv-foundation-v1-20260905/`.

**Corporate Intelligence catalyst/event/risk decision axis V1 (2026-09-05):**
`CORPORATE_INTELLIGENCE_CATALYST_EVENT_RISK_DECISION_INTEGRATION_V1 = COMPLETE /
PARTIAL_BY_EVIDENCE` at local checkpoint `8d62313`, begun from owner-specified
`3d9574942af1dc66b65771701a7e959aceca115b`. Explicit owner authorization opened it despite
`queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_CORPORATE_INTELLIGENCE_CATALYST_EVENT_RISK_DECISION_INTEGRATION_V1`);
no dependency or successor is inferred. Inventory first: the existing corporate-event stack
(`current_corporate_event_context.py`, `market_wide_current_corporate_intelligence.py`,
`official_corporate_action_ledger.py`, `bitemporal_semantic_contract.py`) already carried a rich,
deduplicated, conflict-checked event evidence contract, already built fresh each session inside
`canonical_post_close_pipeline.build_enrichment_components()` -- but never threaded into
`integrated_investment_decision_product.build_artifact()` (confirmed by zero corporate/catalyst/
event grep matches in either that file or `daily_integrated_decision_brief.py`). New
`current_corporate_intelligence_axis.py` adds only the vocabulary that did not exist anywhere:
a canonical 20-value event taxonomy, a 7-value status ladder, deterministic catalyst/risk/mixed/
informational classification (never keyword sentiment), and materiality that fails closed to
`POTENTIALLY_MATERIAL` (never `MATERIAL`). It activates `current_corporate_event_context`'s
existing but previously-unused `supplemental_events` parameter to surface the retained HPG/VNM/VCB
issuer chains, without modifying that shared component. Wired additively as a 9th
`CORPORATE_INTELLIGENCE` evidence axis via the standing `_axis()` helper; built with its own local
try/except in `canonical_post_close_pipeline.py` (mirroring the existing `tactical_boundaries`
isolation pattern) so a corporate-evidence failure cannot cascade into failing the whole Integrated
Decision build, proven both by source inspection and a live monkeypatched-failure test.
`decide_research_action_posture`'s exact signature is unchanged, verified by direct inspection, not
just behavior. Independently discovered, documented, not fixed: `current_official_event_context`'s
retained artifact has had `research_session` frozen at `2026-08-21` since before this milestone, so
the pre-existing `corporate_event_context` enrichment component has been silently degrading to a
frozen prior-as-of artifact every day since; this axis surfaces that staleness explicitly per ticker
(`evidence_session_stale`) instead of hiding it. Self-caught before commit: the first classification
rule marked all 2,578 real retained AGM/governance events `MIXED`, inconsistent with their own
pre-existing `NON_MATERIAL` materiality tag; fixed to be materiality-aware (real-corpus `MIXED`
count dropped from 1,464 to 7). Real market-wide replay: 1,507 denominator, 1,103 tickers with
retained evidence, 4,453 deduplicated events, 0 active-catalyst/active-risk tickers as of the
frozen evidence session (reported honestly). Required case review confirmed live for FPT/HPG/SSI/
QNS/PVD/PNJ/VNM/VCB with concrete dividend (VNM), capital/share-issuance (VCB), and corporate-action
(HPG) examples. Temporal replay (2026-08-25): `future_event_leak_admitted = 0`, proven by unit
tests plus a live pipeline replay. `prospective_decision_retention.py` tracks the new axis's T0
completeness (mirroring the existing `PORTFOLIO_FIT` special case) without retrofitting legacy
snapshots. `export_ai_bundle.py` and `daily_integrated_decision_brief.py` gain matching opt-in AI
pass-through. 51 new tests in the new module plus 18 targeted additions across 4 adjacent files;
every pre-existing adjacent test re-run passes unchanged except one expected 8-to-9-axis-set
update. A broader `export_ai_bundle.py` sweep's 32 failures were independently verified pre-existing
and unrelated (temporary single-file revert to clean HEAD reproduced identical failures). No
provider, policy, score, target, probability, EVENT_DRIVEN eligibility, PIT, RAW_AS_TRADED,
database, publication, deployment, or authority change; `research_action_posture` is unchanged.
Flagged via `spawn_task`, not fixed: five live spellings of "bonus issue" and two independently-
diverging corporate-action ledgers in the raw layer, pre-existing and out of this milestone's
additive scope. Evidence:
`operations-review/corporate-intelligence-catalyst-event-risk-decision-integration-v1-20260905/`.

**Prospective decision retention and outcome maturation V1 (2026-09-05):**
`PROSPECTIVE_DECISION_RETENTION_AND_OUTCOME_MATURATION_V1 = COMPLETE /
PARTIAL_BY_EVIDENCE` at local checkpoint `9f98ac2`, begun from owner-specified
`c7ce041d7a6fefe84fb1afcb94dfc3d4dc95a90c`. Explicit owner authorization opened it despite
`queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_PROSPECTIVE_DECISION_RETENTION_AND_OUTCOME_MATURATION_V1`);
no dependency or successor is inferred. The extension adds one content-addressed immutable T0
snapshot at canonical post-close, before its handoff, binding each record to session, ticker,
Integrated Decision, Daily operation, source artifact, all retained axes/coherence/priority/why-now
context, existing boundary semantics, and T0 close lineage. Identical warm reruns reuse bytes;
changed decisions get a distinct snapshot. It reuses the existing tactical-boundary contract and
close bridge: fixed existing close/level conditions may evaluate downstream; dynamic/narrative
conditions remain explicitly non-evaluable; horizons are governed trading-session maturity only.
The Sep-04 `55173…` handoff versus `8a0e…` mutable working-artifact mismatch is a
`RECOVERABLE_IDENTITY_BINDING_DEFECT`, but lacks immutable historic provenance and remains
excluded. The present retained corpus has no new snapshot or mature outcomes, so readiness—not
policy performance—is proven. No provider, policy, score, target, probability, execution, PIT,
RAW_AS_TRADED, database, publication, deployment, or authority change. Evidence:
`operations-review/prospective-decision-retention-outcome-maturation-v1-20260905/`.

**Prospective decision outcome feedback and policy diagnostics V1 (2026-09-05):**
`PROSPECTIVE_DECISION_OUTCOME_FEEDBACK_AND_POLICY_DIAGNOSTICS_V1 = COMPLETE /
PARTIAL_BY_EVIDENCE` at local checkpoint `3d1b41d`, begun from owner-specified
`5e5d4656cedc1f76c2e2fcaa19705a23d54ea39e`. Explicit owner authorization opened it despite
`queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_PROSPECTIVE_DECISION_OUTCOME_FEEDBACK_AND_POLICY_DIAGNOSTICS_V1`);
no dependency or successor is inferred. The extension reuses the standing prospective close bridge
and retained exact-session P3F9B evidence, adds session-counted T+1/T+3/T+5/T+10/T+20 endpoint
lineage/fitness plus close-only `CLOSE_MFE`/`CLOSE_MAE`, and admits only a canonical-handoff /
Daily-operation / observed-time-bound decision artifact. It is downstream of Daily decision
production and is rolled forward post-Producer before the current handoff is written. Real corpus:
one genuine 2026-09-03 artifact / 1,683 decisions; three current-view copies, one unknown, and
the 2026-09-04 candidate excluded on handoff/artifact identity mismatch. With no later genuine
completed session, every forward horizon is pending; no false-negative, failed-setup,
posture/coherence/axis differentiation, or policy change is claimed. The older artifact lacks
additive axis snapshots and forward-evaluable trigger/invalidation conditions, recorded fail
closed rather than reconstructed. Evidence:
`operations-review/prospective-decision-outcome-feedback-policy-diagnostics-v1-20260905/`.
No provider, policy, score, target, probability, execution, PIT, RAW_AS_TRADED, database,
publication, deployment, or authority change.

**Integrated Decision evidence-axis coherence V1 (2026-09-05):**
`INTEGRATED_DECISION_EVIDENCE_AXIS_COHERENCE_V1 = COMPLETE / PARTIAL_BY_EVIDENCE` at local
checkpoint `f2bb599`, begun from owner-specified `bc9e5a05ab14b060163128d73243c08e4fedea0b`.
Explicit owner authorization opened it despite `queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_INTEGRATED_DECISION_EVIDENCE_AXIS_COHERENCE_V1`);
no dependency or successor is inferred. The small coherent extension closes the real canonical
gap: post-close now builds, retains, and supplies the already-established momentum and tactical
confirmation artifacts to Integrated Decision, using the same retained technical recovery,
Tactical V3, and participation evidence, with no new provider or technical engine. Integrated
Decision exposes explicit distinct evidence axes and a deterministic qualitative coherence
relationship, never a score/vote/probability; Daily local AI passes it through. Primary
2026-09-04 retained replay: 1,683 records; Fundamental 1,363, Valuation 1,009, Structure 952,
Momentum 952, Participation/Confirmation 915, Market/Sector 1,683, Priority 1,507, all major
axes 797; coherence 116 ALIGNED / 529 PARTIALLY_ALIGNED / 0 MIXED / 307 CONTRADICTED / 731
INSUFFICIENT_EVIDENCE. Same-input policy baseline proves 0 posture/trigger/invalidation changes.
The 2026-08-25 temporal replay detects retained financial evidence labelled 2026-Q4 and excludes
it locally (financial/valuation fail closed, `future_leak_admitted=0`), so outcome is partial by
evidence and this is retained as a PIT-only diagnostic, never promoted. No provider, database,
publication, scoring, target, sizing, execution, or authority change. Evidence:
`operations-review/integrated-decision-evidence-axis-coherence-v1-20260905/`.

**Market-wide fundamental valuation analytical product V1 (2026-09-05):**
`MARKET_WIDE_FUNDAMENTAL_VALUATION_ANALYTICAL_PRODUCT_V1 = COMPLETE / PARTIAL_BY_EVIDENCE`
at local checkpoint `5201fb9463d683d20f4cb1f6c02113eaa39afe15`, begun from owner-specified
`5438bc09dc288aaceb53e31b73e1bfdb610e2035`. Explicit owner authorization opened this large
analytical-product milestone despite `queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_MARKET_WIDE_FUNDAMENTAL_VALUATION_ANALYTICAL_PRODUCT_V1`);
no dependency or successor is inferred. Inventory found the fundamental engine
(`financial_analysis_engine_v2.py`, `current_research_valuation_context.py`) already carried
essentially every capability the milestone named -- growth, turnaround states, margins,
average-balance ROE/ROA, cash quality, FCF proxy, leverage, working capital, specialist
families, and peer/own-history percentile context -- so the real work was three additive,
surgical changes rather than a rebuild. Promoted `market_wide_calculation_readiness.py`'s
already-activated-but-unwired EV/EBITDA into a new, separately-named, peer-eligible method
(`EV/EBITDA_CALC_READY`; 0 -> 51 tickers ready on a non-degraded session). Found and fixed a
real defect that silently disabled own-history valuation context for every ticker since it
first shipped (a field-name mismatch, `percentile_in_history` vs. the real `percentile`); real
impact is 1,093/1,683 tickers gaining genuine own-history evidence. Added
`evaluate_financial_composite_context`, a pure additive join of the unmodified
`fundamental_state` and `valuation_context_summary` into `FUNDAMENTALS_IMPROVING/STABLE/MIXED/
DETERIORATING/TURNAROUND_EVIDENCE/INSUFFICIENT_EVIDENCE`, wired into the Integrated Decision
product and the AI-facing Daily brief with zero change to `research_action_posture` or decision
identity. An incidental, pre-existing, out-of-scope gap (the standing Daily pipeline never
wires `momentum_context`/`tactical_confirmation_context` into the Integrated Decision artifact)
was discovered and recorded transparently, not fixed. Retained real replay for 2026-09-04
(primary) and 2026-08-25 (temporal boundary), each rebuilt before/after this milestone's code
via `git stash` to isolate its own contribution; fundamental coverage is byte-identical across
sessions, evidencing no temporal leakage. No provider, PIT, liquidity/sizing, execution,
universal score, target price, probability, or authority-promotion change occurred; no
duplicate analytical engine was introduced. See `docs/STATE.md` and `docs/DECISIONS.md` for
full detail and
`operations-review/market-wide-fundamental-valuation-analytical-product-v1-20260905/` for
evidence. No successor is queued.

**Financial temporal semantic normalization and analytical panel V1 (2026-09-05):**
`FINANCIAL_TEMPORAL_SEMANTIC_NORMALIZATION_AND_ANALYTICAL_PANEL_V1 = COMPLETE / PARTIAL_BY_EVIDENCE`
at local checkpoint `8f9be00`, begun from owner-specified
`f78bf4069abe7b0173b5aecbc350fcd270351087`. Explicit owner authorization opened it despite
`queued_next=[]`
(`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_FINANCIAL_TEMPORAL_SEMANTIC_NORMALIZATION_AND_ANALYTICAL_PANEL_V1`);
no dependency or successor is inferred. Root-caused the prior milestone's unresolved
39,568/94,252 unknown-duration/timestamp-missing corpus using the A-H taxonomy: the dominant
cause is a zero-silent-drop no-raw-evidence placeholder (not a semantic ambiguity); the two
genuine ambiguity causes (VCI's structurally-absent duration marker, thin cash-flow depth) are
both proven source-evidence ceilings via `structured_financial_period_semantics.py`'s new
`period_duration_root_cause`/`timestamp_root_cause` fields (verified zero uncaused facts across
261,360 facts / 1,492 tickers). Fixed a real timestamp-representation defect
(`canonical_financial_facts._normalize_observed_at`, 126,070 facts recovered from unparseable to
timezone-aware, zero fabricated) and wired the previously-unwired TTM/de-cumulation bridge into
the live Financial V2 path -- catching and fixing a real regression
(`current_research_ready_count` 1380->1276->1380) before this checkpoint. New
`canonical_financial_analytical_panel.py` consolidates the period-semantics, bitemporal, and
TTM-bridge layers into the one requested canonical boundary without duplicating any of them.
Currency/scale/statement-scope confirmed genuinely unresolved from source, not touched.
Financial/valuation/PIT authority, provider set, formulae, and score authority are unchanged;
`current_research_ready_count` unchanged at 1,380/1,492. See `docs/STATE.md` and
`docs/DECISIONS.md` for full detail and
`operations-review/financial-temporal-semantic-normalization-and-analytical-panel-v1-20260905/`
for evidence. No successor is queued.

**Core financial data to fundamental valuation scaleout V1 (2026-09-05):**
`CORE_FINANCIAL_DATA_TO_FUNDAMENTAL_VALUATION_SCALEOUT_V1 = COMPLETE / PARTIAL_BY_EVIDENCE`
at local checkpoint `48af3a41f1bd6bba46fb4ea569622dcd2c076fe7`, begun from owner-specified
`b6d28608f8550c09fb725f64f33884c868b5754d`. Explicit owner authorization opened it despite
`queued_next=[]` (`OWNER_AUTHORIZATION_2026_09_05_QUEUED_NEXT_EMPTY_CORE_FINANCIAL_DATA_TO_FUNDAMENTAL_VALUATION_SCALEOUT_V1`);
no dependency or successor is inferred. The retained Financial V2 semantic fact store, engine,
compact product, valuation/peer gates, and calculation-readiness lane are reused unchanged.
The additive product change exposes the existing compact Financial V2 context and existing
method-level valuation/reconciliation gates per Daily watchlist ticker, excluding fair value,
target-price, and probability fields. The feature-fitness catalog now explicitly names the
financial families and records financial PIT backtesting as blocked rather than inferring an
as-of claim. Retained 2026-09-04 evidence: 195,552 semantic facts / 1,492 tickers; 1,476
Financial V2 AVAILABLE plus 207 explicit ABSENT contexts over 1,683 Daily tickers; 1,363 integrated
fundamental contexts; watchlist local-AI financial and method-level valuation exposure 0 -> 11.
The 2026-08-25 replay rejects 94,252 missing-timestamp facts, uses 101,300 timestamped facts at
or before target with zero post-target facts, and uses the target session's own share-resolution
artifact. No provider, financial/valuation/PIT authority, formula, target/probability, database,
remote, publication, or deployment action was added. See
`operations-review/core-financial-data-to-fundamental-valuation-scaleout-v1-20260905/`.

**Market-data historical-series redundancy and feature-safe failover V1 (2026-09-05):**
`MARKET_DATA_HISTORICAL_SERIES_REDUNDANCY_AND_FEATURE_SAFE_FAILOVER_V1 = COMPLETE /
PARTIAL_BY_EVIDENCE`, begun at the owner-specified `fe199796` and implemented at `21ae21d`.
Explicit owner authorization opened it despite `queued_next=[]`; no predecessor was inferred.
The existing DNSE technical-history recovery is extended with independently retained KBS/VCI
provider series, exact target-session-close compatibility, no series splicing, and an ordered
DNSE -> KBS -> VCI route (a KBS clean miss does not spend VCI). Bounded live 2026-09-04 evidence
for 11 diverse tickers found all three providers' close histories target-compatible; KBS's
exclusive-end query boundary was corrected. KBS/VCI are therefore feature-safe only for
Current-Research close history, Tactical V3 structure, and momentum; their volume, participation,
OHLC geometry, PIT, execution/liquidity, and RAW_AS_TRADED uses remain blocked. The canonical
Daily entrypoint now runs the existing recovery tool once under a single Vnstock governor scope.
Retained 2026-09-04 replay restores structure/momentum availability 0 -> 918 of 952 eligible,
with 34 exact-close mismatches correctly unavailable; 2026-08-25 temporal replay is clean.
No provider or authority was promoted and no successor is queued. Artifact:
`operations-review/market-data-historical-series-redundancy-and-feature-safe-failover-v1-20260905/`.

**Core operating spine, provider resilience, feature fitness, and publication V1 (2026-09-05):**
`CORE_OPERATING_SPINE_PROVIDER_RESILIENCE_FEATURE_FITNESS_AND_PUBLICATION_V1 = COMPLETE` at
`ec86754`, started at `a032c49`. Owner-authorized architecture-stabilization override, scoped to
making the operating spine (acquisition -> retained evidence -> classification -> feature-specific
fitness -> deterministic analytics -> strategy -> local AI artifacts -> Git transport/Dashboard)
explicit and governed before further feature/analytical expansion. Reconstructed and verified the
real Daily execution graph against code (canonical Daily never touches `vn_stock.db`; 8 idempotency
guards explain warm-rerun speed). Found and fixed a real, previously-undiscovered defect:
`market_wide_current_descriptive_research.py` (an earlier, independent consumer of the DNSE
technical-history recovery artifact) lacked the target-session-close-agreement guard
`technical_structure_context.py`'s own prior corrective already proved necessary and shared with
`tactical_momentum_context.py`/`market_wide_relative_volume_research.py` -- now all four consumers
enforce one invariant. New `feature_input_fitness_contract.py` consolidates 19 use-case families
(`CURRENT_SESSION_PRICE`, `RELATIVE_VOLUME`, `TECHNICAL_CLOSE_HISTORY`, `VALUATION_PRICE`, etc.),
each pointing at the exact existing authority that governs it, never re-implementing any of them.
New genuine `--local-only` Daily mode: `ai_handoff_publication.publish(local_only=True)` and
`dashboard_release_publisher.publish_dashboard_release(local_only=True)` provide a structural
zero-Git-mutation guarantee (proven by diffing a real repository's HEAD/status/files before and
after), closing the gap where `--replay-local`'s `push=False` still left two real local commits in
the AI-handoff repo. Provider routing for 2026-09-04 extracted directly from retained real evidence
(DNSE 0/1683 at acquisition, KBS 957/1506 recovered, VCI 1/2, final 958/1683 resolved, rate
governor never breaching its 45/60 rpm ceiling); `TACTICAL_MOMENTUM_PARTICIPATION_CONFIRMATION_V1`
independently re-verified `ACCEPT_AS_IS`. Residual, flagged-not-fixed finding: ~15 standalone
`vnstock`-calling sync scripts never install the shared rate governor (out of canonical Daily's own
execution graph, hence out of scope). No provider added; no RAW_AS_TRADED/PIT/liquidity/execution/
score/target/probability authority promoted. Artifact:
`operations-review/core-operating-spine-provider-resilience-feature-fitness-publication-v1-20260905/`.
No successor was opened.

**Tactical momentum and participation confirmation V1 (2026-09-05):**
`TACTICAL_MOMENTUM_PARTICIPATION_CONFIRMATION_V1 = COMPLETE` at `6f08c17`, started at
`18a88b2656bbfdca0fd64cfa7b6bf90a71578a26`. Explicit owner authorization opened this milestone
despite `queued_next=[]`. New `tactical_momentum_context.py` (RSI-14, RSI divergence via the
existing confirmed-swing machinery, MA20/50/100/200, MACD 12/26/9) and `tactical_confirmation_
context.py` (structure+momentum+participation synthesis into CONFIRMED/PARTIALLY_CONFIRMED/
NEUTRAL/CONTRADICTED/INSUFFICIENT_EVIDENCE, no vote-counting) join the existing tactical
structure engine. `market_wide_relative_volume_research.py` gains an additive
`resolve_records_with_recovery` helper recovering participation coverage from 0/1683 to 918/1683
on the real 2026-09-04 wholesale-KBS-degraded session, without changing its own volume checks.
Both new engines and the participation fix reuse the exact-session-close compatibility guard
(`technical_structure_context.resolve_target_session_observations`). Wired additively into
`integrated_investment_decision_product.py`: `research_action_posture` is proven unchanged
(identical with momentum/confirmation `CONFIRMED`, `CONTRADICTED`, or omitted). Retained replay:
RSI 918/1683, MACD 907/1683, participation 918/1683; confirmation distribution 331 CONFIRMED /
272 CONTRADICTED / 185 PARTIALLY_CONFIRMED / 127 NEUTRAL / 768 INSUFFICIENT_EVIDENCE. 2026-08-25
temporal regression clean (zero look-ahead, mismatch-guard, or divergence-backdate violations).
FVG/Order Block/liquidity-sweep stay `DEFERRED_INPUT_BASIS_NOT_QUALIFIED` (high/low basis
unchanged); not built, no successor opened for them. Valuation/financial semantics unchanged
(reused verbatim). Artifact:
`operations-review/tactical-momentum-participation-confirmation-v1-20260905/`. No successor was
opened and no execution, publication, raw-as-traded/PIT, liquidity/sizing, target, probability,
or universal-score authority was added.

**Core Daily decision coherence and valuation integration V1 (2026-09-05):**
`CORE_DAILY_DECISION_COHERENCE_AND_VALUATION_INTEGRATION_V1 = COMPLETE` at
`dbad7c386cea10e752ff26abcecd6f0635ba6e89`, started at
`62db133e00bb83ae12b93ed3f31628b133d66daa`. Explicit owner authorization opened
this milestone despite `queued_next=[]`; no dependency was inferred. The retained 2026-09-04
replay found that 952 Tactical V3 eligible tickers were reaching the structure consumer with only
one observation despite a retained complete-history recovery artifact. The pipeline now validates
and consumes that existing recovery lineage, without changing tactical rules: all 952 recover
multi-session history and the all-`INSUFFICIENT_HISTORY` failure disappears. The valuation lane
now records the existing exact DNSE thousands-of-VND-to-VND representation contract, preserves
provider-native inputs, and fails cross-currency or unproven-scale multiples closed (PVD and VNM)
rather than guessing FX or a multiplier. The existing calculation-readiness engine is attached as
a separate provider-reported, period-aware companion, never a replacement valuation authority.
Retained 1,683-ticker replay: 47 `INITIATE_ON_BREAKOUT`, 74 `ACCUMULATE_ON_RETEST`, 180
`EARLY_WATCH`; all 72 Priority-Now/entry-relevant records have an explicit explanation (70
`LEGITIMATE_POLICY_OUTCOME`, 2 `MISSING_HISTORY_OR_FEATURE_FITNESS` after the terminal-review
corrective below).
Artifact: `operations-review/core-daily-decision-coherence-and-valuation-integration-v1-20260905/`.
No successor was opened and no execution, publication, raw-as-traded/PIT, target, probability,
or universal-score authority was added.

**Terminal acceptance review corrective (2026-09-05) at `9ab5a55`:** the recovery
override was being adopted even when its own target-session close disagreed with the multi-source
exact-session snapshot's already-resolved close (34/952 recovered tickers, up to ~11% apart,
because the snapshot fell back to a KBS sole-source print before DNSE published that day).
`technical_structure_context.py` now requires exact agreement before adopting the recovery series;
a mismatch falls back to the pre-recovery P3F9B-only (insufficient-structure) behavior. No
temporal or look-ahead defect was found. See `docs/STATE.md` for detail.

**Core valuation method coverage and consistency V1 (2026-09-05):**
`CORE_VALUATION_METHOD_COVERAGE_AND_CONSISTENCY_V1 = COMPLETE` at `35bb7e066a7f2bf8d4db6dd924e6e10a241f7da6`.
Activated a complete, previously-unregistered, never-run-with-real-inputs valuation engine
(`market_wide_calculation_readiness.py`: EBITDA, enterprise value, EV/EBITDA, P/E, P/B, ROE)
rather than building a new one -- the existing `current_research_valuation_context.py` lane
(P/E, P/B, P/S, EV/Sales, peer-relative, history-context) is untouched. Fixed a price
unit-conversion gap (`vn_stock.db` OHLC close read with no thousands-of-VND-to-VND conversion,
now routed through the existing `price_representation_contract`), wired real session
price/shares into the reporting CLI for the first time (`--session-date`, backward compatible
when omitted), and fixed an empty-reason gap in the negative/zero-EBITDA block path. Real
1,492-ticker replay: `market_capitalisation` 0 -> 848 ready, `enterprise_value` 0 -> 786, `pe`
0 -> 839, `pb` 0 -> 825, `ev_ebitda` 0 -> 84, all `provider_reported` tier (never `qualified`).
Full detail in `docs/STATE.md`. Artifact:
`operations-review/core-valuation-method-coverage-and-consistency-v1-20260905/`. No successor
is queued.

**Daily 2026-09-04 terminal corrective and canonical operational acceptance (2026-09-04/05):**
`DAILY_20260904_TERMINAL_CORRECTIVE_AND_ROADMAP_RESUME_V1 = ACCEPTED`. Closes the 2026-09-04
Daily incident: reconciliation against real retained evidence found no fan-out or runtime
defect (the activity-aware sentinel and rate governor both worked as designed against a
genuine same-evening DNSE gap), fixed the one real defect
(`recovery_candidates()` reading the prior session's baseline instead of the current session's
own snapshot, commit `c7982bd`), and accepted the first-ever canonical `.\stocklookup.ps1 daily`
run for 2026-09-04 (958/1506 = 63.6% current-research coverage, explicit partial). Full detail
in `docs/STATE.md` and `docs/DECISIONS.md` (2026-09-04 section).

**Canonical daily Financial V2 and current research enrichment V1 (2026-09-03):** `CANONICAL_DAILY_FINANCIAL_V2_AND_CURRENT_RESEARCH_ENRICHMENT_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE` at `f3880c589d345ff20e67fd2df84acdaf57f4681e`. Closes the exact Financial V2 daily-materialization gap `DAILY_INTEGRATED_DECISION_BRIEF_AND_PROSPECTIVE_FEEDBACK_V1` found and deliberately deferred: `canonical_post_close_pipeline.py`'s `_integrated_investment_decision_product()` fed a structurally incompatible legacy fundamental artifact (or nothing) and a raw, unevaluated valuation artifact into `integrated_investment_decision_product.build_artifact()`, silently degrading `fundamental_state` to `INSUFFICIENT` for every ticker every session and blocking real valuation context. New `financial_v2_current_input_authority.py` (a small, pinned, versioned pointer to the retained Financial V2 evidence chain, never a filesystem "latest folder" scan) and `canonical_daily_financial_v2_materialization.py` (builds the engine artifact, the compact product over the Daily Product's own ticker denominator with zero silent drops, peer context, and the evaluated valuation artifact -- pure orchestration over already-governed `financial_analysis_engine_v2`/`market_wide_financial_analysis_v2_scaleout`/`financial_analysis_product_projection`/`current_research_valuation_context` builders, no duplicated formulas) now materialize both fresh each session and persist them at new canonical per-session paths. `integrated_investment_decision_product.build_artifact()` gained a fail-closed contract-shape assertion (present-but-wrong `financial_analysis_artifact.contract_version` now raises; absent stays permitted for existing lightweight fixtures) so this exact defect class cannot recur silently. Real replay (`tools/run_canonical_daily_financial_v2_replay.py`) against retained 2026-08-25/2026-08-28 evidence: independent-replay-exact Financial V2 denominator 1,492 / `current_research_ready` 1,380; `fundamental_state` before/after goes from 100% `INSUFFICIENT` to a real evidence-driven distribution; 2026-08-28's after-fix posture distribution matches `docs/ROADMAP_STATE.json`'s own regression-locked `INTEGRATED_INVESTMENT_DECISION_PRODUCT_V1` figures exactly; financial evidence identity is byte-identical across both sessions while each session's own delivery-wrapper identity correctly differs. `daily_integrated_decision_brief.py` and `export_ai_bundle.py` both gained additive financial-evidence exposure (session-scoped `financial_evidence_context`; auto-resolving `attach_financial_analysis_v2_product_context`) so a normal `stocklookup.ps1 daily` run needs no extra flags. 56 new/updated focused tests; full affected-area suite (550+ tests) shows zero new failures via exact failure-ID diffing against a git-stash baseline. Full detail in `docs/STATE.md`. No successor is queued.

**Integrated investment decision product V1 (2026-09-02):** `INTEGRATED_INVESTMENT_DECISION_PRODUCT_V1 = COMPLETE / FULL_UNIVERSE_INTEGRATED_PRODUCT_ACTIVE` at `9160188f4d5df1d056d184eeef7f26f52afe3419`. Delivers `integrated_investment_decision_product/v1` unifying Core Fundamental Valuation & Peer Context (`financial_analysis_product_integration/v1`, `current_research_valuation_context/v1`), Tactical Market Structure V3 (`technical_structure_context/v2`, `market_structure_breakout_product_projection/v1`), Relative Volume & Participation (`market_wide_relative_volume_research/v1`), and Market/Sector Leadership (`market_wide_current_descriptive_research/v1`) into a deterministic 9-state `research_action_posture` without universal scores, target prices, or probability claims. Universe denominator 1,683 tickers (zero silent drops). Tactical context available: 942, Fundamental context: 1,363, Valuation context: 955, Participation context: 943, Market/Sector: 1,683, Triggers: 942, Invalidations: 911, Portfolio: 1,683 not provided (non-penalizing). Posture distribution: WAIT_FOR_CONFIRMATION 679, AVOID 452, INSUFFICIENT_CURRENT_RESEARCH 200, ACCUMULATE_ON_RETEST 127, EARLY_WATCH 119, INITIATE_ON_BREAKOUT 75, HOLD_DO_NOT_ADD 24, HOLD 7. Extension risk is decoupled from fundamental rejection (extended securities receive HOLD_DO_NOT_ADD or WAIT_FOR_CONFIRMATION rather than AVOID). PNJ multi-session diagnostic confirms `TACTICAL_INFORMATION_NOT_INTEGRATED` as legacy miss reason, with integrated product producing INITIATE_ON_BREAKOUT on 2026-08-20 onset and transitioning to HOLD_DO_NOT_ADD on 2026-08-21. All 11 watchlist tickers replayed. Daily Producer / AI handoff (`export_ai_bundle.py`) opt-in attachment verified. 13/13 unit tests pass across all policy regressions A-H. Operations review artifact: `C:\Projects\StockLookup\operations-review\integrated-investment-decision-product-v1-20260902\`.

**Core analytical product rebaseline V1 (2026-09-02):** `CORE_ANALYTICAL_PRODUCT_REBASELINE_V1 = APPLIED / PRODUCT_CRITICAL_FEATURE_EXPANSION_ONLY`. Applied onto `14cc93ccc5cec97c1de865b69eba958f5f18ee7a` after Claude's completed `CORE_FUNDAMENTAL_VALUATION_AND_PEER_CONTEXT_V1`; that completion record is not overwritten. Current development priority is **CORE ANALYTICAL PRODUCT COMPLETION** — not market-wide data expansion as the default queue, and not a feature freeze. Near-term roadmap is exactly `CORE_FUNDAMENTAL_VALUATION_AND_PEER_CONTEXT_V1` = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE (checkpoint `5e58d79f69810d6800d1f58244c421acb0e4230f`, closeout `14cc93ccc5cec97c1de865b69eba958f5f18ee7a`) → `TACTICAL_MARKET_STRUCTURE_AND_BREAKOUT_V3` = QUEUED_NEXT (not started) → `INTEGRATED_INVESTMENT_DECISION_PRODUCT_V1` = QUEUED_AFTER_TACTICAL (not started). Queued does not mean started. Current Research / Product Mode stays separate from Audit / PIT / Exact Mode. Missing audit-grade authority blocks only the dependent exact use. Provider/research proxies may be used in Current Research when method, provenance, fitness, and limitations are explicit. Never invent monetary scale, unit compatibility, PIT authority, or execution authority. Feature engines own measurements; strategy owns thresholds/policy. Peer comparisons require comparable metric method/provider/scope. BOS/CHoCH/VCP are deterministic technical inference, not institutional/order-flow proof. Prospective false-negative/false-positive review is a product capability. UI/Dashboard frozen until the final integration pass. No standalone Interest Coverage, Insurance, forensic-accounting, monetary-basis, VCI-duration, absolute-liquidity, or further specialist micro-milestones unless they directly block one of the three product milestones. No universal scoring system is required. Feature layout: `docs/ANALYTICS_AND_DECISION_FEATURE_SPEC.md`.

**Tactical market structure and breakout V3 (2026-09-02):** `TACTICAL_MARKET_STRUCTURE_AND_BREAKOUT_V3 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. Extends governed `technical_structure_context.py` to `v2` per the AGENTS.md edit-over-proliferate rule (reusing the close-only engine; `price_structure_breakout_context.py` belongs to the legacy daily research bundle; no competing engine built). All V1 output keys preserved verbatim. Additive V3 keys per eligible ticker record: `swing_structure` (confirmed fractal swings N=2, no lookahead; 185 UPTREND, 372 DOWNTREND, 173 EARLY_BULLISH_REVERSAL, 181 EARLY_BEARISH_REVERSAL, 31 INSUFFICIENT_HISTORY), `bos_context` (BULLISH 74 / BEARISH 71; close-through confirmed level by rule; not institutional activity; explicit governance warning), `choch_context` (BULLISH 53 / BEARISH 26; first structural break against established structure; not smart money), `pivot_context` (942 valid pivots; last confirmed swing high or V1 resistance fallback), `breakout_state_v3` (BREAKOUT 123, EXTENDED_AFTER_BREAKOUT 35, FAILED_BREAKOUT 10, TESTING_PIVOT 272, BELOW_PIVOT 502; measurement only, BREAKOUT != BUY), `trigger_context` (PIVOT_BREAKOUT_TRIGGER 423, CONFIRMED_BOS_TRIGGER 69, RETEST_BROKEN_PIVOT 8, NO_TRIGGER 442; not execution authority), `invalidation_context` (911 available; analytical context, not a stop-loss). Full universe denominator 1,683 / 942 eligible (STRUCTURE_READY) / 741 not eligible. Zero silent drops. ATR/wick/H-L geometry remain `NOT_AVAILABLE_HIGH_LOW_BASIS_NOT_COMPATIBLE` throughout; `candle_body_fraction` blocked as `NOT_AVAILABLE_OPEN_NOT_IN_RETAINED_SERIES`. PNJ diagnostic: `TACTICAL_SIGNAL_WAS_PRESENT` (breakout onset 2026-08-20 close 37.30 over pivot 36.50; extended through 2026-08-27; pullback on as-of 2026-08-28 into UPTREND). Product projection: `market_structure_breakout_product_projection.py` emits compact tactical records without raw histories. 72/72 tests pass (18 V1 + 54 V3 additive); adjacent test suites pass. `INTEGRATED_INVESTMENT_DECISION_PRODUCT_V1` remains DEFERRED and unauthorized. Artifact: `C:\Projects\StockLookup\operations-review\tactical-market-structure-breakout-v3-20260902\`.

**Core fundamental valuation and peer context V1 (2026-09-02):** `CORE_FUNDAMENTAL_VALUATION_AND_PEER_CONTEXT_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE` at `5e58d79f69810d6800d1f58244c421acb0e4230f`. Turns already-retained Financial V2 data into current-research fundamental/valuation/peer/history context. `financial_analysis_engine_v2.py` adds `same_provider_roe_avg_equity`/`same_provider_roa_avg_assets` (average of a quarter's own period-boundary same-provider balances, distinct from the pre-existing end-of-period proxies, which stay exactly as they were) and a new per-ticker `history_context` (retrospective, tie-aware percentile/range of a READY headline ratio against its own retained history); `readiness_features`/`current_research_ready` untouched. `current_research_valuation_context.py` adds `attach_engine_fundamental_peers` -- sector/industry (falling back to entity-class) peer median and tie-aware percentile for the same headline ratios, comparing only rows with an identical method, cohort, scope, currency, scale and period -- purely additive alongside the pre-existing, unmodified P/E, P/B, P/S valuation-and-peer route, which this milestone reuses via its own already-retained-compatible route rather than rebuilding. `financial_analysis_product_projection.py`'s compact projection now carries real ROE/ROA values and history context, not status-only. New `tools/run_core_fundamental_valuation_peer_context_replay.py` reproduces the regression-locked headline numbers exactly from tracked evidence alone: Financial V2 denominator 1,492, `current_research_ready` 1,380, entity family split 1,382/83/27 -- by layering the current tracked `entity_classification_contract` authority (including the 2026-09-02 legacy-recovery tier) over the older 2026-09-01 diagnostics snapshot. Several new counts read 0 market-wide in this specific replay for well-understood, explicitly-reported reasons rather than a defect: the tracked semantic-facts snapshot predates `gross_profit`'s 2026-09-02 and working-capital's 2026-09-01 canonicalization, and same-provider ROE/ROA (both new and the pre-existing EOP proxy) is genuinely rare given the corpus's dominant KBS-income/VCI-balance-sheet provider split (confirmed via `mixed_provider_roa_proxy`'s much higher count for the same tickers). Where the underlying facts predate that gap, real numbers are substantial: `equity_to_assets` history 1,093 `AVAILABLE`, peer context 1,024 `READY_RESEARCH_ONLY`; `net_margin` peer context 1,000 `READY_RESEARCH_ONLY`; 1,472/1,492 tickers resolve a genuine retained sector label. 26 new focused tests plus 95 adjacent regression tests pass; a full-suite controlled `git stash` comparison (~5,900 tests) shows zero new failures/errors/skips anywhere outside this milestone's own 4 new tests. No dashboard, stance, Tactical V2, portfolio, sizing, execution, or VCI-duration change. Artifact: `C:\Projects\StockLookup\operations-review\core-fundamental-valuation-peer-context-v1-20260902\`. No successor is queued.

**Securities specialist financial research foundation V1 (2026-09-02):** `SECURITIES_SPECIALIST_FINANCIAL_RESEARCH_FOUNDATION_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE` at `7bd048040f7c60d3532176df8e0a0f7ca833bed7`. Governed universe: 41 `entity_class=securities` tickers from the current tracked `entity_classification_contract` authority (no sibling artifact), cross-validated against the regression-locked 83 `OTHER_FINANCIAL_LIMITED_ANALYSIS` total (28 bank + 41 securities + 13 insurance + 1 finance_company). Retained-corpus inspection (not label similarity) found the securities-firm chart-of-accounts vocabulary already present verbatim in `data_bctc` for all 41/41 tickers with zero prior canonical consumer. Adds `securities_financial_research_component.py` (provider-agnostic `securities_financial_research_component/v1` contract, mirroring `bank_financial_research_component/v1`) and `securities_statement_capture_import.py` (native-vocabulary importer, mirroring `tcbs_bank_capture_import.py`). Reuses two already-shipped, item-id-agnostic period-semantics routes rather than reopening `VCI_PERIOD_DURATION_REMAINS_UNKNOWN`: balance-sheet point-in-time (provider-agnostic) and the KBS-quarterly `STANDALONE_QUARTER` income-statement contract (the same route `gross_profit` uses). `total_securities_operating_income`'s denominator compatibility was arithmetic-verified against SSI's own statement subtotal before any mix ratio used it. `financial_analysis_engine_v2.py` gains an additive `securities_*` feature/state family (`fvtpl_asset_intensity`, `margin_lending_asset_intensity`, `brokerage_revenue_mix`, `loan_interest_income_mix`, three trajectory states), gated strictly on `entity_class=securities`, mirroring the bank family's wiring exactly; `readiness_features`/`current_research_ready` and the `INDUSTRIAL`/`LIMITED` split are untouched. Real retained replay (`tools/run_securities_specialist_real_replay.py`, reads `data_bctc` directly, zero network calls, zero store writes): 41/41 governed tickers, `fvtpl_asset_intensity` 41/41 READY, `margin_lending_asset_intensity` 41/41 READY, `brokerage_revenue_mix` 41/41 READY, `loan_interest_income_mix` 38/41 READY, `current_research_ready` 0/41 (unchanged by construction). The three trajectory states show real distributions for the two balance-sheet ratios while `brokerage_mix` stays 100% `AVAILABLE` for all 41 tickers -- shallow retained KBS income-statement depth (at most 4 quarters) means no ticker has a same-quarter-prior-year income-statement pair; an evidence-driven asymmetry, not a defect. 70 new focused tests plus the full adjacent bank/engine/product-integration and entity-classification/scaleout suites pass (one pre-existing, unrelated `operations-review`-artifact-missing failure confirmed via `git stash` comparison); `py_compile`/`git diff --check` pass. Financial V2's 1,492 denominator, the 1,382/83/27 entity-family split, `current_research_ready` 1,380, `gross_margin` 1,283 READY, the FCF proxy's 319/284 READY counts, `current_ratio`/`net_working_capital` 1,276 READY, and bank specialist behavior are regression-locked by construction (zero modification to any pre-existing corporate/bank code path). No dashboard, ai-core, production database, stance, valuation, or VCI-duration change. Artifact: `C:\Projects\StockLookup\operations-review\securities-specialist-financial-research-foundation-v1-20260902\`. No successor is queued.

**Market-wide relative-volume research V1 (2026-09-02):** `MARKET_WIDE_RELATIVE_VOLUME_RESEARCH_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_HISTORY`. Activates only retained DNSE same-provider/same-native-field dimensionless comparisons: tie-aware current-session cross-sectional relative-volume percentile and current volume divided by the median of exactly the prior 20 completed sessions. DNSE OHLC `v` remains an undocumented native absolute unit; no shares/lots, monetary traded value, ADV/ADTV, liquidity, execution, position sizing, valuation, portfolio, Tactical V2, or stance authority changes. Offline retained 2026-08-28 replay over 1,683 governed active-equity candidates produces 943 percentile-ready rows, 942 20-session acceleration-ready rows, one insufficient-history row, zero zero-baseline rows, and zero future-session violations. HPG, VCB, and FPT are representative READY rows. The explicit AI handoff is hash-verified, compact, and always `is_actionable=false`. Artifact: `C:\Projects\StockLookup\operations-review\market-wide-relative-volume-research-v1-20260902\`.

**Market-wide gross margin depth V1 (2026-09-02):** `MARKET_WIDE_GROSS_MARGIN_DEPTH_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. Adds canonical `gross_profit` to `canonical_financial_facts.METRIC_REGISTRY`, gated `required_provider="KBS"` (a new additive `_Candidate` field) because the retained corpus shows the identical raw item id is also carried verbatim by 47 VCI-sourced income-statement files, and mapping those would opportunistically widen `VCI_PERIOD_DURATION_REMAINS_UNKNOWN`. `MAPPER_VERSION` 1.2.0 -> 1.3.0. `financial_analysis_engine_v2.py` gains `gross_margin`, `gross_margin_direction` (same-provider, same-quarter, prior-year), and a `gross_margin_trajectory_state`, joining `readiness_features` alongside the existing `net_margin`/`pbt_margin`. The legacy `market_wide_fundamental_feature_store.py` `gross_margin` proxy needed no code change and is proven to stay capped at `READY_RESEARCH_PROXY`, never competing with the new `READY`-capable path. Real replay over the full retained `data_bctc/*.parquet` corpus (a new `tools/run_gross_margin_real_replay.py`, zero network calls, zero shared-cache writes): 1,387 raw gross-profit tickers (47 VCI-sourced, correctly excluded), 1,341 canonical tickers, 1,338 compatible same-provider gross-profit/revenue ticker pairs, `gross_margin` 1,283 READY / 99 BLOCKED / 110 NOT_APPLICABLE, `gross_margin_direction` 45 READY. Financial V2's 1,492-ticker denominator, the 1,382/110 INDUSTRIAL/LIMITED split, and `current_research_ready` (1,380 -> 1,380, zero newly-ready tickers) are exactly regression-locked against the working-capital milestone's own retained full market-wide artifact. All 6 real bank tickers get `gross_margin=NOT_APPLICABLE`. Research stance is unchanged by construction: `security_decision_context._financial_analysis_annotation` reads a fixed named set of state keys that does not include the new trajectory state, mirroring how `working_capital_trajectory_state` was left unwired too. No successor is queued.

**TCBS bank capture import and real replay V1 (2026-09-02):** `TCBS_BANK_CAPTURE_IMPORT_AND_REAL_REPLAY_V1 = COMPLETE`. Adds the retained-only `tcbs_bank_capture_import.py` boundary for `tcbs_bank_public_company_capture/v1`: per-capture raw SHA and optional bundle SHA validation, recursive separator/case-insensitive privacy rejection before raw values are transformed, supported-tool mapping into `bank_financial_research_component/v1`, deterministic dedupe/conflict diagnostics, and empirical TCBS quarter semantics without PIT/audit/source-authority promotion. The real 18-capture public-company bundle covers ABB/ACB/BID/MBB/TCB/VCB and replays 60 observations for each mapped metric: deterministic NPL/LDR/CIR/provision-coverage/loan-growth are READY for all six banks; provider NIM is `RESEARCH_PROXY` only. Currency and scale stay UNKNOWN; absolute comparisons remain blocked. No TCBS client/OAuth, daily pipeline, canonical fact write, stance, valuation, liquidity, portfolio, tactical, dashboard, or AI change. No successor is queued.

**Bank specialist financial research foundation V1 (2026-09-01):** `BANK_SPECIALIST_FINANCIAL_RESEARCH_FOUNDATION_V1 = COMPLETE / COHERENT_PARTIAL_BY_MISSING_STRUCTURED_BANK_INPUT`. Adds a bounded bank-specialist family to Financial Analysis V2 -- `bank_financial_research_component.py` (a new provider-agnostic `bank_financial_research_component/v1` contract for a future retained bank-data importer; no TCBS call, no OAuth/MCP auth, no credential in repo) plus an additive `bank_*` feature family in `financial_analysis_engine_v2.py`: NPL ratio, loan-to-deposit, cost-to-income, provision coverage, same-quarter YoY loan growth (all deterministic, same-provider, same-period), and a NIM provider-derived proxy that is never computed from raw components (a bounded 2026-09-01 TCBS MCP probe against MBB/SSI/HPG reproduced CIR/LDR/NPL exactly but could not independently reconstruct NIM). Applies only when entity classification says bank; every other entity type gets explicit `NOT_APPLICABLE`, exactly mirroring how the existing generic-corporate family already treats banks. `build_ticker_context`/`build_artifact` take the new input as an optional, default-empty parameter, so Financial V2's 1,492-ticker denominator, corporate feature set, and research-stance machinery are structurally unaffected. Real replay over the 6 real bank tickers (ABB, ACB, BID, MBB, TCB, VCB) and their 864 real retained rows finds zero bank-specific raw components canonicalized yet, so all six features honestly report `BLOCKED_BY_EVIDENCE` / 0 READY -- `CAPABILITY_IMPLEMENTED / REAL_DATA_COVERAGE_ZERO_OR_PARTIAL_BY_INPUT`, not fabricated coverage. No successor is queued.

**Current research monetary basis normalization and valuation recovery V1 (2026-09-01):** `CURRENT_RESEARCH_MONETARY_BASIS_NORMALIZATION_AND_VALUATION_RECOVERY_V1 = COMPLETE / COHERENT_PARTIAL_BY_UNDOCUMENTED_PRICE_AND_SHARE_SCALE`. The predecessor's `0` compatible TTM/market-cap pairs was an unconditional code omission (`market_cap` never carried `currency`/`scale` at all), not a reasoned evaluation of the evidence. Added `monetary_basis_contract.py` as the one shared place a currency/scale sentinel (including a `str(None)` stringification defect fixed in `financial_analysis_engine_v2.py`) is recognized, and wired an honest, versioned monetary-basis envelope onto `market_cap` and every qualified TTM feature. On the real retained 2026-08-28 full-universe replay, DNSE close prices are 100% retained as `SOURCE_PRICE_UNIT_UNDOCUMENTED` and provider-reported share counts as `unit: "unknown"` -- genuinely unresolved, not merely unconsidered -- so market cap's basis is `UNKNOWN` for all 1,507 tickers and TTM currency/scale remain the pre-existing `"unknown"` sentinel for all 120 (net income) / 90 (revenue) `READY` tickers. `TTM_MARKET_CAP_MONETARY_BASIS_INCOMPATIBLE` stays exactly 18/32 and P/E-TTM / P/S-TTM `RESEARCH_USABLE` stays 0/0 -- an unchanged, now honestly-reasoned fail-closed outcome; a correct zero remains acceptable where no source contract exists. Financial V2 stays 1,492 and Product stays 1,699 with zero silent drops; research stance distribution, working-capital/debt coverage, peer machinery, and VCI duration UNKNOWN are unchanged and replay-confirmed. No successor is queued.

**Qualified TTM valuation research integration V1 (2026-09-01):** `QUALIFIED_TTM_VALUATION_RESEARCH_INTEGRATION_V1 = COMPLETE / COHERENT_PARTIAL_BY_MONETARY_BASIS_EVIDENCE`. Financial Analysis V2 qualified four-consecutive-quarter TTM net income/revenue now has precedence over an old Feature Store TTM, with source/context/feature/method/period/provider/currency/scale/fitness/conflict lineage retained; the old TTM remains an explicit fallback and no values are merged. PBT TTM is qualitative context only. On the retained full-universe replay, no TTM and current market-cap pair has independently known compatible currency and scale, so P/E-TTM and P/S-TTM correctly emit `INPUT_BLOCKED / TTM_MARKET_CAP_MONETARY_BASIS_INCOMPATIBLE` instead of a ratio. Financial V2 stays 1,492 and Product stays 1,699 with zero silent drops; working-capital/debt coverage, VCI duration UNKNOWN, and the stance machine are unchanged. No successor is queued.

**Screener master projection and Decision Drawer integration V1 (2026-09-01):** `SCREENER_MASTER_PROJECTION_AND_DECISION_DRAWER_INTEGRATION_V1`. Producer now emits `screener_master_projection/v1` as the single derived Screener presentation read model over the current canonical screen-snapshot ticker set. Real 2026-08-28 replay: denominator 1,683 unique tickers, zero duplicates, zero silent drops, 16 Workspace-only extras excluded. Price 943 available / 740 explicit-unavailable; session return remains a fraction; `HNX_LISTED` → `HNX` for all 299 listings (never DELISTED). True VCI industry labels cover 1,678 rows with 5 explicit UNKNOWN; entity class is separate and never used as sector. Research liquidity proxy remains visible on 935 rows while exact execution stays blocked on the full denominator; no numeric GTGD/ADV20 is invented. Compact Financial V2 join is 1,476 AVAILABLE / 207 ABSENT without dropping a ticker. Workspace join 1,683/1,683. Naked required-null count is 0. This projection is not a new factual authority. Dashboard Decision Drawer consumption is the paired surface. No successor is queued.

**VCI full statement depth and same-provider capital efficiency V1 (2026-09-01):** `COMPLETE / COHERENT_PARTIAL_BY_UNQUALIFIED_VCI_PERIOD_SEMANTICS`. Raw VCI statement retention and metadata/cache safety are retained, but the 15-name VCI/KBS corrective cohort establishes neither an explicit `lengthReport` duration rule nor a supported scale/equivalence rule. Duration-dependent VCI features remain fail-closed. No successor is selected.

**Market-wide working capital and short-term liquidity V1 (2026-09-01):** `MARKET_WIDE_WORKING_CAPITAL_AND_SHORT_TERM_LIQUIDITY_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. Root cause of the depth-recovery milestone's `SOURCE_EXPOSES_NOT_RETAINED`: `canonical_financial_facts.MAPPER_VERSION` was never bumped when `current_assets`/`current_liabilities`/finance-lease candidates were added to `METRIC_REGISTRY`, so `canonical_fact_store`'s incremental fingerprint (keyed on that version, not on registry content) kept every shard `unchanged` against the 2026-08-10 persisted store -- the raw VCI observations and the mapping were both already correct (verified directly against `HPG_balance_sheet_quarter.csv` and the retained layer-1 shard). `MAPPER_VERSION` 1.0.0 -> 1.1.0 forces a full rebuild: 195,552 -> 249,872 canonical facts (+54,320, all `POINT_IN_TIME_BALANCE_SHEET`). Also corrected two now-stale claims this exposed in `structured_financial_depth_recovery.py`: its hardcoded `capability_matrix` said `SOURCE_EXPOSES_NOT_RETAINED` for these three metrics while its own per-ticker `missing_components` already proved them retained (fixed to `RETAINED_AND_CANONICALIZED`); its finance-lease missing-entry was unconditional for every VCI ticker rather than evidence-gated like the working-capital check beside it (both now key off a new `usable_metrics` set, since Layer 3 emits an `unavailable`-status row for every registered metric on every ticker with any statement, so mere row presence in `existing_metrics` no longer proves retention post-fix). Real retained coverage: `current_assets`/`current_liabilities` usable on 1,351/1,492 tickers (10,040 `provider_reported` + 20 conflicted + 804 unavailable facts each), and every ticker with both sides retained forms a compatible same-ticker/provider/period/scope/currency/scale pair -- 1,351/1,351, zero representation-mismatch cases, because both lines share one balance-sheet payload; finance leases 41/1,492. New `financial_analysis_engine_v2` features (`net_working_capital`, `current_ratio`, `net_working_capital_direction`, `current_ratio_direction`) and states (`working_capital_state`, `working_capital_trajectory_state`, `current_ratio_trajectory_state`) reuse the existing `_same_period_pair`/`_ratio`/`_pit_ratio_direction_for` primitives verbatim (the last generalized to accept a `method` string, its one prior call site unaffected) plus one new `_difference`/`_pit_component_direction` pair for the plain value delta a percentage-of-prior cannot safely express once net working capital crosses zero. Real market-wide replay, reproduced against the actual 1,382-industrial/85-limited/25-unknown cohort (the real legacy-523 engine artifact plus entity-classification diagnostics from sibling worktrees, not a smaller stand-in): `current_ratio` and `net_working_capital` 1,276 READY (identical population to `debt_to_equity`/`equity_to_assets` -- the same balance-sheet-payload gate), their YoY directions 1,160 READY (identical to `debt_to_equity_direction`), `working_capital_state` 1,038 `POSITIVE` / 238 `NEGATIVE` / 216 `UNAVAILABLE`, `working_capital_trajectory_state` 731 `IMPROVING` / 429 `WORSENING` / 332 `UNAVAILABLE`, `current_ratio_trajectory_state` 627 `IMPROVING` / 533 `WORSENING` / 332 `UNAVAILABLE`. `current_ratio` was added to `readiness_features` (a same-provider P-I-T balance-sheet ratio, structurally the same kind as the `equity_to_assets`/`cash_to_assets` already there); `current_research_ready` is nonetheless unchanged at exactly 1,380/1,492 before and after, because every ticker that gains a READY `current_ratio` already had another READY balance-sheet ratio from the same payload. Every named regression lock holds exactly, before == after: `debt_to_equity`/`debt_to_assets` 1,276 READY, `debt_to_equity_direction` 1,160 READY, same-provider ROA/ROE 0 READY, resilience 2 STRESSED / 1,490 unavailable, cash-quality 3 HEALTHY / 3 WEAK, entity-family distribution 1,382/85/25, product denominator 1,699 = 1,492 AVAILABLE + 207 ABSENT with zero silent drops, and the six-label security-stance distribution byte-for-byte identical (183/344/30/52/188/902). No arbitrary current-ratio threshold, no current-liabilities-to-debt or total-liabilities-to-borrowing substitution, no fabricated resilience formula, no score/rank/target/probability/actionability, no new provider, no PDF/OCR, no stance-machine change. `financial_analysis_product_projection.py`'s compact contract gained three matching state keys (additive; `feature_fitness` already exposed the four new feature ids generically), verified through the existing opt-in `export_ai_bundle.py` attach path and the full-universe NDJSON delivery test, still exposing fitness/state only, never raw statement amounts. No ticker in the retained corpus reports `current_liabilities == 0` today; the zero-denominator behavior is proven by unit test instead. No successor is queued.

**Structured financial depth recovery V1 (2026-09-01):** `STRUCTURED_FINANCIAL_DEPTH_RECOVERY_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. The retained-only `structured_financial_depth_context/v1` rebinds 9,936 pre-existing VCI `short_term_interest_bearing_debt` + `long_term_interest_bearing_debt` component pairs across all 1,492 tickers into provenance-complete derived totals only when ticker, period, provider, source file/SHA, scope, unit representation, and P-I-T semantics match. Financial V2 therefore adds 1,276 exact same-provider `debt_to_equity` and `debt_to_assets` READY features and 1,160 debt/equity YoY directions; no total liabilities are treated as debt and an incomplete component set remains blocked. The VCI raw schema demonstrably exposes current assets/current liabilities and sampled short/long finance-lease lines, so their mappings/retention vocabulary is now present; those fields are absent from the committed wide canonical corpus for the 1,382 retained-VCI names and remain explicitly `SOURCE_EXPOSES_NOT_RETAINED` (the other 110 are `SOURCE_ROUTE_DEPTH_LIMIT`), with no current-ratio output. KBS remains the retained usable income/cash-flow route and VCI the usable balance route: 1,297 tickers have the confirmed provider split, so same-provider ROA/ROE remain 0 READY and mixed-provider ROA remains a proxy. OCF sign (322 READY), CFO/net income (0 READY; 6 labelled proxies), FCF (0), cash-quality (3 HEALTHY / 3 WEAK), resilience (2 STRESSED / 1,490 unavailable), growth history, and `current_research_ready` (1,380/1,492) are unchanged. Product replay remains 1,699 = 1,492 AVAILABLE + 207 ABSENT and preserves the locked six-label stance distribution. No provider, PDF/OCR, authority, PIT, score/rank, target/probability, actionability, Consumer, Dashboard, or stance change occurred. No successor is queued.

**Market-wide financial entity classification scaleout V1 (2026-09-01):** `MARKET_WIDE_FINANCIAL_ENTITY_CLASSIFICATION_SCALEOUT_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. The 966-ticker `UNCLASSIFIED_GENERIC_FINANCIAL_ANALYSIS` bucket was a `METADATA_EXISTS_NOT_CONNECTED` gap, not a missing capability: the already-tested multi-evidence classifier only ever reached 40 tickers. A new third Layered Authority Topology B tier (`config/promoted_entity_classifications_scaleout_v1.json`), strictly lower precedence than the seed CSV and the original P2E3 promoted manifest and never overriding either, reconciles three already-retained governed sources — exchange ICB industry-classification sync (16 non-financial sectors positively evidence `corporate`; `Ngân hàng`/`Bảo hiểm` positively evidence `bank`/`insurance`; the ambiguous `Dịch vụ tài chính` sector never resolves a specific class by itself), retained statement-template evidence, and specialized (non-general) legal-charter evidence. Real replay against the current 1,492-ticker universe: entity-family distribution 455/71/966 (industrial/limited/generic) -> 1,382/85/25; newly industrial 927, newly limited-financial 14, conflicts 0, remaining honestly unknown 25. `current_research_ready` 455 -> 1,380, entirely from READY features these tickers already had. The 520-ticker legacy regression oracle and the pre-existing 526 already-classified tickers are verified byte-identical before/after. Product denominator remains 1,699/1,492/207 ABSENT; the six-label security-stance distribution is unchanged. No Consumer, Dashboard, Financial V2 formula, stance, score/rank/target/probability, provider, or PDF/OCR change is opened, and no successor is queued.

**Market-wide Financial Analysis V2 scaleout V1 (2026-09-01):** `MARKET_WIDE_FINANCIAL_ANALYSIS_V2_SCALEOUT_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. The existing `financial_analysis_context/v2` contract now consumes the current retained 1,492-ticker structured-financial and Feature Store universe without adding a provider or changing V2 semantics. Qualified advanced/period-semantic evidence wins per feature; Feature Store fallback is explicitly `RESEARCH_PROXY`, never turns current research ready, and never promotes authority. The 523 replay remains a separate regression oracle. The existing compact product projection reaches 1,492/1,699 records with 207 explicit ABSENT and the locked six-label security stance distribution is unchanged. No Consumer, Dashboard, valuation, actionability, PIT, provider, PDF/OCR, score/rank, target/probability, or successor change is opened.

**Financial Analysis V2 Product and AI Bundle Integration V1 (2026-09-01):** `FINANCIAL_ANALYSIS_V2_PRODUCT_AND_AI_BUNDLE_INTEGRATION_V1 = COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE`. The verified `financial_analysis_context/v2` replay now reaches the Producer product chain only through `financial_analysis_product_integration/v1`: compact, versioned ticker records preserve explicit READY / `RESEARCH_PROXY` / unavailable semantics and provide ABSENT context for every non-covered product ticker. It is an explanatory sibling to opportunity, security WHY/counter-thesis, the Workspace, session bundle, and NDJSON lookup; it is not an eighth major axis and does not alter stance labels, valuation, PIT, execution, Dashboard, score/rank, target/probability, or actionability. The 523-record engine remains a retained validation cohort beside—not a replacement for—the 1,699 ticker product denominator. Normal AI delivery receives market summary/index plus compact per-ticker context after slimming; deep lineage remains local-only. `export_ai_bundle.py` attaches it only with an explicit opt-in artifact path, and full validation dumps are never published. No successor is queued.

**Market-wide financial analysis engine V2 (2026-09-01):** `MARKET_WIDE_FINANCIAL_ANALYSIS_ENGINE_V2 = COMPLETE / COHERENT_PARTIAL_BY_EVIDENCE`. A generic, retained-evidence-only `financial_analysis_context/v2` now supplies deterministic per-issuer profitability, margin, basis-preserved growth, cash-quality, balance-sheet, capital-efficiency, leverage, resilience, and bounded evidence context. Its accepted replay is the explicitly named historical validation cohort `LEGACY_HISTORICAL_FROZEN_523_V1` (523 records, zero silent drops), not a default/current production cohort. The engine separates `READY`, `RESEARCH_PROXY`, `BLOCKED_BY_EVIDENCE`, and `NOT_APPLICABLE`; no financial authority, PIT, score/rank, target/probability, valuation eligibility, decision integration, Dashboard path, runtime state, or production output is promoted. No successor is queued.

**Dashboard product surface and publication convergence V1 (2026-09-01):** `DASHBOARD_PRODUCT_SURFACE_AND_PUBLICATION_CONVERGENCE_V1 = COMPLETE / COHERENT_PARTIAL / IN_APP_BROWSER_AUTOMATION_UNAVAILABLE`. The Producer-owned `investment_decision_workspace_dashboard_projection/v1` is now copied atomically into the Dashboard only after schema, exact-session, coverage, producer-identity, and zero-silent-drop validation. Home, Analysis, Tactical Signals, Screener deep links, Workspace detail, Portfolio, navigation, and About make that current Workspace contract primary; legacy candle/sector artifacts are optional sidecars. The retained 2026-08-28 projection has 1,699 cards. Contract tests and static local HTTP checks pass; the in-app Browser failed before initialization, so console/click acceptance is intentionally not claimed. No data regeneration, publish, push, deployment, or authority change occurred, and no successor is queued.

**Current valuation and opportunity integration V1 (2026-08-31):** `CURRENT_VALUATION_AND_OPPORTUNITY_INTEGRATION_V1 = COMPLETE LOCALLY / COHERENT PARTIAL`. Market-wide current research now emits per-ticker `opportunity_context/v1` and compact `security_decision_context/v1` from the Feature Store, Tactical V2, current valuation, liquidity research proxy, catalyst/invalidation, and portfolio availability — without a universal score or execution authority. Real denominator 1,699; opportunity and decision coverage 1,699/1,699; ≥3 usable major axes 1,406; ≥5 usable 945. Negative-earnings P/E is `PE_NOT_MEANINGFUL` (289 TTM cases) while other methods remain; banks/securities exclude industrial EV methods. Exact execution capacity stays blocked and does not force WAIT. Sole NEXT: `INVESTMENT_DECISION_WORKSPACE_V1`. Dashboard/Consumer changes are not opened here.

**Market-wide fundamental feature store V1 (2026-08-31):** `MARKET_WIDE_FUNDAMENTAL_FEATURE_STORE_V1 = PASS`. The committed period-semantics artifact now has a product-ready, non-authoritative downstream record for all 1,492 tickers. The feature store uses exact source-native representation contracts for dimensionless research proxies and P-I-T trajectories, retains every feature's inputs and blockers, and emits interpretable financial-health axes rather than a score. Current product sequence is `TACTICAL_AND_BEHAVIORAL_ENGINE_V2` -> `RESEARCH_LIQUIDITY_AND_EXPLICIT_PORTFOLIO_V1` -> `CURRENT_VALUATION_AND_OPPORTUNITY_INTEGRATION_V1` -> `INVESTMENT_DECISION_WORKSPACE_V1`. The older valuation/opportunity/portfolio-only future chain remains historical evidence but is `SUPERSEDED_AS_CURRENT_PLAN`. Tactical V2 is the sole NEXT and is not started here.

**Market-wide structured financial period semantics V1 (2026-08-31):** `MARKET_WIDE_STRUCTURED_FINANCIAL_PERIOD_SEMANTICS_V1 = PASS / OPERATIONAL_PROXY_RESEARCH_SEMANTICS_ONLY`. The new `market_wide_structured_financial_period_semantics/v1` projection is additive over retained canonical provider facts, never an authority or feature-store rewrite. Its real read-only corpus reconciliation is 195,552 facts / 1,492 tickers / 1,493 source files, zero silent drops: 86,912 P-I-T balance-sheet, 68,575 standalone-quarter, 497 YTD cumulative interim, and 39,568 unresolved-duration facts. 94,252 facts retain incomplete source lineage and 12,481 retain conflicts; neither is repaired, chosen, or promoted. Its Feature Store successor is complete; the semantics artifact remains the unmodified retained input.

**Owner financial data-path rebaseline and governance sync V1 (2026-08-31):** `OWNER_FINANCIAL_DATA_PATH_REBASELINE_AND_GOVERNANCE_SYNC_V1 = COMPLETE`. The default financial architecture is structured provider/API evidence with retained provenance, period/scope/unit semantics, canonical operational records, explicit fitness-for-use, and deterministic research. `NOT_AUTHORITATIVE != NOT_USABLE`: `OPERATIONAL_PROXY / RESEARCH_ELIGIBLE` evidence may support explicitly labelled research but never silently becomes official/PIT/audit/backtest authority. Official documents are triggered Tier-2 verification/escalation assets; Tier-3 is reserved for conflict, PIT, corporate-action/share-basis, accounting-identity, audit-sensitive, or explicit owner-verification uses. `PDF_DEFAULT_FINANCIAL_INGESTION = NO`; `TICKER_BY_TICKER_PDF_PROOF_DEFAULT = NO`; OCR is deprioritized/non-critical and Unlimited-OCR remains optional shadow work. HNX's 155 immutable PDFs are retained verification assets, not a market-wide research blocker. Claude's unmerged text-native parser checkpoint `2507c86` is `PARTIAL_BY_EVIDENCE` (30 processed, 14 tables, 0 candidates) and is retained only as an official-financial verification utility; Codex's unmerged Consumer checkpoint `48cc2d4` is an optional verified-evidence attachment path with no authority widening. The completed semantics gate now yields `MARKET_WIDE_FUNDAMENTAL_FEATURE_STORE_V1` as the sole executable next gate, followed strategically by `CURRENT_VALUATION_RESEARCH_PROXY_SCALEOUT_V1` -> `MARKET_FUNDAMENTAL_OPPORTUNITY_INTEGRATION_V1` -> `PORTFOLIO_RISK_AND_CAPITAL_POLICY_RESEARCH_V1`.

**Next-session decision brief V1 (2026-08-30):** `NEXT_SESSION_DECISION_BRIEF_V1 = COMPLETE`. Cherry-picked `cefe3d8`+`d7a1db3` from `feature/next-session-decision-brief-v1` onto production main (`6e63a2a` -> `5bebfaa`, pushed); the real 2026-08-27 -> 2026-08-28 replay against primary governed evidence exactly reproduced every required market/opportunity/lifecycle/tactical/recommendation/C2 figure, and 92/92 focused tests passed. A real `stocklookup.ps1 daily` published `next_session_decision_brief.json` to the private AI-handoff repository (`producer_checkpoint=5bebfaa`); a required second run returned `NO_OP_ALREADY_PUBLISHED`, proving idempotence. Both runs' separate Dashboard-release leg failed on a pre-existing, unrelated `DASHBOARD_REMOTE_AHEAD_OR_DIVERGED` condition, left untouched per this milestone's scope boundary. `authority_effect = NONE`. Full detail in `docs/STATE.md`. Next gate: `OWNER_ROADMAP_SELECTION`; no new milestone is opened here.

**Owner roadmap selection (2026-08-30):** `NEXT_SESSION_DECISION_BRIEF_V1 = ACTIVE / OWNER_SELECTED / READY_BY_EVIDENCE`. The owner selected the accepted read-only audit's bounded downstream brief for implementation. This is only the machine-readable control-plane transition: no implementation result, authority promotion, new source, or change to recommendation, valuation, liquidity, sizing, execution, PIT, RAW_AS_TRADED, Dashboard, or Consumer authority has occurred. `docs/ROADMAP_STATE.json` is synchronized with this sole active milestone; prior operational milestones remain complete/production-active.

**Owner integration release review (2026-08-30):** `OWNER_INTEGRATION_RELEASE_REVIEW = COMPLETE`. Took over Codex's interrupted implementation of the exact-Producer-run runtime-release binding fix -- already correctly committed at `38981ab` on both PRIMARY and the feature worktree, not the uncommitted WIP the interruption assumed -- closed two gaps in its own focused-test matrix, and found and fixed a second live defect: `_p3_snapshot` could not resolve the P3F9B snapshot for a session registered through reused retained evidence rather than a fresh live acquisition attempt (2026-08-28). A real `stocklookup.ps1 daily` run then published session 2026-08-28 to the private AI-handoff repository for real (`handoff_build_5c41b201c84b12e9c535f8d9915e824c6ef6dc15b73e22986a4f999fb664c07d`), preserving the byte-identical legacy 2026-08-28 package, linking `previous_session=2026-08-27`, and reconciling every published hash exactly against the Producer's own output; a required second run correctly returned `NO_OP_ALREADY_PUBLISHED` with the remote HEAD unchanged. `ROADMAP_EXECUTION_CONTROL_PLANE_V1` and `ONE_COMMAND_DAILY_RESEARCH_TO_AI_HANDOFF_V1` are both production-active; `authority_effect = OPERATIONAL_SERVING_ONLY`. Next gate is `OWNER_ROADMAP_SELECTION`; no new milestone is opened here.

**One-command daily research to AI handoff V1 (2026-08-30):** `ONE_COMMAND_DAILY_RESEARCH_TO_AI_HANDOFF_V1 = PASS`. Owner workflow is `stocklookup.ps1 daily`; it delegates canonical production and transports the verified AI handoff through a dedicated private Git repository, with deterministic session package, previous qualified bundle, and `LATEST` pointer handling. The next gate is `OWNER_ROADMAP_SELECTION`; no new research milestone is implied.

**Roadmap execution control plane V1 (2026-08-30):** `ROADMAP_EXECUTION_CONTROL_PLANE_V1 = PASS`. `docs/ROADMAP_STATE.json` is now the machine-readable EXECUTION-STATE authority for current/active/next/blocked/deferred/superseded milestone sequencing and Git-checkpoint verification; this document remains the strategic/narrative roadmap authority and is not superseded. Query it with `python tools/stocklookup_roadmap.py` (`--json`, `--check`, `--can-start MILESTONE_ID`). `authority_effect = NONE`; `queued_next = ONE_COMMAND_DAILY_RESEARCH_TO_AI_HANDOFF_V1`, not started here. See `docs/STATE.md` and `docs/DECISIONS.md` for full detail.

**Canonical wide fundamental research cohort cutover V1 (2026-08-30):** `CANONICAL_WIDE_FUNDAMENTAL_RESEARCH_COHORT_CUTOVER_V1 = PASS`. The owner-authorized cutover is complete: prospective current research resolves `CURRENT_WIDE_GOVERNED_V1` through one retained, versioned, fail-closed selector, while `LEGACY_HISTORICAL_FROZEN_523_V1` is explicit replay/reproduction only. The selector verifies the wide artifact against its qualified reconciliation instead of hardcoding a membership list; daily shadow recommendation records the selected cohort lineage. The normal canonical daily command is unchanged. 27/28 retained comparison remains 75/106→106/106 and 83/123→123/123 recommendation/invalidation retention with 58/58 lifecycle recommendation contexts `UNCHANGED`; no scoring, recommendation, opportunity, lifecycle, provider, evidence, or stronger authority promotion occurred. The next gate remains targeted qualified evidence acquisition for genuinely unavailable names, not another cohort/default change.

**Market-wide fundamental research cohort scale-out V1 (2026-08-30):** `MARKET_WIDE_FUNDAMENTAL_RESEARCH_COHORT_SCALEOUT_V1 = PASS`. Fulfils the next gate the canonical daily shadow-recommendation autosourcing milestone named: widening the 523-name fundamental cross-sectional research cohort. Root cause was a mixture, reconciled rather than asserted — an accidental hardcoded default-path ceiling for cohort *membership* (`market_wide_historical_fundamentals_scaleout.execute()`'s default read the frozen 2026-08-20 523-member snapshot even though `build_artifact()` was already generic), plus a genuine, unchanged evidence ceiling for per-axis *readiness* within that membership. New `fundamental_research_cohort_scaleout.py` reuses the 2026-08-27 financial-fact-coverage-recovery milestone's own generic wideners, completely unmodified, to feed the unmodified `market_wide_historical_fundamentals_scaleout`/`fundamental_cross_sectional_scoring` builders a wide (1,507-name official-research-universe) membership instead of the narrow 523. Cohort: 523 → 1,507 (984 newly admitted, residual zero, 157 of them still genuinely evidence-blocked, 88 correctly sector-excluded by the pre-existing corporate-only scope). READY axis coverage grows ~2.5x market-wide. The narrow 523 subset's own facts are proven byte-identical under widening; downstream replay for 2026-08-27/2026-08-28 (reusing the existing shadow-recommendation/Session-Bundle/lifecycle chain verbatim) closes the exact 31/106 and 40/123 upstream-cohort gaps the prior milestone reported, taking both sessions to full same-session recommendation/invalidation retention. No scoring, threshold, or recommendation rule changed; the narrow cohort remains the live canonical default — this milestone does not rewire `fundamental_cross_sectional_scoring.execute()` or `daily_session_shadow_recommendation`'s shared-context default. The next gate, only if owner-authorized, is a distinct cutover milestone to make the wide cohort canonical, and/or targeted evidence acquisition for the 160 no-source corporate tickers; neither is opened here.

**Canonical daily shadow-recommendation autosourcing V1 (2026-08-30):** `CANONICAL_DAILY_SHADOW_RECOMMENDATION_AUTOSOURCING_V1 = PASS`. The normal canonical command now auto-sources the proven same-session shadow recommendation/invalidation chain after qualified retained inputs exist and before the Daily Session Operation, replacing the former manual intermediate step. Immutable same-session identity matching permits reuse; changed lineage, corruption, and explicit-vs-auto disagreement fail closed, while ordinary per-security `INSUFFICIENT_EVIDENCE` or outside-cohort absence remains local. The retained 27→28 producer replay reproduces 75/106 and 83/123 bundle retention and lifecycle compatibility (58 comparable; 41 recommendation contexts unchanged, 17 missing). No cohort widening or authority promotion occurred. Do not start a successor automatically; the only supported future gate remains owner-authorized upstream cohort expansion or requalification of retained shared contexts.

**Daily-session shadow recommendation and invalidation production V1 (2026-08-30):** `DAILY_SESSION_SHADOW_RECOMMENDATION_AND_INVALIDATION_PRODUCTION_V1 = PASS`. Fulfils the next gate the Session Bundle retention milestone named: `shadow_security_recommendation` and its structured fundamental-invalidation input now run per qualified Daily Producer session, not only once against a frozen cohort. Every engine in the six-stage chain was already a pure, session-agnostic function; the new `daily_session_shadow_recommendation.py` is orchestration only, and `daily_research_session_operations.py` gained one narrow, optional, additive parameter mirroring the existing `portfolio`/`macro` pattern — the canonical pipeline's global failure behavior is unchanged, and a session with no supplied artifact behaves exactly as before. The 27→28 replay proves real production and retention (75/106 and 83/123 tickers), not just the contract's correctness. The next gate, only if owner-authorized, is widening the 523-name fundamental cross-sectional research cohort itself (a distinct, separate milestone) and/or deciding how a future canonical Daily Producer run should source this artifact automatically (a registry/scheduling decision, not an engine change).

**Session Bundle decision-context and lifecycle enrichment V1 (2026-08-30):** `SESSION_BUNDLE_DECISION_CONTEXT_AND_LIFECYCLE_ENRICHMENT_V1 = PARTIAL_BY_EVIDENCE / RETENTION_CONTRACT_ACTIVE`. Fulfils the next gate the prior lifecycle milestone named: the Daily Producer bundle builder now retains the existing shadow-recommendation and structured fundamental-invalidation identities in the Session Bundle, verbatim and same-session-gated, rather than dropping them. The lifecycle engine consumes the new fields unchanged; only a symmetric `INVALIDATION_CLEARED` material-change reason was added alongside the already-existing `INVALIDATION_ACTIVATED` one. The 27→28 replay proves the retention contract correct (unit-tested) but reports zero real retention for that historical pair, because the only existing shadow-recommendation artifact is dated to a different, separate 523-name research cohort (2026-08-25), not either daily session. The next gate, only if owner-authorized, is running the shadow-recommendation (and its structured-invalidation input) engine per daily session so a same-session artifact actually exists for the Producer to retain; it is not a redesign of either engine or of the lifecycle.

**Multi-session thesis and recommendation lifecycle V1 (2026-08-30):** `MULTI_SESSION_THESIS_AND_RECOMMENDATION_LIFECYCLE_V1 = PARTIAL_BY_EVIDENCE / PROSPECTIVE_RETAINED_CONTINUITY_ACTIVE`. A standalone retained-session projection now compares component states across a governed adjacent pair without recomputing upstream opportunity, tactical, strategy, scenario, thesis, invalidation, or recommendation engines. The 27→28 pair provides 58 comparable current contexts, 48 unchanged and 10 neutral state transitions; two confirmation losses are material, while 65 current-only and 48 prior-only contexts are explicit coverage limits. No per-security shadow recommendation or structured fundamental-invalidation state was retained in the two Session Bundles, so V1 records those dimensions as missing rather than manufacturing a transition. The next gate, only if owner-authorized, is prospective retention of the existing per-security shadow-recommendation and structured invalidation identities in a session artifact; it is not a new recommendation model or performance study.

**Market-wide implied-growth and reverse-valuation research V1 (2026-08-30):** `MARKET_WIDE_IMPLIED_GROWTH_AND_REVERSE_VALUATION_RESEARCH_V1 = PARTIAL_BY_EVIDENCE / NO_QUALIFIED_INTRINSIC_OUTPUTS`. A new standalone retained-only 523-record projection reuses, rather than recomputes, current valuation, current-valuation proxy, fundamental, and optional intrinsic inputs. It permits only a numeric existing-intrinsic-value gap or a bounded monotonic reverse FCFF terminal-growth solve with every non-solved input explicitly sourced; no proxy multiple or fundamental score is converted into a valuation assumption. The replay is 0 READY/0 PARTIAL/4 BLOCKED/519 UNAVAILABLE because no current governed intrinsic per-share output or sourced FCFF fixed-input envelope exists. The next evidence gate, only if owner-authorized, is an upstream qualified current intrinsic-input/output envelope—not another reverse-model assumption or proxy expansion.

**Correlation and concentration guard V1 (2026-08-29):** `C2_CORRELATION_AND_CONCENTRATION_GUARD_V1 = OUTCOME_A_CURRENT_RESEARCH_CONTEXT_ACTIVE_NO_AUTHORITY_PROMOTED`. An optional deterministic C2 projection now consumes—not re-computes—C1 pairwise correlations for an explicit supplied security set and explicit frozen lookback. With no existing threshold authority, its frozen V1 heuristic is intentionally simple and non-calibrated: ready Pearson correlation strictly above `0.80` forms a graph edge; sorted connected components expose correlated pairs or three-or-more-name concentration groups. L20's retained 40-name replay finds only `BSR`/`GAS` (0.8235585903592545), and correctly exposes it even though C1 joint-matrix readiness is blocked at `T=19`, `N=40`. The completed bounded producer serialization checkpoint is opt-in only: `export_ai_bundle.py --include-correlation-concentration-guard --correlation-concentration-guard-path PATH --correlation-concentration-lookback {20,60,120,250}` attaches the entire verified C2 artifact at the routine bundle root, and rejects a missing/mismatched horizon. Consumer reads that root artifact and selects ticker-specific context; no Producer C2 recomputation or per-ticker projection exists. It does not change labels/readiness or create allocation, sizing, risk-budget, execution, RAW_AS_TRADED, PIT, backtest, or recommendation authority. The former `SHADOW_RECOMMENDATION_CONSUMER_NARRATIVE_V1` next-pointer is stale: Consumer completion is recorded in its repository, and C2 is the owner-authorized current Producer context capability. Do not begin a successor automatically.

**Shadow security recommendation V1 (2026-08-29):** `SHADOW_SECURITY_RECOMMENDATION_V1 = OUTCOME_A_SHADOW_SECURITY_RECOMMENDATION_ACTIVE`. A full-denominator deterministic current-research packet layer now maps upstream shadow posture to six non-execution labels and independently maps readiness. It keeps evidence, confirmation, invalidation, catalyst/event, valuation, risk, monitoring, temporal facts, and authority boundaries separate—there is no score, probability, target, allocation, sizing, or trade instruction. The C1 risk attachment, valuation, and catalyst contexts are optional and cannot reclassify a label. The artifact may be opt-in attached to Producer AI bundles; default outputs are unchanged. Consumer narrative completion does not reopen this Producer contract, and no successor starts automatically.

**Current portfolio risk research V1 (2026-08-29):** `CURRENT_PORTFOLIO_RISK_RESEARCH_V1 = OUTCOME_A_CURRENT_RISK_RESEARCH_MATERIAL_ACTIVE`. An optional retained Producer attachment now measures exact-window security volatility, pairwise correlation, unrepaired candidate-set covariance diagnostics, and descriptive sector context for the recalculated 40-name `READY_SHADOW` cohort. The fixed horizons are 20/60/120/250 completed closing sessions and returns are simple close-to-close under `ADJUSTED_RETROSPECTIVE`. L20 is correctly joint-blocked by `T=19 < N+5`; L60/L120/L250 pass the joint numerical guard. This creates no allocation, sizing, execution, recommendation, RAW_AS_TRADED, PIT, or backtest authority and cannot alter shadow posture/readiness. Do not automatically begin a successor; `SHADOW_SECURITY_RECOMMENDATION_V1`, `A3_ASOF_QUERY_AND_REVISION_LEDGER_V1`, and any policy-bearing C2 guard remain owner-review candidates.

**A2 provider publication and first-seen retention V1 (2026-08-29):** `A2_PROVIDER_PUBLICATION_AND_FIRST_SEEN_RETENTION_V1 = OUTCOME_B_PROVIDER_TEMPORAL_RETENTION_ACTIVE_WITH_HISTORICAL_CEILING`. A1 now receives a prospective immutable raw-receipt envelope from governed official documents, KBS provider sidecars, DNSE closed-OHLC shadow observations, and offline official corporate-event imports. It retains qualified direct publication only where explicitly established, provider report/update/event and HTTP metadata separately, canonical UTC receipt, and monotonic earliest observation for identical bytes; legacy records remain unknown rather than backfilled. The next temporal capability, only if owner-authorized, is `A3_ASOF_QUERY_AND_REVISION_LEDGER_V1`; it must not promote `ADJUSTED_RETROSPECTIVE`, `RAW_AS_TRADED`, historical PIT/backtesting, or same-close execution.

**Fundamental thesis invalidation precision V1 (2026-08-28):** `FUNDAMENTAL_THESIS_INVALIDATION_PRECISION_V1 = OUTCOME_A_FUNDAMENTAL_INVALIDATION_UNLOCKS_READY_SHADOW`. Existing corporate-cohort qualification rules safely provide 125 exact fundamental monitoring boundaries: 55 quality-threshold loss transitions and 70 avoid-recovery transitions. Together with existing technical-risk instrumentation, 40 records now meet the unchanged complete `READY_SHADOW` contract; 406 remain conditional and 77 not-ready. High-risk remains a conditional warning-only risk state; no sign, margin, axis, or balance-sheet threshold is invented. If owner-authorized, the next bounded capability may define a shadow security-recommendation contract. Do not reopen generic acquisition, KBS, TTM, OCR, or evidence-tier architecture.

**Shadow action readiness V1 (2026-08-28):** The active research-case layer retains its six experiment-only posture classes across all 523 records, now with 40 `READY_SHADOW`, 406 `CONDITIONAL_SHADOW`, and 77 `NOT_READY_SHADOW` after exact fundamental-boundary integration. These labels remain non-recommendation research states; WAIT and AVOID semantics remain intact.

**Thesis, catalyst, downside, and dual invalidation V1 (2026-08-28, corrective continuation):** `THESIS_CATALYST_DOWNSIDE_AND_DUAL_INVALIDATION_V1 = OUTCOME_B_RESEARCH_CASE_LAYER_ACTIVE_WITH_CATALYST_OR_INVALIDATION_LIMITS`. The existing 523-name opportunity universe has a deterministic, research-only case layer with mutually exclusive terminal dispositions: 446 opportunity-case-eligible, 73 market-only, and 4 insufficient (residual zero; pairwise intersections empty). The prior count of 8 insufficient was a terminal/readiness coverage-counter collision, not an eligibility change. The retained event corpus supplies 42 tickers / 45 `RETAINED_EVENT_CONTEXT` entries and zero qualified catalysts because causal thesis linkage and usable temporal/knowledge evidence are not retained together; technical confirmation remains its separate 159-record trigger. Valuation and the three exact net-margin TTM records remain optional. Current tactical rules have no retained explicit invalidation prices, so technical invalidation is conditional; fundamental invalidation is likewise conditional unless a thesis-relevant, compatible baseline exists (zero margin-20% triggers). Advance only with an owner-authorized bounded action-readiness/recommendation contract; do not reopen KBS lookback, TTM scaleout, or generic OCR without new external evidence.

**KBS quarterly financial lookback and semantic retention (2026-08-28):** `KBS_QUARTERLY_FINANCIAL_LOOKBACK_AND_SEMANTIC_RETENTION_V1 = OUTCOME_C_EXISTING_KBS_ROUTE_CANNOT_SUPPLY_MATERIAL_LOOKBACK`. The repository-owned two-page KBS KQKD proof retained raw response/`Head` metadata and restatement variants but proved a route ceiling: page 1 provides only 4–5 distinct periods and page 2 is HTTP-200 empty for all five proof issuers. No ticker reaches eight quarters; existing TTM coverage does not materially expand. The frozen-contract Anti-Gravity acquisition is not ready. Continue directly to `THESIS_CATALYST_DOWNSIDE_AND_DUAL_INVALIDATION_V1`; new compatible retained source evidence needs separate authorization.

**Financial flow semantics and TTM bridge foundation (2026-08-28):** `FINANCIAL_FLOW_SEMANTICS_AND_TTM_BRIDGE_FOUNDATION_V1 = OUTCOME_B_TTM_SEMANTICS_PARTIAL_UNLOCK_WITH_DEPTH_LIMITS`. Retained KBS KQKD semantics safely activate only 3 revenue/net-income rolling-4Q pairs plus 56 cash-flow-only TTM series across the 523-name universe. No retained full-year/YTD bridge, exact EBIT, resolved currency/scale, LFY, or valuation multiple is available; TTM remains read-only operational-proxy research context. Continue directly to `THESIS_CATALYST_DOWNSIDE_AND_DUAL_INVALIDATION_V1` using the established opportunity matrix, rather than reopening generic financial semantic work absent newly retained compatible source evidence.

**Workspace topology convergence (2026-08-28):** `WORKSPACE_TOPOLOGY_CONVERGENCE_V1 = OUTCOME_B_CANONICAL_TOPOLOGY_ESTABLISHED_WITH_SAFE_RETIREMENT_BLOCKERS`. Runtime is canonically `dashboard-runtime`, the sole served source/publish checkout is `market-dashboard` on `main`, and only the Producer release orchestrator can publish. The former runtime checkout is preserved as data/runtime only because it contains unique local commits and WIP; it is not an alternate publication path. The topology invariant is now enforced in publisher identity checks. No Dashboard publication occurred.

**Fundamental plus market opportunity ranking (2026-08-28):** `FUNDAMENTAL_PLUS_MARKET_OPPORTUNITY_RANKING_V1 = OUTCOME_A_FUNDAMENTAL_MARKET_OPPORTUNITY_RANKING_MATERIAL_COVERAGE`. The retained 523-record research denominator now has a session-coherent fundamental-plus-market opportunity matrix: 446 names have both a corporate fundamental-quality view and current technical/tactical evidence, while valuation remains optional enrichment (9 true-multiple records). The no-global-score Pareto buckets keep quality, setup, valuation, and confidence distinct; the same-milestone `SUPER_SETUP_RESEARCH` (13) and warning-only `HIGH_RISK_SPECULATION` (17) labels use the actual 449-name valid corporate cohort, never an unproven sector cohort. No action authority is created. Advance to `THESIS_CATALYST_DOWNSIDE_RESEARCH_V1`, not another general data-architecture detour.

**Approved subsequent intents — not implemented in this milestone:** current TTM valuation research scale-out; implied-growth / reverse-valuation research; thesis/catalyst/downside research with dual technical plus fundamental invalidation; and a one-time workspace-topology convergence before the next Dashboard publication.

**Current valuation research proxy and relative-value axis (2026-08-28):** `CURRENT_VALUATION_RESEARCH_PROXY_AND_RELATIVE_VALUE_AXIS_V1 = OUTCOME_B_VALUATION_AXIS_ACTIVE_WITH_INPUT_LIMITS`. Corrective replay separates market-cap/EV size context from relative valuation: 9 true-multiple records are ready, while 519 market-cap records are context only. Advance only with this bounded optional axis.

**Fundamental cross-sectional scoring and ranking (2026-08-28):** `FUNDAMENTAL_CROSS_SECTIONAL_SCORING_AND_RANKING_V1 = OUTCOME_A_FUNDAMENTAL_CROSS_SECTIONAL_RANKING_MATERIAL_COVERAGE`. Multi-axis proxy research ranking is active; composite/action outputs remain intentionally absent. The next step is investment analysis (`CURRENT_VALUATION_RESEARCH_PROXY` or `FUNDAMENTAL_PLUS_MARKET_OPPORTUNITY_RANKING`), not another evidence-tier/classification audit.

**Market-wide quarterly fundamental research features (2026-08-28):** `MARKET_WIDE_QUARTERLY_FUNDAMENTAL_RESEARCH_FEATURES_V1 = OUTCOME_A_QUARTERLY_FUNDAMENTAL_FEATURES_MATERIAL_COVERAGE`. Completed research-only same-period/EOP proxy activation across the existing 523 denominator. The next capability is `FUNDAMENTAL_CROSS_SECTIONAL_SCORING_AND_RANKING_V1` over only semantically comparable proxy features; no new data-tier work is needed.

**Market-wide fundamental feature activation (2026-08-28):** `MARKET_WIDE_FUNDAMENTAL_FEATURE_ACTIVATION_V1 = OUTCOME_B_FEATURE_ENGINE_ACTIVE_WITH_SHARED_INPUT_LIMITS`. The existing proxy/consumer contracts now emit a full research-only annual-feature envelope across 523 records after retained entity-class resolution. Current retained provider flow periods are explicitly quarterly, not annual, so annual feature values remain fail-closed. The next analysis capability is `VALUATION_RESEARCH_EXPANSION` only when compatible annual/duration-evidenced retained inputs exist; no new data-tier program is opened.

**Market-wide historical fundamentals scaleout (2026-08-28):** `MARKET_WIDE_HISTORICAL_FUNDAMENTALS_SCALEOUT_V1 = OUTCOME_B_MARKET_WIDE_SCALEOUT_PARTIAL_PROVIDER_CEILING`. Completed retained-provider replay across the 523-name current research universe, with zero terminal residual and no evidence-tier redesign. The next shared capability is entity-class/sector-specific provider financial semantic coverage, especially the 490 unknown-class records; it is not started by this result.

**Financial operational proxy foundation and research tier activation (2026-08-28):** `FINANCIAL_OPERATIONAL_PROXY_FOUNDATION_AND_RESEARCH_TIER_ACTIVATION_V1 = OUTCOME_A_OPERATIONAL_FINANCIAL_PROXY_RESEARCH_TIER_ACTIVE`. Introduces `financial_operational_proxy.py`'s three-tier evidence contract (`OPERATIONAL_PROXY` / `VERIFIED_RESEARCH_EVIDENCE` / `AUTHORITATIVE_EVIDENCE`) and purpose-specific fitness-for-use matrix over already-retained provider financial facts, reconciled against the unchanged P3-F13 official panel. Bounded to corporate entities and a real 11-ticker proof cohort this milestone; see STATE.md for exact counts. **Financial lane sequencing is rebaselined starting here:** `FINANCIAL_OPERATIONAL_PROXY_FOUNDATION` (this milestone) -> `MARKET_WIDE_HISTORICAL_FUNDAMENTALS_SCALEOUT` (widen the same architecture past the proof cohort to the full research universe) -> `DETERMINISTIC_FUNDAMENTAL_FEATURES` (harden the five derived scale-invariant metrics and extend the metric family where evidence supports it) -> `VALUATION_RESEARCH_EXPANSION` (report, then where evidence allows, unlock more of `market_wide_current_valuation_input_scaleout.py`'s research-usable surface -- absolute valuation authority itself stays gated on independently resolved currency/scale, never relaxed for coverage) -> `OFFICIAL_VERIFICATION_ON_TRIGGER` (targeted OCR/official-document verification only when a provider conflict, a critical owner-focus metric, or a PIT/audit need justifies the cost -- see the OCR scope note below). `AUTHORITATIVE_EVIDENCE`/PIT financial evidence remains a separate, harder authority lane with its own existing requirements (P3-F13, `semantic_evidence_bridge.py`), unweakened by this sequencing. No other roadmap program is rewritten by this entry.

**OCR/PDF scope note (2026-08-28, see `docs/DECISIONS.md` for full text):** the owner-focus image-only OCR batch completed at commit `6c6755f` is the final *default* OCR scale-out milestone. OCR/PDF extraction capability is retained (never deleted) for triggered, bounded use: important owner-focus/portfolio candidates, provider conflict resolution, critical metric verification, PIT/audit-sensitive authority, and exceptional high-value documents. It is no longer the default path for market-wide financial coverage growth -- that path now runs through `financial_operational_proxy.py`'s provider-tier research layer instead, per the sequencing above.

**Retained official financial PDF extraction scaleout (2026-08-27):** `RETAINED_OFFICIAL_FINANCIAL_PDF_EXTRACTION_SCALEOUT_V1 = OUTCOME_B_RETAINED_PDF_EXTRACTION_PARTIAL_LAYOUT_CEILING`. The local corpus is SHA-deduplicated before replay and is classified into native-text, image-only, metadata-insufficient, and non-financial dispositions. AAA is reproduced exactly; other native-text corporate reports are fail-closed where their bilingual/non-form-code layouts cannot meet the current exact table contract, while bank/securities reports remain raw-layout-only. No acquisition, OCR, new P3-F13 fact, or valuation change occurred.

**Official financial PDF page/table extraction and citation (2026-08-27):** `OFFICIAL_FINANCIAL_PDF_PAGE_TABLE_EXTRACTION_AND_CITATION_V1 = OUTCOME_A_GENERIC_PDF_EXTRACTION_QUALIFIES_NEW_OFFICIAL_FACTS`. A generic native-text `official_financial_pdf_page_evidence/v1` now binds each retained-PDF text page to the document hash, parser identity, page hash, table fragment, current-period column, and row span where native coordinates exist. The retained AAA FY2024 official PDF yielded seven qualified, cited corporate facts through the existing P3-F13 ingress; no separate official store or acquisition route was created. The contract blocks missing metadata/scale, rejects blocked facts at ingress, and declines corporate mapping for bank/securities inputs. The full-universe retained-evidence replay adds six required AAA financial identity cells (64→70 official-qualified) but no valuation research-usable, READY, or VALUE coverage. Next expansion is evidence-led document/layout coverage only.

**Approved issuer-IR official financial evidence cohort (2026-08-27):** `APPROVED_ISSUER_IR_OFFICIAL_FINANCIAL_EVIDENCE_COHORT_V1 = OUTCOME_C_APPROVED_ROUTES_CURRENT_EVIDENCE_CEILING`. Exactly nine owner-approved issuer-IR routes were checked; one AAA PDF was retained but cannot qualify without page-bound metadata/value extraction. Official coverage and valuation remain unchanged. Next gate is a direct same-approved-host statement with explicit full semantics and citation; do not reopen provider semantic inference from this result.

> **Authoritative Technical Roadmap.** This document defines the engineering phases, milestone matrix, dependency contracts, and acceptance gates for Stock Lookup.
> Current operational state and immediate milestones are tracked in [STATE.md](STATE.md).
> Machine-readable current/active/next/blocked execution state is tracked in [ROADMAP_STATE.json](ROADMAP_STATE.json) (query via `python tools/stocklookup_roadmap.py`); this document remains the strategic/narrative authority and is not superseded by it.

**AI research handoff scope and routing (2026-08-27):** `AI_RESEARCH_HANDOFF_SCOPE_AND_ROUTING_V1 = COMPLETE_LOCALLY / AI_HANDOFF_READY`. Hardens the existing Daily Producer AI delivery path after an external-AI front-of-file test. Primary input remains `ai_research_session_bundle.json`; `ai_research_full_universe.ndjson` stays lookup-only. Owner-focus coverage is an analysis-scope contract, not a new research architecture. Do not open a ranking/recommendation lane from this defect.

**Market-wide current valuation session-native scale-out (2026-08-27):** `MARKET_WIDE_CURRENT_VALUATION_SESSION_NATIVE_SCALEOUT_V1 = OUTCOME_C`. Canonical 26/8 Daily Producer valuation is already session-native. Isolated replay of retained 26/8 P3F9B reproduces the same research-usable ceiling (market cap 887 / P/E 8 / P/B P/S EV EV/Sales 9 / EV/EBITDA 0; READY 0). Do not promote issued shares or provider trends. Next evidence is official financials and current-common shares; this milestone does not open that acquisition.

**Prospective research cohort first-future attribution (2026-08-27):** `PROSPECTIVE_RESEARCH_COHORT_FIRST_FUTURE_ATTRIBUTION_V1 = COMPLETE_LOCALLY / FIRST_STRICTLY_LATER_SESSION_MATERIALIZED`. Follow-on inside the existing entry-relevant prospective collection lane. The frozen 2026-08-25 95-member `cohort_snapshot/v1` is attributed against exact-session 2026-08-26 closes only. This is descriptive observed change, not calibration, scoring, or strategy promotion. The next evidence gate is `ACCUMULATE_ADDITIONAL_STRICTLY_LATER_SESSIONS` before any calibration review. Do not reopen ADTV20 or retry FHSC 2026-08-06 from this lane.

**ADTV20 exact-reconciled trailing-window recovery (2026-08-27):** `ADTV20_EXACT_RECONCILED_TRAILING_WINDOW_RECOVERY_V1 = OUTCOME_C_CURRENT_SOURCE_CEILING_ESTABLISHED`. Follow-on inside the existing ADTV20 / matched-value lane. The expected trailing 20 remains `2026-07-15`…`2026-08-11`. FHSC trading-history does not emit `2026-08-06`, so 20/20 READY is structurally unreachable on this calendar until that independent anchor exists. G1 formula, G4 exclusion, and no-sizing gates unchanged. No daily-pipeline change.

**Canonical daily one-command operational cutover (2026-08-27):** `CANONICAL_DAILY_ONE_COMMAND_OPERATIONAL_CUTOVER_V1 = COMPLETE_LOCALLY / DAILY_ONE_COMMAND_READY`. One foreground command `python daily_analysis_pipeline.py --canonical-post-close --complete-publication` owns Phase A attempt eligibility, one canonical P3F9B market acquisition, Phase B exact-session completion, Daily Producer, same-session runtime + trusted-subset materialization, and publication via the existing `release_orchestrator.py all --live --expected-session --complete-publication`. M1 READY remains `EXACT_SESSION_OBSERVED_AFTER_SAFETY_FLOOR` after acquisition; pre-acquisition is `ATTEMPT_ELIGIBLE`. 18:00 is still not market-completion authority. No second orchestrator. No M0/M1 reopen.

**Governed publication one-command completion (2026-08-27):** `GOVERNED_PUBLICATION_ONE_COMMAND_COMPLETION_V1 = COMPLETE_LOCALLY / ONE_COMMAND_PUBLICATION_READY`. Extends `python tools/release_orchestrator.py all --live --expected-session YYYY-MM-DD --complete-publication` so CI, Pages, and public-byte proof are operator-invisible gates on the existing orchestrator. No second release orchestrator. No M0 reopen.

**Capability-First EOD 18:00 operational scheduling (2026-08-26):** `CAPABILITY_FIRST_EOD_1800_OPERATIONAL_SCHEDULING_V1 = COMPLETE_LOCALLY / SCHEDULER_READY`. One-shot foreground command `python tools/run_capability_first_eod_operation.py` with provider-aware completed-session gate. 18:00 Asia/Ho_Chi_Minh is a safety floor. DNSE `working_dates` is calendar identity only. READY is `EXACT_SESSION_OBSERVED_AFTER_SAFETY_FLOOR`, never `PROVIDER_CONFIRMED_COMPLETED`. No OS scheduler is installed. A later milestone requires separate owner authorization.

**Canonical post-close one-command pipeline V1 — published/closed (2026-08-26):** `CANONICAL_POST_CLOSE_ONE_COMMAND_PIPELINE_V1 = PUBLISHED / CLOSED`. Deployed 26/8 content source `534e4971edf2b9be62467ce89758b6625544558d`; Dashboard CI `32986483019` SUCCESS; Deploy Pages `32986911616` SUCCESS; public session `2026-08-26` `PUBLIC_BYTE_IDENTITY_PASS`. Dashboard `main` later includes workflow-only fallback `691b63dedec8625bfab7f6b126d8928a2184abf4`, which is not the deployed content source.

**Canonical post-close public session companions (2026-08-26):** `READY_FOR_OWNER_PUSH_AND_RETRY_PUBLICATION` at the time of that local Producer checkpoint; subsequently published/closed as recorded above. The missing publication-layer step materializes the current-session Dashboard companions (`data/session_<YYYY_MM_DD>_manifest.json` and `report-<YYYY-MM-DD>.html`) from retained canonical evidence before release-smoke.

**Canonical trusted-subset financial-evidence bridge (2026-08-26):** `READY_FOR_OWNER_RUNTIME_TRUSTED_SUBSET_MATERIALIZATION` at the time of that local Producer checkpoint; subsequently published/closed as recorded above. Publication required the four-artifact trusted subset. The bridge is a Producer-owned retained-session adapter, not a VCI sidecar relabel and not a Consumer contract change.

**Canonical post-close runtime-release adapter (2026-08-26):** `CANONICAL_POST_CLOSE_ONE_COMMAND_PIPELINE_V1 = LIVE_VALIDATED_COMPLETE / READY_FOR_OWNER_RUNTIME_MATERIALIZATION`. The bounded Producer-owned retained-output adapter is implemented and wired after future Canonical Daily Producer completion, before runtime readiness/tiering. It stages, validates, and manifest-last promotes only the governed publisher input files from frozen canonical/Daily-Producer evidence; it has no DNSE/network or legacy VCI/KBS/indicator/scoring path. Retained 2026-08-26 evidence materializes cleanly in a temporary runtime and passes the existing publisher’s read-only preflight exactly for that session. The actual runtime remains a deliberately untouched coherent 2026-08-25 projection until the owner runs the explicit replay command. Dashboard publication, authority promotion, and M1 remain out of scope.

**Canonical post-close one-command pipeline (2026-08-26):** `CANONICAL_POST_CLOSE_ONE_COMMAND_PIPELINE_V1` is operational composition, not a new architecture phase: `daily_analysis_pipeline.py --session YYYY-MM-DD --canonical-post-close` chains the existing DNSE P3F9B acquisition, `daily_session_level2_package`'s current-research materialization, new exact-session registry writing, unmodified Canonical Daily Producer, and post-hoc prospective collection into one operator command, ending in a tiered (session/opportunity/full-universe/dashboard-release) handoff. DNSE/Livespeed is `CANONICAL` for this path; FHSC stays `SUPPLEMENTAL_BOUNDED`; VNStock/VCI/KBS stays `LEGACY_NON_CANONICAL` and is never silently routed through. Fixed two pre-existing dead-code bugs in `daily_session_level2_package.materialize_independent_components` (P3F9B and universe-resolution subprocess calls used flags that do not exist on those CLIs) discovered while integrating. A real 2026-08-26 attempt was deliberately stopped before registration because configured DNSE credentials/API reachability do not by themselves prove the session is complete against the owner's ~18:00 Asia/Ho_Chi_Minh collection cutoff; no session was manufactured, and `config/daily_research_session_input_registry.json`/`dashboard-runtime` have zero 2026-08-26 mutation. Next real step: rerun after the cutoff, once registered inputs pass the same coherence gate Canonical Daily Producer itself uses.

**Prospective research cohort collection scale-out (2026-08-26):** `PROSPECTIVE_RESEARCH_COHORT_COLLECTION_SCALEOUT_V1` scopes recurring prospective evidence collection to the deterministic triage entry-relevant cohort (95 for 2026-08-25: 17 `BASE_BUILDING`/14 `BREAKOUT_READY`/64 `EARLY_REVERSAL_CANDIDATE`) instead of the original 2–5 name HPG/VCB/SSI/AAN/AAA pilot scope. This is evidence-collection scale-out, not a new architecture phase and not a learning/scoring engine; retrospective attribution stays Consumer-side and evidence-limited, and the existing full-universe `freeze_current_decision_surface` freeze is unchanged. Requested under the label "P0-A.3E", which is not used here because it already names the unrelated Prospective Price Basis/PIT program item (still `PART_B_BLOCKED_PENDING_QUALIFIED_EX_DATE`). Future sessions reuse `python tools/run_prospective_research_cohort_collection.py --latest-completed-session` once that session is registered; no session is manufactured ahead of registration.

**P0 Daily Producer triage and session governance recovery (2026-08-26):** `full_universe_entry_candidate_triage/v1` is implemented from the recovered 2026-08-24 post-close contract. Session 2026-08-25 is governed and Canonical Daily Producer completed (888 same-session technical coverage). This is blocker recovery, not a new architecture phase. Do not wait 20 future sessions for current Daily Producer eligibility. Prospective learning still needs more real later-session outcomes; HPG/VCB were seed cases, not a frozen universe. After 2026-08-26 close, register exact-session inputs and run `python tools/run_daily_producer.py --session 2026-08-26`. Remaining separate evidence lanes: ADTV20, PIT/ex-date, current-share authority.

**Current research packet dashboard shadow surfacing (2026-08-25):** `CURRENT_RESEARCH_PACKET_DASHBOARD_SHADOW_SURFACING_V1` organizes the retained `current_research_decision_packet/v1` through the existing daily decision research product and opt-in bundle attach. Default product/bundle behavior is unchanged. Packet Bear/Base/Bull is not CONSERVATIVE/BASE/SPECULATIVE. 1,507/1,507 rendered with residual 0.

**Current corporate event context (2026-08-25):** `CURRENT_CORPORATE_EVENT_CONTEXT_V1` projects retained official exchange events and the HPG/VNM/VCB issuer-VSDC chains into current-research statuses without inferring ex-dates, enabling EVENT_DRIVEN, or claiming price impact.

**Current financial momentum context (2026-08-25):** `CURRENT_FINANCIAL_MOMENTUM_CONTEXT_V1` is a current-research comparable-period context over retained official FY YoY metrics and provider YoY/QoQ trends. It does not enable `FUNDAMENTAL_IMPROVEMENT`, change eligibility/priority/entry, or promote valuation. Official 1,507 denominator; 107 names with at least one comparable dimension; true broad improvement 1.

**ADTV20 window-integrity gate (2026-08-25):** `ADTV20_WINDOW_INTEGRITY_AND_CONFLICT_QUALIFICATION_V1` restricts matched-value authority to HOSE discriminating exact sessions. The expected 1,507 × 40 grid residual is 0. `ADTV20_MATCHED_VALUE` is the trailing 20 trading sessions of that calendar, not any 20 qualified rows; true READY is 0. Conflicts are classified and not coerced. No sizing or ADV20 volume authority. The 2026-08-27 recovery follow-on established that FHSC history omits expected session `2026-08-06`, so READY remains 0 until that independent anchor exists.

**Historical matched traded-value gate (2026-08-24):** `HISTORICAL_MATCHED_TRADED_VALUE_AND_ADV_AUTHORITY_V1` is complete locally with a deliberately narrow Outcome C.  Twelve retained DNSE Trades/FHSC exact anchors (FPT, HPG, SSI, VCB; 2026-08-07/10/11) qualify only `G1` matched value via the explicit `price × raw quantity × 10 × 1,000 VND` contract.  The retained 40-session corpus is still insufficient for the 20-session ADV20 denominator, and no liquidity, sizing, execution, PIT, or backtest gate is promoted.  The next dependency is a sufficiently deep exact-reconciled G1 matched-value cohort, not a new liquidity/sizing engine.

**Historical PIT raw-as-traded/corporate-action gate (2026-08-24):** `HISTORICAL_PIT_RAW_AS_TRADED_AND_CORPORATE_ACTION_EVIDENCE_V1 = OUTCOME_D`. A single governed VSDC index page and its direct DTP cash-dividend notice yielded retained official corporate-action evidence, but only a record date—not an explicit ex-date—and no new raw historical price stream or pre-event snapshot pair. The event is intentionally `INSUFFICIENT`, its adjustment ledger is shadow-only `NOT_IMPLEMENTED`, and RAW_AS_TRADED/PIT/valuation/return-risk/backtest remain blocked. The next gate is an explicit official ex-date plus matching pre-event raw snapshot evidence for the same ticker/event window.

**Historical matched-value existing-store requalification (2026-08-24):** `HISTORICAL_MATCHED_VALUE_EXISTING_STORE_REQUALIFICATION_V1 = EXISTING_STORE_CANNOT_UNLOCK_ADV20`. Existing raw/canonical Trades history supplies execution price/quantity/board identity but no independently semantic matched-value field; existing daily Parquet/SQLite is OHLCV-only. The requalification fixes zero-based DNSE page-chain replay and reproduces the existing 12 FHSC exact anchors, but does not generalize them outside their four-ticker/three-session scope. The next data gate remains 20 complete sessions with independent exact G1 matched-value anchors or an independently qualified explicit matched-value field.

**Same-session technical coverage recovery (2026-08-24):** `SAME_SESSION_TECHNICAL_COVERAGE_RECOVERY_V1 = COVERAGE_CEILING_ESTABLISHED`. 881/1,507 official names have same-session technicals because 881 names have an exact 2026-08-24 bar; the remainder lack that bar. Future sessions keep the same exact-session contract and a 365-day OHLC lookback so recoverable history gaps cannot silently drop coverage.

**Governed state synchronization (2026-08-24):** The completed 2026-08-24 opportunity/decision session is frozen prospectively with exact source identities, including the 131 `PRIORITY_NOW` queue (34 entry-relevant, 97 non-entry-relevant) and an explicit `ABSENT_NOT_RECORDED` final-human-selection state. The normal AI-bundle path is temporally repaired at `bba1afe56375ea44397d06211f2570aedf446d84` to attach only the same-session governed queue and fail closed otherwise. `DAILY_SESSION_TEMPORAL_INPUT_LOCK_HARDENING_V1` then locks registered session inputs (and completed-session `frozen_input_identities`) so a completed/frozen session cannot acquire later `official_universe`/`event_context` by injection, a self-consistent manifest, or registry mutation. The roadmap does not open a follow-on source, issuer-IR, ranking, or selection milestone from this state: later attribution must wait for a genuinely later completed-session outcome horizon. PIT, liquidity, and authoritative-valuation blockers remain constraints, not automatic tasks.

**HOSE public SPA/XHR reconciliation (2026-08-24):** `HOSE_PUBLIC_XHR_AND_PERIODIC_SERIES_RECON_V1 = HOSE_PUBLIC_SCOPED_DATA_OPERATIONAL`. The official SPA's disclosed public XHR contract supplies a 405-row stock master, public HPG foreign-room series, corporate-event and disclosure indexes, and VN30 membership. It does not supply a public static room/free-float document series or a full-cohort current market-cap/liquidity corpus. `outStanding` is retained strictly as source-labelled exchange outstanding volume, never as common shares outstanding. Licensed catalog field contracts remain a distinct owner decision; no authenticated/licensed delivery was accessed.

---

## 1. Architectural Phases

```
┌──────────────────────────────────────────────────────────────────────────┐
│  PHASE 0: DATA FOUNDATION & PROVENANCE (P0-RECOVERY, P0-A, P0-B, P0-C)   │
│  - Raw lake ingestion, canonical universe, corporate actions, PIT price, │
│    volume semantics, and field-level temporal provenance contracts.      │
└────────────────────────────────────┬─────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  PHASE 1: RESEARCH EVIDENCE & FEATURE STORE EXPANSION (P1)               │
│  - Vectorized feature store, market internals, foreign flow scale-out,   │
│    and deterministic research evidence artifact generation.              │
└────────────────────────────────────┬─────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  PHASE 2: FUNDAMENTAL & VALUATION FOUNDATION (P2)                        │
│  - Sector-specific taxonomy packs, official filing multi-period OCR      │
│    materialization, and structured financial statement analysis.         │
└────────────────────────────────────┬─────────────────────────────────────┘
                                     │
                                     ▼
┌──────────────────────────────────────────────────────────────────────────┐
│  PHASE 3: STRATEGY ENGINES, PORTFOLIO RISK & BACKTESTING (P3)            │
│  - Polymorphic strategy evaluation, cross-sectional ranking, calibrated │
│    scenarios, portfolio sizing, and point-in-time alpha validation.      │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Milestone Matrix & Status

> **2026-08-22 program state:** `MARKET_WIDE_FINANCIAL_EVIDENCE_AND_SEMANTIC_COVERAGE_V1` is **CLOSED_PARTIAL / SOURCE_ROUTE_AND_EVIDENCE_CONSTRAINED**. Provider-financial semantic inference is closed: 520 issuer observations remain descriptive-only and the official panel is 13 issuers / 138 facts / 94 exact research metrics. `MARKET_WIDE_ENRICHMENT_AND_CANONICALIZATION_V1` is **PAUSED_RATE_LIMIT_CONSTRAINED**; it is not closed.

> `CORPORATE_ACTION_AND_PIT_AUTHORITY_V1` is **CLOSED_PARTIAL / OFFICIAL_EX_DATE_AND_PRICE_BASIS_EVIDENCE_CONSTRAINED**: the official ledger and HPG executed share-change lineage are reusable, but no representative event chain is PIT-qualified. Explicit official ex-date evidence is absent; record/listing/effective/trading dates cannot substitute, `RAW_AS_TRADED` remains not promoted, and provider-adjusted/retrospective prices remain non-PIT. `OFFICIAL_SOURCE_ROUTE_AND_EVIDENCE_COVERAGE_SCALEOUT_V1` is **PARTIAL_OFFICIAL_ROUTE_SCALEOUT_WITH_EXPLICIT_GAPS**: lifecycle reporting is cumulative (7 discovered, 1 ownership-proven, 1 technically reachable, 0 at later gates), while the separate terminal-state distribution is six access blockers plus ABB. ABB proved only ownership/reachability plus generic listing-policy links; no route qualified a demonstrated downstream evidence class, no candidate is ready for owner promotion, and no authority changed.

| Program / Milestone | Description | Status | Acceptance Gate & Authority Constraints |
|---|---|:---:|---|
| **Core Fundamental Valuation And Peer Context V1** | Product-critical Current Research fundamentals, growth/margins/ROE/ROA/DuPont/cash-flow/FCF, and P/E P/B P/S EV multiples with peer/history context | **COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE** | Checkpoint `5e58d79f69810d6800d1f58244c421acb0e4230f`, closeout `14cc93ccc5cec97c1de865b69eba958f5f18ee7a`. Average-balance same-provider ROE/ROA, own-history context, and sector/industry peer context over retained Financial V2. Completion record is not reopened by the 2026-09-02 product rebaseline. No score, rank, target, probability, UI, or exact-valuation promotion. Spec: `docs/ANALYTICS_AND_DECISION_FEATURE_SPEC.md`. |
| **Tactical Market Structure And Breakout V3** | Product-critical MA/ATR/RSI/MACD/relative strength; swings HH/HL/LH/LL; BOS/CHoCH; base/VCP/pivot/breakout/failed-breakout; relative-volume/acceleration/dry-up; market/sector breadth and relative strength; trigger/invalidation | **QUEUED_NEXT / NOT STARTED** | Extends governed Tactical V2; does not fork `entry_state`. BOS/CHoCH/VCP are deterministic technical inference, not institutional/order-flow proof. True ATR/MFE/MAE remain blocked while `HIGH_LOW_BASIS_NOT_COMPATIBLE`. Legacy `candle_scan`/`vn_indicators`/`stock_analyzer` SMC scoring is not current product authority. No UI. Queued does not mean started. |
| **Integrated Investment Decision Product V1** | Join fundamental, valuation/peer, tactical V3, trigger/invalidation, portfolio interaction, and prospective forward-return/MFE-MAE/false-negative-false-positive review into one decision product | **QUEUED_AFTER_TACTICAL / NOT STARTED** | Sole UI/Dashboard unfreeze. Portfolio fit stays separate from security attractiveness. No universal score. Prospective review is a product capability, not PIT backtest or exact execution. |
| **Tactical and Behavioral Engine V2** | Secondary technical/behavioral evidence (structure, setup tags, live confirmation/invalidation boundaries, compact projection) layered around the unmodified primary nine-state `entry_state` | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | `technical_structure_context.py`, `tactical_setup_tags.py`, `tactical_confirmation_invalidation_boundaries.py`, `tactical_behavior_context.py`. Does not fork or replace `watchlist_tactical_entry_classifier.entry_state`. Close-only per the retained `RETAINED_HIGH_LOW_SCALE_INCOMPATIBLE_NOT_USED` limitation; a localized `HIGH_LOW_BASIS_NOT_COMPATIBLE` block names unavailable feature classes per record without blocking the rest of that record. Real 2026-08-28 run: 1,683 candidates, 942 technical-eligible, confirmation READY 402/CONDITIONAL 279/UNAVAILABLE 1002, technical-invalidation READY 642/CONDITIONAL 239/UNAVAILABLE 802. Canonical market/sector regime source is `current_market_sector_leadership_context.market`; macro regime stays a separate, unconflated concept. No score, rank, target price, probability, fixed stop percentage, or universal indicator gate (RSI/ADX/MACD/relative-volume multiplier) anywhere. Not wired into `export_ai_bundle.py` or any Consumer/Dashboard path this milestone. |
| **Current Research Packet Dashboard Shadow Surfacing V1** | Shadow/opt-in product display of the canonical current research decision packet | **COMPLETE LOCALLY / SHADOW OPT-IN** | Extends `current_daily_decision_research_product/v2` and `--include-current-research-decision-packet`. Default output unchanged. Fail-closed on missing/malformed packet identity. Packet scenario remains evidence-bound Bear/Base/Bull. 1,507/1,507; residual 0; malformed 0. No default cutover, recommendation, probability, target, or sizing. |
| **Current Corporate Event Context V1** | Deterministic current-research corporate-event context from retained official events | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY** | Reuses `current_official_event_context` plus three retained issuer/VSDC chains. 1,507 denominator; 1,103 tickers with events; 4,453 events. Does not infer ex-date/execution, enable EVENT_DRIVEN, or alter priority/entry. No price-impact or probability. |
| **Current Financial Momentum Context V1** | Deterministic current-research financial momentum from retained comparable-period evidence | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY** | Reuses official FY YoY and provider series trends without mixing tiers. QoQ is not substituted as YoY. Banks/securities keep parent-earnings identity; industrial metrics are not forced. Coverage 107/1,507 with ≥1 comparable dimension; FULL 3. Does not enable `FUNDAMENTAL_IMPROVEMENT` or alter priority/entry. No forecast, VALUE, target, probability, or sizing. |
| **ADTV20 Window Integrity And Conflict Qualification V1** | Prove reconciliation completeness, bound conflicts, and make ADTV20 the expected trailing 20 trading sessions | **SCOPE RESTRICTED** | Session grid 60,280 with residual 0 (7 structurally absent parquet pairs). 5,447 conflicts classified; 5,258 are FHSC matched = G1+G4 raw shares; formula not rewritten. Qualified matched-value observations are HOSE discriminating exact only (214 tickers / 6,805 sessions). True ADTV20 READY 0 vs prior any-20 claim 295; PARTIAL 213; BLOCKED 190; NOT_APPLICABLE 1,104. No older-session gap fill, no missing-as-zero, no ADV20 volume, no sizing. |
| **Historical Matched Trading Value Authority V1** | Strongest defensible historical daily matched trading value and ADTV20 liquidity-input contract | **CEILING ESTABLISHED / SUPERSEDED IN PART** | G1 matched value `price×qty×10×1000` VND remains the identity. Window-integrity V1 narrows generic applicability to HOSE discriminating exacts and replaces the 295 any-20 claim with trailing-20 READY 0. `ADV20_MATCHED_VOLUME` not emitted. No sizing, participation cap, RAW_AS_TRADED, PIT, or frozen-session rewrite. |
| **Current Common Shares Authority And Scaleout V1** | Strongest defensible current-common-share basis market-wide, fed prospectively into current valuation | **CEILING ESTABLISHED** | Official 1,507 denominator, one terminal disposition each. No generic current-common source: 0 `QUALIFIED_CURRENT_COMMON_SHARES`; 1 official HPG anchor not coverage-through-session; 1,501 lagged issued-share research; 2 unverifiable (SSI/VCB); 3 CA-reconciliation (HCC/IPA/NAG). Identities not collapsed. Valuation formulas unchanged: READY 0; research-usable market cap 958→955; VALUE 0/1,507. Frozen 2026-08-21/24 identities unchanged. |
| **Market-Wide Current Valuation Research Scale-Out V1** | Official-universe CURRENT RESEARCH valuation inputs with explicit share-authority tiers and per-metric fitness | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY / OWNER REVIEW REQUIRED** | Official 1,507 denominator; 960 price-ready; share tiers 1,504 lagged / 1 official (HPG, not coverage-through-session) / 2 unverifiable (SSI/VCB fail-closed). Authoritative `READY` 0 on every method. Research-usable: market cap 958; P/B P/S EV EV/Sales 9; P/E 8; EV/EBITDA 0. Bank/securities industrial EV excluded. VALUE 0/1,507. `RESEARCH_USABLE` does not satisfy VALUE. Frozen 2026-08-21/24 valuation identities unchanged. No target price, DCF, PIT, RAW_AS_TRADED, or authority promotion. Artifact `market_wide_current_valuation:94d6116f6277cced9988281ea2d0327347079a973a8755128ee2ed726a5315aa`. |
| **Market-Wide Historical Research Context V1** | Retrospective within-ticker descriptive historical context for current research | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | `market_wide_historical_research_context/v1` reuses `mva_daily_research_bundle.market_features` on explicit 20-observation slices over retained DNSE snapshot/recovery bars. Trailing range, drawdown, volatility/momentum/MA persistence, provider-scoped relative volume, technical-state frequency, and structural context are emitted where retained observations permit; missing/short history is `MISSING`/`INSUFFICIENT_HISTORY`/`PARTIAL` with no calendar imputation or synthetic values. 52-week range requires 252 observed sessions and is currently 0. Cross-sectional historical comparison is blocked without PIT membership. Opt-in Producer attach is a sibling of current descriptive research and does not modify `research_priority`, `entry_action`, strategy eligibility, or sizing. Adjusted/retrospective history is never relabelled `RAW_AS_TRADED` or PIT-safe; no historical performance, win rate, alpha, backtest, ranking, or recommendation authority. Governed 2026-08-24 session and 2026-08-21 freeze are unchanged. |
| **Watchlist Tactical Entry-State Classifier V1** | Deterministic nine-state tactical entry classification joining the descriptive/screening/fundamental current-research lanes, wired Producer→Consumer→AI-context | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | `watchlist_tactical_entry_classifier.py` computes no new technical indicator, ranking, or evidence -- it joins `market_wide_current_descriptive_research`, `current_market_screening_opportunity_comparison_foundation`, and `market_wide_current_fundamental_research` (independently identity-verified; the 523-member fundamental cohort is a structurally distinct universe from the 1,510-denominator descriptive/screening cohort, so absence is `NOT_IN_FUNDAMENTAL_COHORT`, never a mismatch). `ticker_structure_state` (5 values) and the required nine-state `entry_state` taxonomy come from one ordered, gapless, exhaustively-verified decision table; `action` is a fixed 9→7 lookup (`EARLY_ENTRY` reserved for `EARLY_REVERSAL_CANDIDATE`, never requiring a confirmed uptrend); `is_full_position_ready` is `False` for every state except `BREAKOUT_READY` under liquidity/fundamental/market gates, enforced independently by both Producer and Consumer. Real run: 956/1,683 classified, all nine states populated, `full_position_ready_count=0` (an honest evidence-driven zero). No ranking, target price, probability, portfolio-sizing formula, RAW_AS_TRADED, PIT, or backtest authority. |
| **Market-Wide Current Fundamental Research Scale-Out V1** | Sector-aware, coverage-explicit join of the officially-qualified and provider-research fundamental lanes, wired Producer→Consumer | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Joins the current 13-issuer official panel (`p3f13_official_financial_evidence_scaleout.py`, now exposing `refreshed_panel_data`/`refreshed_fundamental_readiness` additively) with P3-F10's 523-candidate disposition matrix, retagged `OFFICIAL_QUALIFIED`/`PROVIDER_RESEARCH`/`BLOCKED` (13/507/3). Evidence presence and usable-metric presence stay distinct: PNJ/FPT are `OFFICIAL_QUALIFIED` but `fundamental_research_readiness=BLOCKED`, so 13 official facts but only 11 usable-metric issuers. Provider records now additionally retain only allowed same-ticker/same-provider/consecutive-quarter trends from their pinned retained canonical facts: 500 issuers have one or more of 1,236 available revenue/earnings growth, assets/equity direction, or operating-cash-flow direction metrics; 1,299 remain explicitly blocked (no retained reported series, no consecutive pair, or non-positive growth base). Every trend carries provider, periods, method, provider tier, lineage, limitations, comparability scope, and blocker; it never exposes an absolute provider fact as official. Bank/securities `NOT_APPLICABLE` official metrics remain preserved, never forced. Wired through `export_ai_bundle.py`/`build_ticker_context.py` via the existing default-off explicit-path/hash-verified attach convention. No new evidence acquisition, source-route discovery, valuation, ranking, recommendation, sizing, or backtest authority. |
| **Current Screening Research Consumption Closeout V1** | Opt-in Producer→Consumer consumption of retained deterministic screening/comparison context | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Extends only the existing `tickers[ticker].market_wide_current_descriptive_research` path with an optional nested `screening_comparison` section; no top-level packet, feature store, digest, workbench, or orchestration layer. Both explicit paths are default-off and hash/lineage/session/ticker-set verified. Every result preserves 1,510/960 coverage, exact screen eligibility/counts, independent flags, non-ordinal relative context, unavailable states, four insufficient sector cohorts, and SHB's `OTHER` warning. Frozen-time real artifact E2E is byte-identical through Consumer. No score/ranking/recommendation/forecast/valuation/probability/traded-value/ADV/ADTV/sizing/execution/PIT/RAW_AS_TRADED/backtest or authority promotion. |
| **Current Market Screening and Opportunity Comparison Foundation V1** | Coverage-aware descriptive screen flags and market/sector-relative ticker context | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Reuses the retained rebuilt 2026-08-21 current descriptive artifact only: 1,510 denominator, 960 observed, 956 same-session technical, 955 descriptive-liquidity, and 951 exact technical/liquidity intersection. Independent flags (trend-plus-positive momentum 241/956; momentum above cohort median 478/956; provider-scoped relative volume above cohort median 478/956; technical-plus-liquidity 951/1,510) each carry exact eligibility and coverage. Market-relative context is limited to the 956 technical records; sector-relative context is 946 retained sufficient-cohort records and otherwise fails closed. SHB's `G1/v=OTHER` warning propagates wherever liquidity is consumed. No score, ordinal ranking, recommendation, target/probability, traded-value/turnover/ADV/ADTV, sizing/execution, PIT, or RAW_AS_TRADED authority. |
| **Market-Wide Current Technical Coverage Scale-Out V1** | Bounded extended-history recovery for existing current technical features | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Diagnosed 52 stale and 695 unavailable records before acquisition. Only 197 current-session-observed/history-insufficient gaps were fetched through 20 sequential resumable DNSE batches: 193 recover complete windows, 4 remain insufficient. Same-session technical coverage is 956/1,510 (63.311258%; previously 763/1,510), stale remains 52, unavailable is 502 (498 target-bar-missing, 4 history-insufficient). Existing `market_features()` contract only; provider-scoped descriptive history, no RAW_AS_TRADED/PIT, ranking, recommendation, liquidity, sizing, execution, or Consumer schema promotion. |
| **Current Market Universe and Breadth Foundation V1** | Orthogonal current equity-candidate membership and session-observability projection | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | The 1,683 P3F9B candidates resolve to a 1,635 current-DNSE-security-master `EQUITY` candidate denominator and 48 explicit unknown memberships; session availability never changes membership. The retained 2026-08-21 observation ledger is 960 observed, 550 missing, 173 provider-rejected, and 0 incomplete; the eligible observed cohort is 960/1,635 (58.715596%). Current descriptive breadth/cross-sectional screening only, with coverage mandatory; no authoritative active-listing, historical/PIT, ranking, valuation, recommendation, liquidity, or execution authority. |
| **Current Official Market Universe Integration V1** | First-party HNX/UPCoM and HOSE current exchange-master reconciliation and compatible consumer filter | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY** | `current_official_market_universe/v1` retains 1,507 exact current official-master matches (1,104 HNX/UPCoM; 403 HOSE), 176 Stock Lookup-only residual dispositions, and 18 official-only rows for review. It replaces no raw dataset and adds no production candidate automatically. Breadth/screening/tactical/strategy/scenario/daily consumers retain their existing logic behind an explicit ticker-filter adapter; historical PIT and survivorship-safe membership remain blocked. |
| **Current Official Event Context Integration V1** | Retained official exchange-event context through existing Corporate Intelligence | **COMPLETE LOCALLY / CURRENT RESEARCH CONTEXT PIT LIMITED** | `current_official_event_context/v1` maps 210 upcoming/today/recent explicit-ex-date events to 189 current tickers through the existing CI event shape, then proves scenario/strategy/Daily Product compatibility without new classification rules. Indexed HNX publication timing is absent, so the adapter cannot support historical known-at replay. No score, event-impact assertion, adjustment, RAW_AS_TRADED, PIT, or backtest authority is emitted. |
| **Current Opportunity Prioritization V1** | Deterministic, strategy-lane current research queue | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY** | `current_opportunity_prioritization/v1` preserves separate lane priorities rather than emitting a global score. The EVENT_DRIVEN gate now accepts only current price/share-affecting official events (plus the retained legacy current qualified corporate-action case), excluding PAST, date-incomplete and AGM-only evidence. Current research prioritization remains non-actionable and does not grant sizing, execution, valuation, PIT, or backtest authority. |
| **Current Universe Status and Session Coverage Resolution V1** | Cross-provider-corroborated listing/activity status and provider support, orthogonal to membership and session observability | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Extends the breadth-foundation artifact unchanged. Already-retained VCI `metadata.exchange="DELISTED"` corresponds exactly 1:1 (173/173) to the P3F9B `PROVIDER_REJECTED` cohort, resolving all 173 -- including the prior 48 `SECURITY_MASTER_SYMBOL_NOT_RETAINED` unknowns -- to a new `INACTIVE_OR_DELISTED` state with 0 residual. The 550 `SESSION_MISSING` records split 408-with-nearby-activity / 142-without via their already-retained OHLC lookback windows, both staying `ACTIVE_LISTED_NO_QUALIFIED_SESSION_OBSERVATION` and never promoted to inactive or to a proven no-trade session. Corrected denominator `current_active_equity_denominator = 1,510`; coverage recomputes to 960/1,510 = 63.576159%. `canonical_universe_tiers.py` and `canonical_instrument_reconciliation.py` remain unmodified; no active-listing, `ACTIVE_UNIVERSE`, historical/PIT, ranking, valuation, recommendation, liquidity, or execution authority. |
| **Market-Wide Current Research End-to-End Integration V1** | Producer -> AI bundle -> Consumer opt-in path for market_wide_current_descriptive_research | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | `export_ai_bundle.py` gains `--include-market-wide-current-descriptive-research` (disabled by default), mirroring the existing liquidity attach layer; `ai-core-private`'s `build_ticker_context.py` gains the matching `*_contract_pass_through`, and `prompts/ai_analysis_templates.md` gains a new lane plus four prohibited-claims items (mandatory coverage disclosure, no stale-as-current, no filling insufficient-coverage sectors, no ranking/recommendation/sizing derivation). SHB's residual and a stale technical-feature record both survive a real 6/6 frozen-time E2E run unmodified; no new feature store/packet/workbench/orchestration layer; no historical PIT, RAW_AS_TRADED, active-universe, corporate-action, ranking, recommendation, portfolio, or valuation authority. |
| **Market-Wide Current Descriptive Research V1** | One deterministic market-wide breadth/sector/cross-sectional/liquidity research artifact over the corrected current-descriptive universe | **COMPLETE LOCALLY / COHERENT PARTIAL / OWNER REVIEW REQUIRED** | Reuses the existing pipeline unmodified (`mva_daily_research_bundle.market_features`, `market_regime_breadth_context._descriptor`, `sector_relative_research_context`'s cohort/percentile/classification helpers, `market_wide_current_liquidity_research.content_identity`) -- no new feature store, digest, workbench, packet, case, or orchestration layer. Joins the 1,510/960 universe-resolution split, the P3F9B snapshot, and the retained liquidity artifact, all session-cross-checked (2026-08-21); every technical feature carries an explicit `is_current_session` flag so a stale prior-session value can never be counted as today's breadth (763/960 same-session-eligible, 52 stale tracked separately). Market breadth 457/160/146 (`MARKET_BREADTH_MIXED`); sector breadth 20/24 cohorts `AVAILABLE`, 4 fail closed; liquidity 955/1,510 eligible with SHB's four-unit G1/v `OTHER` residual as the sole warning. No ranking, recommendation, valuation, ADV/ADTV, sizing, execution, historical PIT, corporate-action, backtesting, or active-universe authority. |
| **Capability-First Scenario Distribution V1** | Deterministic descriptive distribution of retained scenario cases | **COMPLETE LOCALLY** | `CAPABILITY_FIRST_SCENARIO_DISTRIBUTION_V1` records the real scenario denominator (12) and universe denominator (524). It emits no probability, target, recommendation, or ranking authority. |
| **Qualified Liquidity Inputs Reconciliation V1** | Retained DNSE/FHSC volume reconciliation and canonical board-semantic correction | **COMPLETE LOCALLY / RECONCILIATION EVIDENCE ONLY** | `QUALIFIED_LIQUIDITY_INPUTS_RECONCILIATION_V1` confirms DNSE `v` = FHSC matched volume in 35/35 discriminating observations within its tested scope. Canonical board labels are resolved, while numeric board composition remains unavailable; `adv_turnover_input_eligible = false`. No liquidity/sizing/portfolio/PIT/RAW_AS_TRADED authority is promoted. |
| **Authoritative Current Common Shares Qualification & Scaleout V1** | Raw-first KBS public-profile candidate investigation | **COMPLETE / NO_NEW_SCALABLE_AUTHORITY** | Six immutable representative KBS profile endpoint responses were retained. Every response returned HTTP 400 with the same hash-invalid non-JSON body, so no `outstanding_shares` observation or provider semantic/effective-date evidence was acquired. The 1,683-candidate ledger records six `UNAVAILABLE` and 1,677 justified non-attempts; strict valuation, peer valuation, and Value remain blocked. |
| **Current Share Basis Capability Reconciliation V1** | Retained current-share semantic and fitness-for-use reconciliation | **COMPLETE / BLOCKED_BY_EVIDENCE** | The 1,683-row provider metadata inventory yields 1,680 structurally usable but non-authoritative `issued_shares` proxies; the 11-row valuation corpus has 0/11 authoritative current-common denominators. `VCI.overview.issue_share` is never aliased to outstanding shares; SSI/VCB corporate-action cases remain stale/blocked. Current market cap, P/E, P/B, P/S, EV, historical/PIT, and share authority remain blocked. |
| **Current Common Shares Official Evidence Acquisition V1** | Finite official-evidence acquisition for current common-share denominators | **COMPLETE / NO_QUALIFYING_CURRENT_SHARE_EVIDENCE** | The bounded 11-symbol cohort has 0/11 denominators qualified through 2026-08-19: nine currentness-unresolved, SSI/VCB execution-unresolved. HPG’s 8,442,964,520 official count is effective 2026-07-02 but only continuous through 2026-07-30. Current valuation remains blocked; no broad official-share acquisition resumes without new finite evidence. The complete-stream retention fix is hash-verified. |
| **Provider-Reported Current Valuation Proxy V1** | Explicit non-authoritative issued-share valuation proxy | **COMPLETE / NON-AUTHORITATIVE RESEARCH PROXY** | `PROVIDER_ISSUED_SHARES_PROXY` / `NON_AUTHORITATIVE_RESEARCH_PROXY` produces 9 proxy market cap/P-B/P-S/EV-Sales, 8 P-E, and 0 EV/EBITDA outputs across the 11-symbol cohort. SSI/VCB are corporate-action blocked; authoritative valuation remains blocked. The next blocker is broader retained price/financial-input coverage, not proxy abstraction expansion. |
| **Market Price/Volume Basis Authority V1** | Consolidated fitness-for-use matrix over existing DNSE/FHSC price and volume/traded-value evidence | **COMPLETE LOCALLY / CONSOLIDATION, NO NEW AUTHORITY** | `market_price_volume_basis_authority.py` reuses 11 already-qualified capability sources (`market_data_source_authority`, `provider_price_basis_registry`, `dnse_ohlc_price_basis_capability`, `dnse_provider_native_closed_ohlc`, `current_valuation_input_authority`, `market_volume_value_semantic_contract`, `market_capability_taxonomy`, `dnse_fhsc_volume_basis`, `dnse_fhsc_market_composition_scaleout`, `market_volume_capability_matrix`, `official_corporate_action_ledger`) across 8 downstream uses. Current DNSE close is `ELIGIBLE` for current-descriptive and current-valuation-price-input use; every liquidity/ADV-ADTV/position-sizing/PIT/backtest cell is `BLOCKED`/`NOT_APPLICABLE`, enforced by `assert_registry_fail_closed()`. The documented G1/G4/T1/T3/T4/T6 board-label mapping is verified unchanged; numeric board composition and DNSE traded value remain unavailable. No RAW_AS_TRADED, PIT, liquidity, sizing, or valuation-formula authority promoted. |
| **DNSE Trades and Liquidity Basis Qualification V1** | Bounded live `trades_latest`/`trades_history` adapter, G1 x10 cross-check, session liquidity-research contract | **COMPLETE LOCALLY / CURRENT-SESSION BOARD COMPOSITION UNLOCKED, HISTORICAL STILL BLOCKED** | `dnse_trades_liquidity_basis.py` + `tools/derive_dnse_trades_liquidity_basis.py` build a small new adapter over the already-registered `trades_latest`/`trades_history` endpoints (never resurrecting the orphaned `dnse_trades_canonical_shadow.py`, `SOURCE_GENERATOR_NOT_IN_CURRENT_MAIN_ANCESTRY`). A bounded 23-call live run proves `10 x G1.totalVolumeTraded == OHLC daily v` exactly across 5 fresh cross-sector tickers (HPG/VCB/SSI/FPT/QNS, 2026-08-21) -- independent live corroboration of the still-unpromoted shadow `C5` candidate -- and that complete `trades_history` reconstruction is activity-dependent: QNS fully exhausts in 4 bounded pages (confirming a true zero-put-through session) while HPG/VCB/SSI stay `PARTIAL_BOUNDED_SCAN` under the same cap on two different sessions. `trades_latest` alone (no pagination) yields a complete current-session per-board reading, so `CURRENT_SESSION_LIQUIDITY_RESEARCH = ELIGIBLE` (research-descriptive only); historical/ADV/ADTV/sizing/execution/PIT remain `BLOCKED`/`UNKNOWN`, enforced by `assert_fail_closed()`. `grossTradeAmount` is retained only as a non-authoritative direct value candidate with an open, board-dependent scale ambiguity; no derived value is computed without an explicit external lot-multiplier. `QUALIFIED_LIQUIDITY_INPUTS = NO`, `POSITION_SIZING_IS_SAFE = NO` unconditionally re-confirmed. |
| **Market-Wide Current Liquidity Research Scale-Out V1** | Deterministic, resumable current-session DNSE board-composition materialization | **COMPLETE LOCALLY / COHERENT PARTIAL** | 68 fixed foreground batches complete all 1,683 retained canonical candidates for session 2026-08-21: 955 descriptive current-session records, 241 incomplete, 479 missing, and 8 provider-rejected; zero unattempted. G1/G4/T1/T3/T4/T6 stay separate; G1 matches compatible OHLC `v` in 954/955 eligible records, while SHB's four-unit residual remains explicit. Raw composition ratios are descriptive only. `grossTradeAmount`, ADV/ADTV, sizing, execution, PIT/backtest, and RAW_AS_TRADED stay blocked/unpromoted. |
| **Market-Wide Current Research-Universe Qualification V1** | Deterministic current descriptive research denominator with explicit exclusion ledger | **COMPLETE LOCALLY / OWNER REVIEW REQUIRED** | The retained 1,683 P3F9B candidates resolve to 960 current descriptive included equities, 675 retained-current-market exclusions (550 `SESSION_MISSING`, 125 `PROVIDER_REJECTED`), and 48 explicit missing-security-master unknowns. DNSE instrument class, exchange, and unknown listing context remain distinct; this is a current breadth/cross-sectional-screening denominator only, not authoritative membership, historical/PIT, ranking, valuation, recommendation, or RAW_AS_TRADED authority. |
| **Current Liquidity Research Feature Integration V1** | Wires the retained market-wide current liquidity research artifact into the existing `export_ai_bundle.py` feature-consumption contract | **COMPLETE LOCALLY / COHERENT PARTIAL** | New opt-in `--include-market-wide-current-liquidity-research` / `--market-wide-current-liquidity-research-path` attach layer, reusing the same `tickers[ticker].*` pattern as `foreign_flow`/`current_state_market_risk`/`current_state_price_analytics`/`current_state_relative_valuation` -- no new feature store, digest, workbench, or orchestration abstraction, and no DNSE trade reacquired or re-qualified. Disposition, board composition, `g1_v_reconciliation` (SHB's `OTHER` residual preserved, never coerced), and `liquidity_research_contract` are reused verbatim; the loader fails the whole step closed on a missing file or a hash mismatch against `market_wide_current_liquidity_research.content_identity()`. That verification gate surfaced and fixed a pre-existing hash-scope defect in `tools/run_market_wide_current_liquidity_research.py`'s `consolidate()` (the recorded hash covered a subset of the persisted payload); the retained checkpoint was regenerated offline from its own already-retained batches with zero data drift (only the two identity fields changed) and now self-verifies. Not enabled in any default/production invocation; no Consumer (`ai-core-private`) pass-through was added. |
| **DNSE/FHSC Market Composition Scale-Out V1** | Fixed 12-issuer cross-exchange volume-composition replay | **COMPLETE LOCALLY / HOSE-ONLY PARTIAL** | Retained HPG/VCB/SSI/FPT HOSE evidence yields 33/33 discriminating exact DNSE-volume = FHSC-matched confirmations plus seven non-discriminating zero-put-through rows. `DNSE_MATCHED_VOLUME_SEMANTICS_HOSE_SCALEOUT_VALIDATED` is not a universal mapping: FHSC rate limiting left HNX/UPCOM unavailable after the bounded 24-call budget. DNSE generic traded value remains unavailable; no liquidity, turnover, sizing, capacity, price, PIT, RAW_AS_TRADED, or provider authority changes. |
| **HNX Enumerable Universe, Event, and Disclosure Scaleout V1** | First-party security lists and full rights calendar | **HNX_UNIVERSE_EVENT_OPERATIONAL_DISCLOSURE_BINDING_PARTIAL** | 299 listed plus 821 UPCoM exchange-stock-list candidates and all 4,628 event rows are retained. Strict accounting common-equity and H1 filing bindings remain unresolved. |
| **DNSE/FHSC Volume Basis Qualification V1** | Bounded retained closed-session volume-identity reconciliation | **COMPLETE LOCALLY / BOUNDED SHADOW BASIS** | Across HPG/VCB/SSI × 2026-08-14, 17–20, all 11 discriminating nonzero-put-through observations establish `DNSE_OHLC_VOLUME_MATCHED_EMPIRICAL`; four zero-put-through rows are consistent but cannot distinguish matched from total. FHSC remains shadow-only. This creates neither liquidity/turnover/sizing/market-capacity authority nor any price, PIT, RAW_AS_TRADED, or source promotion. |
| **DNSE Closed OHLC Provider-Native Semantics V1** | Official-doc-backed bounded semantic gate for retained closed-session OHLC | **COMPLETE LOCALLY / BOUNDED REPRESENTATION-SAFE USES** | `PROVIDER_NATIVE_CLOSED_OHLC_REPRESENTATION` is valid for deterministic reconciliation, field consistency, anomaly detection, and scale-invariant same-representation transformations with independently confirmed interval continuity. It does not establish formal unit, adjustment/finalization semantics, RAW_AS_TRADED, valuation, liquidity, sizing, PIT, or FHSC authority. |
| **DNSE Uniform OHLC Anchor Qualification V1** | Bounded raw-byte DNSE 2026-08-20 representation anchor | **COMPLETE LOCALLY / REPRESENTATION READY** | Three raw `/price/ohlc` payloads retain uniform provider-native O/H/L/C for HPG/VCB/SSI. P3F9B V2 removes its close-only transform; V1 remains historical defect evidence. This is `EMPIRICALLY_UNIFORM_REPRESENTATION_UNIT_UNDOCUMENTED`, not formal unit, adjustment, RAW_AS_TRADED, or provider authority. FHSC raw equality is reconciliation compatibility only; semantic replay remains `BASIS_UNRESOLVED`. |
| **FHSC/DNSE OHLC Reconciliation Integrity V1** | Retained-evidence correction of the FHSC 10×10 OHLC comparator | **COMPLETE LOCALLY / ANCHOR UNSUITABLE** | The P3F9B DNSE snapshot stores O/H/L provider-native but materializes close ×1,000. The corrected uniform-representation gate excludes all 400 pairs as `NOT_COMPARABLE`; no FHSC scale transform or authority is qualified. |
| **FHSC Historical Price Semantics Qualification V1** | Official documentation and retained ten-ticker OHLC scale calibration | **SUPERSEDED EMPIRICAL MATRIX / SEMANTICS PARTIAL** | The retained FHSC bytes and documentation scope persist, but its 282 exact 1:1 / 94 exact ×1,000 / 24 SSI matrix is superseded because it compared DNSE raw O/H/L with a normalized DNSE close. `NO_TRANSFORM_QUALIFIED`; no FHSC authority, valuation, PIT, RAW_AS_TRADED, liquidity, or sizing change. |
| **FHSC DNSE Retained Live Reconciliation V1** | Bounded Tier-1 retained daily-history and realtime comparison | **COMPLETE LOCALLY / PARTIAL** | Exactly 6 successful FHSC responses were retained for HPG/VCB/SSI. Historical 2026-08-20 closes show an empirical ×1,000 relation to retained DNSE closes, but FHSC's published route has no price-unit, adjustment, or finalization declaration, so all comparisons fail closed as `UNIT_MISMATCH`. Realtime listing/volume/foreign fields are current-session descriptive observations only; no FHSC authority or DNSE replacement. |
| **FHSC Reference Reconciliation Foundation V1** | Generic provider-reference comparison foundation | **COMPLETE LOCALLY / CREDENTIAL BLOCKED** | FHSC is modeled only as `SHADOW_REFERENCE_PROVIDER`, with no provider replacement or authority promotion. `provider_reference_observation/v1` requires session, finalization, unit, basis, and timestamp semantics before comparing values; provider majority has no authority effect. No FHSC credential exists for the bounded read-only probe, so retained DNSE HPG/VCB/SSI closes are currently `MISSING_SOURCE_OBSERVATION` against FHSC. FHSC financial statements remain descriptive-only. |
| **Official Source Registry Owner Promotion V1** | Explicit owner activation of nine retained-evidence issuer IR routes | **COMPLETE LOCALLY** | Exactly `ABS`, `ABW`, `ACB`, `MBB`, `MWG`, `TCB`, `AAA`, `AAT`, and `BID` now pass activated qualification (9/9). Authorization is only for downstream official-document acquisition. `bidv.com.vn` is the retained final host under `redirect_domain_authority/v1`; `tiensonaus.com` only is activated for AAT. ABT and `tienson.vn` remain unapproved; no financial-fact/provider/RAW_AS_TRADED/liquidity/valuation/recommendation authority changes. |
| **Official Source Route & Evidence Coverage Scaleout V1** | Reusable lifecycle, ownership/evidence contract, and bounded direct official-route qualification | **PARTIAL_OFFICIAL_ROUTE_SCALEOUT_WITH_EXPLICIT_GAPS** | Deterministic hash-bound records prevent lifecycle overclaim, guessed domains, duplicate routes, and source-family conflation. Cumulative coverage is 7/1/1/0/0/0 from discovery through approval; exclusive terminal states are six access failures plus ABB technically reachable. ABB's generic listing policy is not demonstrated financial/corporate-action/share-change evidence. 0 owner-promotion candidates and 0 approval/authority changes. |
| **Bounded Official Route Evidence Enrichment V1 / Redirect-Domain Authority Correction V1** | Bounded first-party route enrichment with requested-to-final redirect validation | **COMPLETE LOCALLY** | `redirect_domain_authority/v1` preserves requested/final hosts and permits only exact `www.` canonicalization with retained lineage. BID’s `www.bidv.com.vn` → `bidv.com.vn` route is `SAFE_SAME_AUTHORITY_REDIRECT`; its retained evidence supports the separately owner-approved final-host activation. Cross-domain and non-`www` subdomain redirects still fail closed. |
| **Retained Official Route Ownership Evidence Acquisition V1** | Historical bounded first-party ownership evidence retention for 17 Wave-2 issuers | **SUPERSEDED SEMANTICS / RETAINED BYTES** | The ten retained objects and content hashes remain immutable. Their catalog-injected legal/statutory spans and unconditional 10-candidate result are superseded by byte-derived prospective review; no registry, document, fact, or readiness mutation occurred. |
| **Prospective Route Ownership Review Contract V1** | Byte-derived non-activating review before owner registry admission | **COMPLETE LOCALLY** | The prospective contract remains separate from activation. Its six `OWNER_REVIEW_READY` records plus three later enriched `OWNER_REVIEW_READY` routes are now activated only through the separate explicit owner decision; ABT remains insufficient and historical AAT remains a conflict. |
| **Official Financial Source Route Discovery V1 / Evidence-Binding Correction V1** | Offline deterministic route hints with retained-evidence-only ownership qualification | **CORRECTED LOCALLY** | Historical V1 is `IMPLEMENTATION_PRESENT_BUT_QUALIFICATION_INVALIDATED`: its 28 qualified claims were static assertions/templates, not retained evidence. Corrected replay consumes zero retained ownership objects and returns 34 `OWNERSHIP_EVIDENCE_MISSING` / 0 qualified across the same 17 issuers. Generic HSX/HNX hosts are not ticker-specific financial-filing routes; zero registry candidates, document/fact, or readiness changes. |
| **Official Financial Evidence Scale-Out Wave 2** | Bounded Wave 2 official evidence qualification & source discovery over empirical cohort | **PARTIAL LOCALLY** | Evaluates 17 bounded candidates selected under Layered Topology B across Banks (5), Securities (2), and Corporate (10). Closed-world route discovery confirms all 17 fail closed with `NO_APPROVED_ROUTE_FOUND` and `OWNERSHIP_EVIDENCE_MISSING`. Baseline qualified panel remains 100% stable at 13 issuers / 138 canonical facts / 94 exact P3-B metrics (510 blocked). Zero synthetic facts, zero unpromoted provider authority, zero PIT/valuation/recommendation promotion. |
| **Prospective Research Cohort Diagnostics V1** | Deterministic cohort diagnostics and sample maturity across prospective learning horizons | **COMPLETE LOCALLY** | Maps 1,047 observations to 87 cohort summaries across 10 descriptor dimensions and H1/H3/H5 horizons. Real H1 transition (521 observed, 2 missing) is classified as `OBSERVED_IMMATURE_SAMPLE` and descriptive only; H3/H5 remain 100% `PENDING_OUTCOMES`. Zero alpha/backtest/predictive/recommendation/sizing authority; no RAW_AS_TRADED/PIT promotion. |
| **Prospective Daily Rollforward & Learning Ledger V1** | Offline immutable T-state sealing and session-counted prospective learning | **COMPLETE LOCALLY** | Preserves the first 2026-08-20 → 2026-08-21 descriptive H1 attribution, seals a separate 524-member 2026-08-21 shadow cohort before any later exact session, and retains H1/H3/H5 maturity explicitly. Missing same-session research context is unavailable, never silently carried forward. No backtest/PIT/RAW_AS_TRADED/alpha/edge/recommendation authority. |
| **Prospective Research Case & Learning Ledger V1** | Prospective immutable decision-time case, append-only evidence update, and observational learning contract | **READY FOR REVIEW** | Builds from the 2026-08-20 523-member decision/AI workflow without historical backfill: readiness is 523 `CASE_CREATABLE`, 0 `NEEDS_MORE_EVIDENCE`, 0 `NOT_CREATABLE`, and no cases are persisted. Frozen case content retains decision/AI/validated-draft/human-review/evidence lineage; later source-bound observations and scenario/catalyst states are append-only. Test updates are `TEST_FIXTURE` and excluded. No price-only thesis proof, recommendation, automatic model rule, authority promotion, PIT/RAW_AS_TRADED, liquidity, portfolio, or execution capability. |
| **Analyst Research Workbench & Case Operations V1** | One analyst-facing orchestration surface for retained research state, AI/human handoff, local case operations, trace, and learning | **READY FOR REVIEW** | `analyst_research_workbench.py` reuses, never recalculates, the dated decision/AI/case contracts. The exact 2026-08-20 523-member snapshot is selected explicitly; 2026-08-21's 524-member shadow is not substituted. All 523 are `CASE_STRUCTURE_ELIGIBLE` (the prior prospective `CASE_CREATABLE` meaning), while current operational counts are 0 validated drafts, 0 qualifying reviews, 0 `CASE_CREATION_READY`, and 0 local cases. It validates drafts before review, preserves human provenance, creates only local-session immutable cases, enforces source-identity/timestamp-ordered updates, and returns traceable read-only learning. No runtime DB write, recommendation, model/authority promotion, liquidity/PIT/valuation, portfolio, or execution capability. |
| **Durable Prospective Research Case Store V1** | Explicit local durable case envelopes and append-only event-chain persistence for the workbench | **READY FOR REVIEW** | A caller-selected non-production root persists immutable content-addressed T0 case/draft/validation/review/HUMAN_EDIT envelopes and later content-addressed update events under one-writer semantics. Reload verifies identities and deterministically reconstructs history/lifecycle/claim/scenario/catalyst/provenance. Fixture-origin cases and `TEST_FIXTURE` updates are excluded from learning. No migration of 523 structural records, committed mutable data, implicit runtime/production DB, recommendation, analytical authority promotion, liquidity/PIT/valuation, portfolio, or execution capability. `DURABLE_CASE_SYSTEM_READY` applies only when real future validated-review inputs are supplied. |
| **Prospective Research Case Operations V1** | First real retained-evidence operating cohort and human-review continuation manifest | **READY FOR HUMAN REVIEW** | `prospective_research_case_operations.py` selects `HPG`/`VCB`/`SSI`/`AAN`/`AAA` for evidence/authority-pattern diversity from the exact dated 2026-08-20 523-member snapshot. It freezes actual decision/AI-input/evidence identities, supplies the existing prompt/schema, human-review queue, future-evidence relationship readiness, and zero-outcome learning baseline. All five remain `MODEL_DRAFT_PENDING`; no validation, human review, real durable case, future evidence, or timestamp is fabricated. The separate 2026-08-21 524-member shadow is not substituted; no recommendation, authority promotion, monitoring, provider activity, or persistence write occurs. |
| **P0-RECOVERY** | Canonical Trades Materialization & Task 160 | **CLOSED** | `TERMINAL_SUCCESS_QUALITY_RESTRICTED`. 18.1M trades across 40 sessions. |
| **P0-A.1** | OHLC Raw Coverage Completion | **COMPLETE** | 1,528/1,660 successful; 132 `PERMANENT` provider-rejected failures classified. |
| **P0-A.2** | Corporate Action Evidence Scale-Out | **COMPLETE** | Official document authority & multi-event extraction integrated (`official_corporate_action_ledger.py`). |
| **P0-A.3** | Market-Wide PIT Price Reconstruction | **PART A COMPLETE / PART B BLOCKED** | • Part A: Multi-session WebSocket collection is `COMPLETE_EVIDENCE_ACQUIRED` (Sessions 1–4).<br>• Part B: Event-window qualification is `BLOCKED_PENDING_QUALIFIED_EX_DATE`.<br>• **`RAW_AS_TRADED = NOT_PROMOTED`**. |
| **P0-A.4** | Scoped Price-Basis Promotion | **DEFERRED** | Dependent on verified official ex-date notices. |
| **P0-B** | Qualified Volume/Liquidity Basis & Turnover | **CLOSED** | `TERMINAL_CLOSEOUT_NO_AUTHORITY_PROMOTION`.<br>• $C_5 = 10 \times G_1$ shadow empirical candidate (99.81%), unit `UNKNOWN`.<br>• 67 residuals unresolved.<br>• Traded value `OBSERVED_ABSENT`.<br>• **`QUALIFIED_LIQUIDITY_INPUTS = NO`**, **`POSITION_SIZING_IS_SAFE = NO`**. |
| **P0-C.1** | Canonical Instrument-Master Reconciliation | **COMPLETE** | Reconciled across 3,250 instruments (1,660 listed equity candidates, 1,590 unclassified). |
| **P0-C.2** | Universe-Tier Hierarchy & Exclusion Ledger | **COMPLETE** | `ACTIVE_UNIVERSE` fails closed as `UNKNOWN` pending verified exchange/listing evidence. |
| **P1** | Feature Store Normalization & Multi-Session Export | **COMPLETE** | `cross_sectional_export.py` normalized semantic taxonomy, multi-session export contract, fail-closed PIT/liquidity boundaries (`bb0cafa4417471b0`). |
| **P2** | Multi-Period Fundamentals & Sector Normalization | **COMPLETE (`P2_CLOSEOUT_COMPLETE`)** | `multi_period_financial_panel.py` deterministic panel & `generic_financial_canonicalizer.py` dictionary-driven scale-out.<br>• Integrates all authoritative Phase 2 financial fact cohorts: promoted corporate facts (`GAS`, `VRE`, `HPG`, `VNM`, `PAN`, `PVD`, `NVL`, `POW`, `QNS`), promoted VCB FY2024 bank scope (15 facts), promoted SSI FY2024 securities scope (16 facts), Layered Entity Classification Topology B (40 positive, 1,620 unpromoted fail-closed as UNKNOWN).<br>• Enforces strict sector boundaries, intermediary corporate debt ratio inapplicability (`NOT_APPLICABLE`), normalized `ENDING_EQUITY_ROE_PROXY`, zero synthetic observations, zero forward-fill, zero scope/currency mixing.<br>• Deterministic closeout artifact emitted: `p2_closeout_financial_panel_artifact.json` (`p2_closeout_financial_panel:46335e0b527ed39cbbcc8082508c85e86892f83137bf205f416e9d0bbbbc8eed`).<br>• Phase 3 entry evaluated: `PHASE3_ENTRY_READY_FOR_BOUNDED_REVIEW` with strict negative gates (`RAW_AS_TRADED = NOT_PROMOTED`, `QUALIFIED_LIQUIDITY_INPUTS = NO`, `POSITION_SIZING_IS_SAFE = NO`). |
| **P3** | Portfolio Sizing, Execution, Backtest | **FAIL-CLOSED (`P3A_BLOCKED_PENDING_QUALIFIED_EX_DATE`)** | Blocked until upstream price and liquidity qualifications are complete.<br>• `P3-A` evaluated: No retained official document contains an explicit official ex-date (HPG, SSI, VCB, VNM state only record dates, payment dates, approval dates, or new-shares listing dates). Fail-closed invariant strictly prohibits inferring ex-dates from record dates or settlement rules.<br>• `RAW_AS_TRADED = NOT_PROMOTED`, `QUALIFIED_LIQUIDITY_INPUTS = NO`, `POSITION_SIZING_IS_SAFE = NO`. |
| **P3-B** | Sector-Aware Fundamental Quality & Research Readiness | **COMPLETE** | Independent price/liquidity-free research lane. The deterministic engine consumes only P2 authoritative facts, preserves calculation/evidence lineage and scope/currency/PIT gates, distinguishes exact results from ending-balance proxies, and applies corporate/bank/securities-specific metrics. It produces no score, ranking, valuation, strategy, or portfolio output. |
| **P3-C** | Multi-Period Comparative Financial Evidence Scale-Out | **PARTIAL** | SSI FY2023 audited consolidated filing is retained, SHA-256-verified, and replayed through the generic sector recognizer. Six facts were promoted, taking the panel from 102 to 108 qualified facts and upgrading SSI FY2024 ROA/ROE to exact average-denominator results. VCB FY2023 and corporate residuals remain unqualified; no CapEx proxy was introduced. |
| **P3-D** | Residual Comparative Financial Evidence Scale-Out & Gap Reconciliation | **PARTIAL** | The P3-C 55-gap inventory is correct by definition. Five retained approved HPG/PVD filings yielded ten generic-recognizer facts, lifting the panel 108→118 and P3-B exact status 75→86; residual gaps are 42. HPG/PVD FY2022 ROE upgrades from proxy to exact. VCB FY2023 remains blocked by the unapproved candidate host; no source registry change or CapEx proxy. |
| **P3-E** | Fundamental Coverage Closeout & Valuation-Input Readiness Gate | **COMPLETE** | Six retained approved FY2024 reports provide twelve exact revenue/total-assets facts, lifting the panel 118→130 and P3-B exact status 86→94. The comparative evidence lane is `COMPARATIVE_EVIDENCE_LANE_CLOSED`: 28 residuals are structural boundaries and VCB FY2023 is the sole source-authority block. Financial valuation-input readiness is explicit, while market/PIT/share authority remains blocked and no valuation is produced. |
| **P3-F** | Current Market Valuation Basis Activation & Sector-Aware Valuation MVP | **PARTIAL** | One exact DNSE `CURRENT_MARKET` snapshot is qualified: HPG close on 2026-07-30 with official current-share coverage through that same session. It emits descriptive-only P/E, P/B, P/S, and EV/Sales; `RAW_AS_TRADED` and historical valuation remain unpromoted. All other issuers preserve issuer-level price/share blockers; VCB's retained DNSE payload is malformed. |
| **P3-F2** | Current Valuation Input Authority Foundation | **COMPLETE** | Generic completed-session, DNSE current-price, current-common-share, corporate-action invalidation, resolver, and coverage-scanner contracts are established. This does not widen DNSE/share instance authority or calculate a new valuation; P3-F retains formula ownership and P3-G remains reserved for future scenario/relative-valuation research. |
| **P3-F3** | Operational Current Valuation Input Scale-Out | **PARTIAL** | Operational materialization through generic P3-F2 contracts over the 11-issuer cohort at session 2026-08-19. Price authority scales 1→11 (`PRICE_READY = 11`, `PRICE_BLOCKED = 0`, `ADJUSTED_RETROSPECTIVE`, `NOT_PROMOTED`). Share coverage fails closed across the cohort (0/11 `SHARE_READY`, 11/11 `SHARE_BLOCKED`) due to lack of verified official share evidence through 2026-08-19. Zero valuation multiples activated for this session. |
| **P3-F4** | Generic Current Share Authority Root-Cause & Enablement | **COMPLETE** | A generic effective-dated current-common-share timeline is now the P3-F2 share qualification boundary. It preserves identity distinctions, forbids synthetic continuity, blocks unresolved share-changing actions, and fails closed on conflicts. Existing authority cannot improve the cohort without promotion: 0/11 share-ready and 0/11 both-ready remain correct. One future provider-metadata candidate is recorded `AUTHORITY=NOT_PROMOTED`; P3-G is not started. |
| **P3-F5** | Current Share Source Promotion & Allowed-Use Review | **COMPLETE** | Review-only evidence packet confirms 1,682 retained positive `VCI.overview.issue_share` values but no provider semantic/effective-date documentation. At session 2026-08-19, retained observations are 2026-08-14; SSI and VCB fail closed on unresolved share-event freshness. The outcome is `MORE_EVIDENCE_REQUIRED` and `PROVIDER_PROXY_USE_ONLY`; `ISSUED_SHARES` remains `AUTHORITY_NOT_PROMOTED_PENDING_OWNER_DECISION`, not a common-outstanding or market-cap authority. P3-G is not started. |
| **P3-F6** | MVA Provider-Share Proxy Policy & Shadow Valuation Activation | **COMPLETE** | Owner-approved `MVA_PROVIDER_ISSUED_SHARE_PROXY_USE` creates a separate `PROVIDER_REPORTED_ISSUED_SHARES_PROXY` lane under the mandatory MVA envelope. The generic policy keeps `ISSUED_SHARES` and `NOT_PROMOTED` source authority visible, blocks unresolved corporate-action states, and labels stale observations `DERIVED_PROXY` / `MVA_SHADOW`. It reuses P3-F formulas without widening P3-F2/P3-F authority: authoritative coverage remains 0/11 share-ready and 0/11 both-ready; 9/11 cohort proxy market-cap inputs are available, while SSI/VCB remain blocked. P3-G is not started. |
| **P3-F9** | Exact-Session MVA Market Snapshot Materialization | **PARTIAL** | The generic DNSE-only immutable shadow snapshot replaces direct dependence on the stale runtime session for MVA freshness. A controlled live window retains 6 exact-current / complete-20-session candidates out of 1,683 and proves `resolved_completed_session = retained_snapshot_session = MVA_bundle_session = 2026-08-20`; the other 1,677 are explicit missing/unattempted records. |
| **P3-F9B** | Market-Wide Exact-Session Snapshot Scale-Out | **COMPLETE** | Generic DNSE exact-session materialization scaled over full 1,683 canonical candidate universe for session 2026-08-20 (`resolved_session == retained_snapshot_session == mva_bundle_session == 2026-08-20`). 843 exact-session observations retained (50.09%), 667 session missing, 173 provider rejected, 0 transport failures, 0 unattempted without explicit disposition. Refreshed 20-session empirical active cohort: 523 members (1,160 excluded for incomplete/missing session coverage). Breadth completely reconciles (223 advancing, 187 declining, 113 unchanged, 0 missing over 523 denominator, advance ratio 0.4264). `MVA_POST_CLOSE_MARKET_WIDE_SESSION_READY = YES`. Zero runtime DB mutations; zero ticker-specific branches. Artifact: `operations-review/p3f9b-market-wide-exact-session-scaleout-20260820/p3f9b_market_wide_exact_session_scaleout_artifact.json`. |
| **P3-F10** | Generic Fundamental Evidence Scale-Out | **PARTIAL** | Full current empirical-active cohort pass is deterministic and generic: 520/523 members have retained raw observations and canonical mappings, while the existing 11-issuer approved-evidence cohort remains the only qualified lane. 509 mapped provider observations fail closed on scope/currency/scale evidence; no authority widening or document campaign occurred. The next generic blocker is approved evidence acquisition carrying those dimensions. |
| **P3-F11** | Generic Official Financial Filing Evidence & Statement-Metadata Qualification Foundation | **PARTIAL** | A fail-closed, hash-bound retained-filing metadata envelope now requires explicit evidence spans for period, periodicity, scope, currency, and scale. Corporate, bank, and securities pilots qualify document metadata; an unsupported-route case blocks. Metadata creates no provider observation or canonical fact: exact value-level official citation and provider matching remain mandatory. |
| **P3-F12** | Generic Value-Level Financial Evidence Qualification Foundation | **COMPLETE** | An offline, exact-only value-evidence projection binds retained official value spans to retained provider observations. It proves three cross-sector total-assets candidates through existing FY/Q4 balance-sheet identity rules and explicit document units; malformed values block. No store mutation, authority expansion, or 509-instrument acquisition occurred. |
| **P3-F14** | Generic Official Financial Source Discovery & Registry Expansion | **PARTIAL** | A closed-world discovery pass derives the 510 P3-F13 route-gap issuers and gives each one deterministic disposition. No retained issuer-domain ownership or exchange-detail signal exists for this target set, so zero candidates become approved routes and the registry remains unchanged. |
| **P3-F13** | Generic Official Financial Evidence Operational Scale-Out | **COMPLETE** | Applied P3-F11 metadata qualification and P3-F12 value qualification across the 512 blocked cohort members. 2 retained issuers (PNJ, FPT) qualify document metadata and 8 canonical facts (total_assets, total_liabilities, shareholders_equity, cash_and_equivalents) via exact provider reconciliation. 510 instruments finish with explicit `NO_APPROVED_ROUTE_FOUND` (0 unattempted without disposition). Financial panel expands 11→13 issuers and 130→138 qualified facts; P3-B research readiness expands to 13 `PARTIAL` issuers (510 `BLOCKED`). Scaleout gate: `OFFICIAL_FINANCIAL_EVIDENCE_SCALEOUT_PARTIAL`. Zero database mutations; zero ticker-specific branches. Artifact: `operations-review/p3f13-official-financial-evidence-scaleout-20260820/p3f13_official_financial_evidence_scaleout_artifact.json`. |
| **Official Financial Filings & Canonical History Scale-Out V1** | HNX retained-RSS bulk filing reuse | **OUTCOME C / DOCUMENTS RETAINED, CANONICAL BLOCKED** | The 100-item approved HNX disclosure RSS exposes 11 H1-2026 filing disclosures. Eight first-party PDFs are hash-retained; no detail page states a ticker and no PDF yields an exact cited value line, so canonical facts and downstream readiness remain unchanged. |
| **HNX Official Filing Evidence Binding & Extraction V1** | Retained HNX parent-detail to PDF identity replay | **OUTCOME C / OFFICIAL_PARENT_BINDING_UNPROVEN** | All eight retained statement PDFs have exact one-to-one HNX parent attachment links, but the retained parent disclosure blocks provide no ticker. Filename/page-chrome clues are excluded; identity-dependent text/OCR extraction and canonicalization remain terminally blocked without a duplicate route audit. |
| **HNX Multi-Attachment Binding & Citable Extraction V1** | Attachment-level retained HNX parent topology replay | **OUTCOME C / MULTI_ATTACHMENT_BINDING_UNPROVEN** | Eight parents expose 27 ordered first-party attachments (16 statement candidates, ten explanatory letters, one other). Scope is attachment-level only. No parent ticker exists; the eight retained PDFs are image-only and no operational governed OCR executor exists, while the other 19 attachment bytes are unretained. No canonical fact or authority change. |
| **HNX Official Issuer-Profile Multi-Gate Data Unlock V1** | First-party identity/profile/share/event contract | **OUTCOME C / HNX_EVENT_AND_IDENTITY_ONLY** | Three retained profiles prove HNX profile labels, ticker identity, and 17 explicit GDKHQ dates. KLLH/KLNY are separate (one equality and one listed-issuer inequality); UPCoM KLĐKGD is separate. KLLH is current exchange-reported circulating-share data only, not common-outstanding authority. The autocomplete list is capped/non-enumerable, existing PIT price cohorts do not overlap, and no retained filing parent gains exact ticker binding. |
| **Daily Research Product** | MVA research, AI briefs & prospective learning | **COMPLETE LOCALLY** | 523 exact-session shadow research records, transparent 25-name attention queue, evidence-bound AI briefs, and one immutable prospective snapshot are available only for current descriptive research. |
| **Persistent Research Dossier & Thesis Change Detection V1** | Daily evidence-aware research memory | **COMPLETE** | Immutable per-ticker state versions retain deterministic research identity, tiered evidence paths, thesis/counter-thesis hashes, questions, data gaps and warnings. A comparison emits explicit change categories and a transparent human follow-up queue; it never evaluates thesis truth or emits recommendations. Initial 2026-08-20 coverage is 523 `NEW_RESEARCH_STATE`; deterministic replay is 523 `NO_MATERIAL_CHANGE`. |
| **Research Question Resolution & Evidence Tasking V1** | Persistent question and gap workflow | **COMPLETE** | Immutable task states deduplicate dossier questions/gaps, preserve dossier/evidence/thesis lineage, distinguish qualified from descriptive resolution, and defer known terminal authority routes without retry loops. Baseline: 1,046 tasks across 523 dossiers (523 `OPEN`, 523 `DEFERRED_NO_CURRENT_EVIDENCE_ROUTE`); 25 transparent human follow-up tasks preserve AI-queue membership. |
| **Human Research Review Pack V1** | Owner-facing daily review artifact | **COMPLETE** | A deterministic structured pack carries all dossier/task evidence lineage; the concise rendering retains the full 25-name queue, separates FACT/INFERENCE/DATA_GAP/QUESTION, aggregates repeated known blockers, and exposes unpopulated human-only annotation fields. It cannot alter queue membership, task status, authority, or immutable research state. |
| **Owner Research Journal & Human Feedback Overlay V1** | Human workflow overlay | **COMPLETE** | Append-only owner events and a latest-state projection retain human review status, notes, follow-up/evidence requests, and an owner-only priority override. The overlay is separately keyed to the review-pack/dossier/task identities and cannot mutate system research or become evidence, task-resolution, recommendation, or authority state. |
| **Sector-Relative Research Context V1** | Qualified-archetype descriptive comparisons | **COMPLETE** | Same-session technical relative context uses only retained qualified entity-class cohorts of at least five empirical-active members. It preserves shadow/proxy tiers, cohort identity, member count, metric lineage, and missing reasons; no broad sector/industry inference, ranking, fundamental cross-metric analytics, valuation, or authority promotion occurs. |
| **Evidence-Bound Expectations & Scenario Research V1** | Retained-evidence scenario structure | **COMPLETE** | Immutable Bear/Base/Bull scenario objects organize existing facts, inferences, data gaps, thesis/counter-thesis and tasks. Explicit external/market-implied expectations, catalyst evidence, probabilities, targets, expected returns, recommendation authority, and scenario grading remain unavailable unless separately evidenced and contracted. |
| **Strategy Research Eligibility Engine V1** | Use-case-level research eligibility | **COMPLETE** | A versioned registry maps retained feature/evidence states to independent lens eligibility. It reports requirements, observed state, authority ceiling and blockers per ticker without creating signals, recommendations, rankings, execution, or performance claims. |
| **Evidence-Aware Research Screener V1** | Research discovery query contract | **COMPLETE** | Validated composable predicates filter retained research fields, authority and lens states with deterministic identities and explainable match/exclusion lineage. It is discovery-only: no scoring, ranking, recommendation, target, probability, or universe authority is created. |
| **Evidence-Bound Classification & Relative Context Scale-Out V1** | Provider-descriptive industry relative context | **COMPLETE** | Retained VCI industry metadata scales source-local same-session technical context to 486 lower-authority records while preserving the original 27 qualified records and filters. The next meaningful product gap is evidence-backed catalysts/events, not renewed classification qualification. |
| **Catalyst & Event Research Context V1** | Evidence-backed issuer-event research | **COMPLETE** | A versioned event context preserves event facts separately from linked, bounded catalyst interpretations. Retained coverage is one completed official HPG corporate-action event and no review-pack event; broader useful coverage requires a governed generic issuer-event feed, not manual URL work. |
| **Market-Wide Current Corporate Intelligence & Catalyst V1** | Current retained-evidence corporate facts/events and daily-session integration | **COMPLETE LOCALLY / PARTIAL EVIDENCE** | One 1,683-record artifact materializes three retained official corporate-action chains with per-event authority/freshness/lifecycle and explicit neutral catalyst descriptors. It binds HPG current execution, VNM historical completion, and VCB approval without execution proof into scenario, product, Consumer, session, and future identity-freeze surfaces. Market-wide ownership/governance-change coverage and a generic issuer-event feed remain unavailable; no source authority or price/PIT/recommendation capability changes. |
| **Polymorphic Current Strategy Classification V1** | Independent current strategy-fit requirements and multi-strategy research surface | **COMPLETE LOCALLY / CURRENT RESEARCH ONLY** | The 1,683-record materialization evaluates seven versioned strategy definitions independently from tactical timing and scenario probability. Technical, fundamental, event, and strict-value requirements expose feature-level authority and blockers. Value remains blocked without authoritative valuation; Event Driven is source-evidence-limited. Product V2, Consumer, session lineage, and an additive prospective identity snapshot are integrated; no score, best strategy, recommendation, sizing, execution, backtest, or authority promotion is created. |
| **Governed Generic Issuer Disclosure / Event Feed V1** | Existing-route qualification | **TERMINAL DEFERRED** | Existing `news_sync` RSS is operational publisher-news collection, not an approved generic issuer-disclosure/event feed. Its retained export is stale, its feed contract has no event lifecycle/revision or raw payload retention, and a live 60-item probe produced zero accepted issuer mappings. A future event-feed effort requires owner approval for one provider/source contract. |
| **Evidence-Aware Candidate Comparison V1** | Evidence-bound shortlist research view | **COMPLETE** | A deterministic comparison matrix consumes explicit shortlist requests and existing evidence identities. It exposes same-session technical facts, authority-labelled lens availability, scenario/task evidence, and non-comparability instead of producing a composite opportunity score or recommendation. |
| **Market Regime & Breadth Research Context V1** | Contemporaneous empirical-cohort participation context | **COMPLETE** | Fixed transparent rules classify only the retained exact-session 523-record empirical/shadow cross-section. The artifact supplies trend/momentum breadth, contemporaneous volatility distribution, provider-relative-volume availability, screener reconciliation, Review Pack overlay, and comparison context; it is not a bull/bear call, forecast, timing model, or authority promotion. |
| **Evidence-Aware Downside & Uncertainty Research Context V1 / V2** | Versioned research-risk context | **COMPLETE** | Restored V1 retains only the four original technical predicates (378/523 adverse); V2 carries price-structure downside context in a separate domain. Prospective attribution consumes explicit `downside:*_V1` and `price_structure:*` keys from the successor only, preventing semantic conflation. No score, rank, probability, sizing, or recommendation output. |
| **Price Structure & Breakout Research Context V1** | Current-descriptive technical structure | **COMPLETE** | A deterministic close-only prior-19-session overlay covers all 523 exact-session records, with explicit support/resistance candidates, breakout/breakdown rule states, range state, existing trend state, and lower-authority relative-volume proxy context. Its downside-related states are consumed only as a separate V2 price-structure domain, never as V1 technical-downside membership. Retained high/low scale is incompatible with close and is deliberately unused; 50-session structure remains unavailable. No signal, forecast, PIT/raw-as-traded, backtest, liquidity, or recommendation authority is created. |
| **Evidence-Aware Research Setup Classification V1** | Multi-label setup research context | **COMPLETE** | A small versioned registry combines only existing same-session facts into independently qualified research contexts. It preserves `NOT_PRESENT` versus `UNAVAILABLE`, assigns provider-descriptive relative strength only `QUALIFIED_LOWER_AUTHORITY`, attaches Market Context without using it as a gate, and supplies daily/Review Pack/Scenario/Downside overlays plus Screener and Candidate Comparison sections. It freezes setup-at-T identities for later prospective joins and emits neither score, strategy edge, forecast, rank, nor recommendation. |
| **Prospective Research Context Extension V1 successor** | Corrected supplemental frozen T-state for later attribution | **COMPLETE** | The original 382-member extension is retained immutable but excluded from attribution. A lineage-bound successor supplements, never rewrites, the original snapshot using restored V1 four-predicate downside state plus separate V2 price structure. It is sealed before future observation, rejects non-T sources, and is the only adapter-accepted grouping input for a later strictly-future descriptive join. No outcome, hit-rate, alpha, probability, performance, recommendation, or authority promotion occurs. |
| **Capability-First Data Foundation Rebaseline V1 Phase 1** | Canonical semantic-identity capability registry + explicit price representation contract | **COMPLETE LOCALLY** | `market_capability_taxonomy.py` and `price_representation_contract.py` establish the capability-first default sequence (`AGENTS.md`) and Invariant 6 (`docs/STATE.md`): a semantic field may have zero, one, or several qualified sources, overlap is useful but never mandatory, and K-VND→VND unit representation is independent of RAW_AS_TRADED/PIT price-basis authority. Delegates to, and does not modify, every existing provider-scoped registry. No authority promoted; `authority_effect: "NONE"` throughout. Phase 2 (unified EOD collector) is explicitly out of scope for this phase. |
| **Capability-First Market Evidence & Research Consumer V1** | Retained EOD collector, FHSC capability expansion, canonical integration, and research-consumer activation | **COMPLETE LOCALLY** | The upstream registry/collector/FHSC/canonical foundation is checkpointed at `e9ac939fb9612440347a7335f7db90d6ecd3951e`. `market_analysis_artifact.py` consumes provenance-bound canonical observations with per-use usability, without provider-parity prerequisites or synthetic blending. No RAW_AS_TRADED, PIT, liquidity/sizing, valuation, or recommendation authority changes. Next bounded milestone: `CAPABILITY_FIRST_DAILY_RESEARCH_MATERIALIZATION_V1`. |

---

## Global Roadmap Rebaseline — Active Gates

| Milestone | Scope | State | Exit and boundary |
|---|---|---|---|
| **Evidence-Gated Research Decision Workflow V1** | Deterministic reusable per-instrument decision-support packet over retained dated research artifacts | **READY FOR REVIEW LOCALLY** | `evidence_gated_research_decision_workflow.py` and `tools/run_evidence_gated_research_decision_workflow.py` produce 523 packets for the 2026-08-20 empirical-active shadow cohort. The packet joins existing eligibility, setup, scenario, event, downside, market, P3-F13 official evidence, and MVA-proxy artifacts without creating analytical engines or source authority. Official evidence presence is sector-neutral: 13 `ELIGIBLE`, 510 `BLOCKED`; independent fundamental model readiness is 13 `PARTIAL`, 510 `BLOCKED`; VCB/SSI corporate-model metric `NOT_APPLICABLE` states do not erase their official evidence. It remains decision support only: all packets need human decision; liquidity/PIT are blocked; current-share/authoritative valuation are blocked; MVA proxy is labelled non-authoritative; no portfolio, sizing, execution, probability, target, or recommendation output. No subsequent milestone is authorized by this local result. |
| **Evidence-Bound AI Research & Human Review V1** | Deterministic AI-input, untrusted structured-draft validator, and provenance-bearing human review | **READY FOR REVIEW LOCALLY** | `evidence_bound_ai_research_human_review.py` and its runner produce 523 content-addressed inputs from the dated 2026-08-20 workflow cohort without a live model call. A strict validator requires claim/evidence/authority lineage and rejects invented FACTs/numbers, authority escalation, missing counter-evidence, proxy valuation promotion, changed gates, and recommendation/target/probability/sizing/execution text. Human review is explicit; human edits are append-only provenance and internal-research approval conveys no investment/execution authority. No subsequent milestone is authorized by this local result. |

| Order | Gate | Current state and boundary |
|---:|---|---|
| 1 | Current-share source-authority decision | P3-F5 review remains binding: `VCI.overview.issue_share` is `ISSUED_SHARES` and `AUTHORITY_NOT_PROMOTED_PENDING_OWNER_DECISION`. P3-F6’s owner-approved MVA proxy policy permits only a labelled `DERIVED_PROXY` / `MVA_SHADOW` observation; it does not promote the source or authorize current-common, authoritative market-cap, or P3-F valuation use. |
| 2 | Minimum Viable Analysis shadow operating mode | `MINIMUM_VIABLE_ANALYSIS_SHADOW` may emit deterministic current descriptive artifacts while PIT/liquidity/sizing remain blocked. Mandatory envelope: `is_actionable_for_execution=false`; `pit_backtest_eligible=false`; `liquidity_sizing_authority=BLOCKED`; `valuation_scope=CURRENT_DESCRIPTIVE_ONLY`. |
| 3 | Canonical-universe / empirical-active shadow denominator | `ACTIVE_UNIVERSE` remains unknown. Any empirical cohort is derived shadow-only and must carry as-of date, lookback/window, source completeness, inclusion rule, and deterministic identity; it is never a hardcoded canonical denominator. |
| 4 | Volume/traded-value semantic authority | Market-composition authority is insufficient; liquidity, turnover, risk sizing, and execution sizing remain blocked. `DNSE_TRADES_AND_LIQUIDITY_BASIS_QUALIFICATION_V1` (2026-08-23) newly unlocks live current-session numeric board composition via `trades_latest`, but proved historical multi-session decomposition activity-dependent and still infeasible within a bounded call budget for actively-traded names; the gate itself does not open. |
| 5 | Current relative valuation/scenario | P3-G remains the future relative-valuation/scenario milestone. Preserve sector-aware applicability: industrial CapEx/FCFF/EV semantics do not gate banks/securities; their future contracts may use appropriate P/B-ROE/residual-income-style semantics. |
| 6 | Corporate-action + historical RAW_AS_TRADED/PIT | Current DNSE price supports bounded current descriptive use; historical `RAW_AS_TRADED`/PIT remains unpromoted pending qualified corporate-action/event-window authority. |
| 7 | Historical universe/entity PIT | Point-in-time listing inclusion and entity classification authority remains required. |
| 8 | Liquidity/risk/sizing | Blocked until volume/traded-value and the required PIT authorities qualify. |
| 9 | PIT backtesting | Blocked until historical price, corporate-action, universe/entity, and liquidity prerequisites qualify. |
| 10 | Deeper valuation/financial/sector/macro expansion | Subsequent bounded work only. `interbank_on_rate`, `sbv_net_injection_20d`, and `vn30f1m_basis` are source-qualification backlog fields, not current authority. |

`FULL_DECISION_SUPPORT_READY` requires every gate needed for canonical universe, current shares, volume/traded value, historical price/corporate-action PIT, historical universe/entity PIT, liquidity/risk/sizing, and PIT backtesting. It is not satisfied by MVA.

---

## 3. Active Critical Path Sequence

Execution focus strictly follows the ordered critical path:

1. **P0-RECOVERY** — Canonical Trades Materialization: **CLOSED**.
2. **P0-C.1 / P0-C.2** — Canonical Universe Foundation & Hierarchy: **COMPLETE LOCALLY**.
3. **P0-A.2** — Corporate Action Official Evidence Ledger: **COMPLETE LOCALLY**.
4. **P0-A.3E** — Prospective Price Basis: Part A **COMPLETE**; Part B **BLOCKED FAIL-CLOSED**.
5. **P0-B.2D / P0-B** — Volume/Liquidity Scoped Review: **CLOSED (NO_AUTHORITY_PROMOTION)**.
6. **P0-C.3** — Field-Level Freshness & PIT Retrofit: **COMPLETE LOCALLY**.
7. **First Market-Wide Deterministic Analysis/Research Artifact** — **COMPLETE LOCALLY** (`market_analysis_artifact.py`).
8. **Phase 1 Feature Store Normalization & Multi-Session Export** — **COMPLETE LOCALLY** (`cross_sectional_export.py`, `bb0cafa4417471b0`).
9. **Phase 2-A Multi-Period Financial Fact Panel & Sector Applicability** — **COMPLETE LOCALLY** (`multi_period_financial_panel.py`, `33cfa0a4e5ee114e`).
10. **Phase 2-B Generic Financial Canonicalization & Retained Scale-Out** — **COMPLETE LOCALLY** (`generic_financial_canonicalizer.py`, `256f374c08df327b`).
11. **Phase 2-C Official Financial Evidence Scale-Out / First Corporate Acquisition Wave** — **COMPLETE LOCALLY** (`tools/run_p2c_corporate_evidence_scale_out.py`, `f9ab8e98d2e691d8`).
12. **Phase 2-D2/D2C Bounded Official Source Registry Promotion & Host Narrowing (GAS + VRE)** — **COMPLETE LOCALLY** (`config/official_source_registry.json`, `official_source_registry.py`).
13. **Phase 2-C2 Bounded Financial Evidence Onboarding (GAS + VRE)** — **SUPERSEDED** (Manual fact lineage replaced by P2-C2C).
14. **Phase 2-C2C Governed Financial Evidence Lineage Correction (GAS + VRE)** — **COMPLETE LOCALLY** (`tools/run_p2c2_corporate_evidence_onboarding.py`, `p2c2_governed_onboarding_report.json`).
15. **Phase 2-D Generic Financial Statement Template Recognition & Extraction Contract** — **COMPLETE LOCALLY** (`financial_statement_template_recognizer.py`, `p2d_generic_onboarding_report.json`).
16. **Phase 2-E Evidence-Backed Entity Classification Scale-Out Foundation** — **COMPLETE LOCALLY** (`evidence_backed_entity_classifier.py`, `p2e_entity_classification_artifact.json`).
17. **Phase 2-E3 Bounded Current-State Entity Classification Authority Promotion** — **COMPLETE LOCALLY** (`config/promoted_entity_classifications.json`, `p2e3_entity_classification_promotion_artifact.json`).
18. **Phase 2-F1 Sector Financial Taxonomy & Disclosure Parsing Foundation** — **COMPLETE LOCALLY** (`sector_financial_taxonomy.py`, `financial_disclosure_recognizer.py`, `p2f1_sector_financial_taxonomy_artifact.json`).
19. **Phase 2-F2 Sector Financial Authority Promotion Review** — **COMPLETE LOCALLY** (`P2F2_PROMOTION_RECOMMENDED`).
20. **Phase 2-F3 Bounded Generic Sector Extraction Authority Promotion** — **COMPLETE LOCALLY** (`config/promoted_sector_extractions.json`, `p2f3_sector_extraction_promotion_artifact.json`).
21. **Phase 2 Closeout & Market-Wide Financial Fact Panel Integration** — **COMPLETE LOCALLY** (`p2_closeout_financial_panel_artifact.json`, `p2_closeout_financial_panel:46335e0b527ed39cbbcc8082508c85e86892f83137bf205f416e9d0bbbbc8eed`).
22. **Phase 3-A Bounded Price Adjustment & Dividend Ex-Date Event Window Qualification** — **BLOCKED FAIL-CLOSED (`P3A_BLOCKED_PENDING_QUALIFIED_EX_DATE`)**.
23. **Phase 3-B Sector-Aware Fundamental Quality & Research Readiness** — **COMPLETE LOCALLY** (`fundamental_research_readiness.py`, `tools/run_p3b_fundamental_research_readiness.py`, `operations-review/p3b-fundamental-research-readiness-20260820/`). This does not relax P3-A or any price/liquidity gate.
24. **Phase 3-C Multi-Period Comparative Financial Evidence Scale-Out** — **PARTIAL LOCALLY** (`p3c_comparative_financial_evidence.py`, `tools/run_p3c_comparative_financial_evidence.py`, `operations-review/p3c-comparative-financial-evidence-20260820/`).
25. **Phase 3-D Residual Comparative Financial Evidence Scale-Out & Gap Reconciliation** — **PARTIAL LOCALLY** (`p3d_residual_comparative_financial_evidence.py`, `tools/run_p3d_residual_comparative_financial_evidence.py`, `operations-review/p3d-residual-comparative-financial-evidence-20260820/`). Next bounded fundamental gate: P3-E retained/approved annual evidence completion for the residual six revenue and seven total-assets gaps; VCB FY2023 requires a separately authorized source-authority promotion decision.
26. **Phase 3-E Fundamental Coverage Closeout & Valuation-Input Readiness Gate** — **COMPLETE LOCALLY** (`p3e_fundamental_coverage_closeout.py`, `valuation_input_readiness.py`, `tools/run_p3e_fundamental_coverage_closeout.py`, `operations-review/p3e-fundamental-coverage-closeout-20260820/`). Comparative financial evidence is closed. A future P3-F valuation/scenario capability requires separate market-price and dated-share authority and must not calculate valuation before that gate.
27. **Phase 3-F Current Market Valuation Basis Activation & Sector-Aware Valuation MVP** — **PARTIAL LOCALLY** (`p3f_current_market_valuation.py`, `tools/run_p3f_current_market_valuation.py`, `operations-review/p3f-current-market-valuation-20260820/`). HPG's frozen current-market snapshot is available only for descriptive multiples. No historical/PIT valuation, target price, ranking, recommendation, execution, or backtest is authorized.
28. **Phase 3-F2 Current Valuation Input Authority Foundation** — **COMPLETE LOCALLY** (`current_valuation_input_authority.py`, `tools/run_p3f2_current_valuation_input_authority.py`, `operations-review/p3f2-current-valuation-input-authority-20260820/`). The next operational use is data materialization under these contracts; P3-G is not redefined or consumed.
29. **Phase 3-F3 Operational Current Valuation Input Scale-Out** — **PARTIAL LOCALLY** (`tools/run_p3f3_operational_valuation_input_scaleout.py`, `operations-review/p3f3-operational-valuation-input-scaleout-20260820/`). Price observations materialized 1→11 PRICE_READY at session 2026-08-19; share coverage fails closed 11/11 SHARE_BLOCKED. Zero multiples activated.
30. **Phase 3-F4 Generic Current Share Authority Root-Cause & Enablement** — **COMPLETE LOCALLY** (`current_share_authority.py`, `tools/run_p3f4_generic_current_share_authority.py`, `operations-review/p3f4-generic-current-share-authority-20260820/`). The future owner gate is provider-source promotion review; it is not an authorization to promote or to start P3-G.
31. **Phase 3-F5 Current Share Source Promotion & Allowed-Use Review** — **COMPLETE LOCALLY** (`provider_share_promotion_review.py`, `tools/run_p3f5_current_share_promotion_review.py`, `operations-review/p3f5-current-share-promotion-review-20260820/`). Review evidence supports `MORE_EVIDENCE_REQUIRED` and a future owner-approved labelled proxy only; it does not promote `VCI.overview.issue_share`, enable market cap, or start P3-G.
32. **Phase 3-F6 MVA Provider-Share Proxy Policy & Shadow Valuation Activation** — **COMPLETE LOCALLY** (`mva_provider_share_proxy.py`, `tools/run_p3f6_mva_provider_share_proxy.py`, `operations-review/p3f6-mva-provider-share-proxy-20260820/`). The explicit owner-approved MVA-only proxy lane emits current descriptive proxy valuations while P3-F/P3-F2 authority remains unchanged; it neither promotes VCI nor starts P3-G.
33. **Phase 3-F7 MVA Daily Research Bundle & Shadow Active-Cohort V1** — **COMPLETE LOCALLY** (`mva_daily_research_bundle.py`, `tools/run_p3f7_mva_daily_research_bundle.py`, `operations-review/p3f7-mva-daily-research-bundle-20260820/`). `MVA_DAILY_RESEARCH_BUNDLE_READY = YES` for deterministic shadow research only; it creates neither canonical active-universe authority nor a P3-G ranking/scenario capability.
34. **Phase 3-F8 MVA Operational Daily Run & Research Quality Validation** — **COMPLETE LOCALLY** (`tools/run_p3f8_mva_operational_run.py`, `operations-review/p3f8-mva-operational-run-20260820/`). `MVA_OPERATIONALLY_USABLE` confirmed for shadow daily research.
35. **Phase 3-F9 Exact-Session MVA Market Snapshot Materialization** — **PARTIAL LOCALLY** (`tools/run_p3f9_exact_session_mva_materialization.py`, `operations-review/p3f9-exact-session-mva-materialization-20260820/`). The generic DNSE-only immutable shadow snapshot replaces direct dependence on a stale runtime session for MVA freshness. The controlled live window proves exact 2026-08-20 resolved/snapshot/bundle equality for 6 observed/current and 6 complete 20-session candidates of 1,683; the unattempted 1,677 are explicitly missing, not backfilled. Full marketwide transport remains the freshness blocker. No Dashboard/runtime database write occurred.
36. **Phase 3-F9B Market-Wide Exact-Session Snapshot Scale-Out** — **COMPLETE LOCALLY** (`tools/run_p3f9b_market_wide_exact_session_scaleout.py`, `operations-review/p3f9b-market-wide-exact-session-scaleout-20260820/`). `MVA_POST_CLOSE_MARKET_WIDE_SESSION_READY = YES`. Generic DNSE exact-session materialization scaled over the full 1,683 candidate universe for session 2026-08-20 (843 exact-session retained, 523 20-session empirical active members, 0 unattempted without explicit disposition). Zero runtime DB mutations; zero ticker-specific branches.
37. **Phase 3-F10 Generic Fundamental Evidence Scale-Out** — **PARTIAL LOCALLY** (`p3f10_fundamental_evidence_scaleout.py`, `operations-review/p3f10-generic-fundamental-evidence-scaleout-20260820/`).
38. **Phase 3-F11 Generic Official Financial Filing Evidence & Statement-Metadata Qualification Foundation** — **PARTIAL LOCALLY** (`official_financial_filing_evidence.py`, `operations-review/p3f11-official-financial-filing-evidence-20260820/`).
39. **Phase 3-F12 Generic Value-Level Financial Evidence Qualification Foundation** — **COMPLETE LOCALLY** (`official_financial_value_evidence.py`, `operations-review/p3f12-value-level-financial-evidence-20260820/`).
40. **Phase 3-F13 Generic Official Financial Evidence Operational Scale-Out** — **COMPLETE LOCALLY** (`p3f13_official_financial_evidence_scaleout.py`, `tools/run_p3f13_official_financial_evidence_scaleout.py`, `operations-review/p3f13-official-financial-evidence-scaleout-20260820/`). `OFFICIAL_FINANCIAL_EVIDENCE_SCALEOUT_PARTIAL` confirmed (13 issuers, 138 qualified facts; 510 issuers blocked on unapproved official source routes).
41. **Persistent Research Dossier & Thesis Change Detection V1** — **COMPLETE LOCALLY** (`persistent_research_dossier.py`, `run_persistent_research_dossier.py`, `operations-review/persistent-research-dossier-v1-20260820/`). This is a consumer-only daily research memory: it retains immutable state versions, emits transparent change categories and preserves the deterministic 25-name research queue. It does not promote evidence, decide thesis truth, or open execution/backtest authority.
42. **Research Question Resolution & Evidence Tasking V1** — **COMPLETE LOCALLY** (`research_question_tasking.py`, `run_research_question_tasking.py`, `operations-review/research-question-tasking-v1-20260820/`). This consumer-only task layer converts retained dossier questions/gaps into immutable, evidence-linked task states and a transparent human queue. It creates no web acquisition workflow, authority promotion, recommendation, or automated resolution.
43. **Human Research Review Pack V1** — **COMPLETE LOCALLY** (`human_research_review_pack.py`, `run_human_research_review_pack.py`, `operations-review/human-research-review-pack-v1-20260820/`). This consumer-only daily artifact compresses the task inventory into a complete 25-name owner review queue with structured lineage and shared-blocker aggregation. It exposes a human annotation contract but does not populate or persist owner decisions.
44. **Owner Research Journal & Human Feedback Overlay V1** — **COMPLETE LOCALLY** (`owner_research_journal.py`, `run_owner_research_journal.py`, `operations-review/owner-research-journal-v1-20260820/`). This append-only human layer records only explicit owner workflow state against immutable review-pack/dossier/task references. It can reorder the owner view but cannot alter deterministic research state, source authority, task resolution, or investment actions.
45. **Sector-Relative Research Context V1** — **COMPLETE LOCALLY** (`sector_relative_research_context.py`, `run_sector_relative_research_context.py`, `operations-review/sector-relative-research-context-v1-20260820/`). This consumer-only context layer supplies only same-session qualified entity-class-archetype technical comparisons and a Review Pack overlay. Unqualified or too-small groups, and all daily fundamental comparisons, remain explicit unavailable states.
46. **Evidence-Bound Expectations & Scenario Research V1** — **COMPLETE LOCALLY** (`expectations_scenario_research.py`, `run_expectations_scenario_research.py`, `operations-review/expectations-scenario-research-v1-20260820/`). This consumer-only overlay structures retained research into partial Bear/Base/Bull cases, task-linked invalidation review conditions, and explicit expectation/catalyst gaps. It does not forecast, score, or evaluate scenarios.
48. **Capability-First Market Evidence & Research Consumer V1** — **COMPLETE LOCALLY**. The registry, retained EOD collector, exact-session FHSC expansion, and canonical integration are checkpointed at `e9ac939fb9612440347a7335f7db90d6ecd3951e`; `market_analysis_artifact.py` now consumes the resulting provenance-bound canonical observations through their per-use usability projection. Provider parity remains unnecessary for research ingestion, and no RAW_AS_TRADED, PIT, liquidity/sizing, valuation, or recommendation authority is promoted.
49. **Capability-First Daily Research Materialization & Real EOD Vertical Slice V1** — **COMPLETE LOCALLY** (`tools/materialize_daily_market_research.py`, `tests/test_materialize_daily_market_research.py`, `operations-review/capability-first-real-eod-2026-08-21/`, `operations-review/daily-market-research-2026-08-21/`). Builds deterministic, immutable daily research artifact bundles (`market_research_artifact.json`, `canonical_integration.json`, `run_manifest.json`, `completion_record.json`) from retained EOD session packets using whole-run staging directory atomic promotion and `run-<materialization_identity>` directory addressing. Validated across real completed-session 2026-08-21 evidence over `HPG`, `VCB`, `SSI` with 21/21 HTTP 200 responses, 21/21 SHA-256 byte-verified raw payloads, and idempotent offline replay (`is_idempotent_replay=true`). All authority gates remain strictly `NONE` / unpromoted / blocked; zero runtime DB mutations. The operational follow-on `CAPABILITY_FIRST_EOD_1800_OPERATIONAL_SCHEDULING_V1` is complete locally (item 60).
50. **Capability-First Scenario Distribution V1** — **COMPLETE LOCALLY** (`tools/derive_capability_first_scenario_distribution.py`, `tests/test_derive_capability_first_scenario_distribution.py`). Deterministically distinguishes the real 12-case scenario denominator from the 524-record universe denominator. It creates no probability, target, recommendation, or ranking authority.
51. **Qualified Liquidity Inputs Reconciliation V1** — **COMPLETE LOCALLY / RECONCILIATION EVIDENCE ONLY** (`tools/derive_qualified_liquidity_inputs_reconciliation.py`, `tests/test_derive_qualified_liquidity_inputs_reconciliation.py`). Retained evidence confirms DNSE `v` = FHSC matched volume in 35/35 discriminating observations within the tested scope; canonical DNSE board labels are centralized while numeric board composition remains unavailable. `adv_turnover_input_eligible = false`; liquidity/sizing/portfolio/PIT/RAW_AS_TRADED authority remains blocked.
52. **Current Share Basis Capability Reconciliation V1** — **COMPLETE / BLOCKED_BY_EVIDENCE** (`current_share_basis_capability_reconciliation.py`, `tools/derive_current_share_basis_capability_reconciliation.py`, `tests/test_current_share_basis_capability_reconciliation.py`). The retained 1,683-row provider inventory contains 1,680 structurally usable, non-authoritative `issued_shares` proxies; current authoritative common-share coverage is 0/11 in the valuation corpus. Issued/listed/outstanding/period-end/weighted-average identities remain separate, SSI/VCB corporate-action cases remain blocked, and current market cap/P-E/P-B/P-S/EV plus historical/PIT/share authority remain blocked.
53. **Current Common Shares Official Evidence Acquisition V1** — **COMPLETE / NO_QUALIFYING_CURRENT_SHARE_EVIDENCE** (`current_common_shares_official_evidence_acquisition.py`, `tools/derive_current_common_shares_official_evidence_acquisition.py`, `tests/test_current_common_shares_official_evidence_acquisition.py`). The exact 11-symbol cohort remains 0/11 qualified through 2026-08-19: nine currentness-unresolved and SSI/VCB corporate-action execution-unresolved. HPG’s official 8,442,964,520 count is effective 2026-07-02 but covers only through 2026-07-30. Authoritative current valuation remains blocked; new finite evidence is required before broad acquisition can resume. The source-streaming truncation correction is complete-stream hash-verified.
54. **Provider-Reported Current Valuation Proxy V1** — **COMPLETE / NON-AUTHORITATIVE RESEARCH PROXY** (`provider_reported_current_valuation_proxy.py`, `tools/derive_provider_reported_current_valuation_proxy.py`, `tests/test_provider_reported_current_valuation_proxy.py`). Method `PROVIDER_ISSUED_SHARES_PROXY` emits only `NON_AUTHORITATIVE_RESEARCH_PROXY` outputs: 9 proxy market cap/P-B/P-S/EV-Sales, 8 P-E, and 0 EV/EBITDA across the 11-symbol cohort. SSI/VCB remain corporate-action blocked and authoritative valuation remains blocked. Broader price/financial-input coverage, not proxy expansion, is the next blocker.
55. **DNSE Trades and Liquidity Basis Qualification V1** — **COMPLETE LOCALLY / CURRENT-SESSION BOARD COMPOSITION UNLOCKED, HISTORICAL STILL BLOCKED** (`dnse_trades_liquidity_basis.py`, `tools/derive_dnse_trades_liquidity_basis.py`, `tests/test_dnse_trades_liquidity_basis.py`, `tests/test_derive_dnse_trades_liquidity_basis.py`, `operations-review/dnse-trades-liquidity-basis-v1-20260823/`). A bounded 23-call live probe of the already-registered `trades_latest`/`trades_history` endpoints (never the orphaned `dnse_trades_canonical_shadow.py`) proves `10 x G1 == OHLC v` exactly across 5 fresh cross-sector tickers and shows complete historical session reconstruction is activity-dependent (achievable for a low-activity name in 4 bounded pages, infeasible for HPG/VCB/SSI within the same bounded cap on two different sessions). Numeric board composition is newly live-readable for the current session only; historical/ADV/ADTV/sizing/execution/PIT remain blocked. No liquidity, sizing, or traded-value authority promoted.
56. **Current Evidence-Bound Scenario Engine V1** — **COMPLETE LOCALLY / RESEARCH_ONLY** (`current_evidence_bound_scenario.py`, `tools/run_current_evidence_bound_scenario.py`, `operations-review/current-evidence-bound-scenario-v1-20260824/`). Additive full-universe conditional Bear/Base/Bull envelopes port, rather than rewrite, the retained `expectations_scenario_research/v1` semantics. Probabilities are `UNKNOWN_UNCALIBRATED`; no target, expected return, ranking, recommendation, sizing, portfolio, execution, outcome, or calibration authority is emitted.
57. **Current Daily Decision Research Product V2** — **COMPLETE LOCALLY / HUMAN_REVIEW_RESEARCH_ONLY** (`current_daily_decision_research_product.py`, `tools/run_current_daily_decision_research_product.py`, `operations-review/current-daily-decision-research-product-v2-20260824/`). Integrates retained market, tactical, peer, fundamental, valuation, and conditional-scenario contracts into one deterministic JSON/Markdown human-review workflow. Existing entry actions remain non-actionable tactical research states; no ranking, recommendation, probability, target, sizing, portfolio, execution, or authority promotion is emitted.
58. **Daily Research Session Operations V1** — **COMPLETE LOCALLY / RETAINED_EVIDENCE_ONLY** (`daily_research_session_operations.py`, `tools/run_daily_research_session_operation.py`, `config/daily_research_session_input_registry.json`, `docs/RUNBOOK_DAILY_RESEARCH_SESSION.md`). One foreground workflow resolves exact artifact identities, rejects incompatible/superseded same-session mixes, rebuilds coherent downstream research/product artifacts, validates Consumer pass-through, seals the existing prospective contract, and retains an immutable manifest. It creates no scheduler, acquisition, authority promotion, outcome, calibration, or execution capability.
59. **Current Research Packet Dashboard Shadow Surfacing V1** — **COMPLETE LOCALLY / SHADOW OPT-IN** (`current_research_decision_packet_product.py`, `tools/run_current_research_decision_packet_dashboard.py`, `tests/test_current_research_decision_packet_product.py`). Surfaces retained `current_research_decision_packet/v1` through the existing daily decision research product and opt-in bundle attach. Default product/bundle paths stay unchanged; incorrect packet identity fails closed. Packet Bear/Base/Bull is not CONSERVATIVE/BASE/SPECULATIVE. Market-wide render 1,507/1,507, residual 0, malformed 0. No recommendation, probability, target, sizing, or default cutover.
60. **Capability-First EOD 18:00 Operational Scheduling V1** — **COMPLETE LOCALLY / SCHEDULER_READY** (`completed_market_session_gate.py`, `tools/run_capability_first_eod_operation.py`, `config/capability_first_eod_operation.json`, `tests/test_completed_market_session_gate.py`, `tests/test_run_capability_first_eod_operation.py`). One-shot foreground command: completed VN market session → completed-session gate → existing capability-first EOD collector → retained packet validation → existing immutable research materialization → operation disposition. 18:00 Asia/Ho_Chi_Minh is a safety floor. DNSE `working_dates` is working-date identity inside a forward calendar window only. READY is `EXACT_SESSION_OBSERVED_AFTER_SAFETY_FLOOR`, never `PROVIDER_CONFIRMED_COMPLETED`. No OS scheduler, poll, sleep, or daemon. A later milestone requires separate owner authorization.
61. **Governed Publication One-Command Completion V1** — **COMPLETE LOCALLY / ONE_COMMAND_PUBLICATION_READY** (`governed_publication_completion.py`, `tools/release_orchestrator.py --complete-publication`, `tests/test_governed_publication_completion.py`). The existing `all --live` orchestrator now owns Dashboard CI, Deploy Pages, and public-byte identity for the exact release SHA. No second orchestrator. Automatic GitHub triggers remain defense in depth.
62. **Investment Decision Workspace V1** — **COMPLETE LOCALLY / PRODUCT_CONVERGENCE_COMPLETE** (`investment_decision_workspace_projection.py`, `tools/run_investment_decision_workspace_projection.py`; Dashboard `investment-workspace.html`, `assets/js/investment-workspace.js`). Joins the retained `opportunity_context/v1` + `security_decision_context/v1` pair into a seven-section per-ticker Dashboard decision workspace with filters, portfolio-fit-separate-from-security-stance, mixed-session freshness, and a bounded T0 export. Found and fixed a real market-cap-only valuation mislabel in `current_research_valuation_context.py::attach_peer_relative` (440/1,699 tickers before, 0 after, verified on real 2026-08-28 data — see `docs/DECISIONS.md`). Closes this session's product-convergence chain; no next milestone queued. **Same-day bounded decision-quality corrective pass** (`security_decision_context.py`, `tactical_confirmation_invalidation_boundaries.py`): fixed confirmation-trigger semantics, fundamental/tactical missingness routing, deterministic WHY/counter-thesis/counterbalancing-context coverage, and boundary text/metric consistency, per a read-only real-corpus review; `research_stance_distribution` on the same real 1,699-ticker corpus moved INITIATE 68→52, HIGH_RISK 47→30, INSUFFICIENT_EVIDENCE 757→188, WAIT_FOR_CONFIRMATION 300→902 (ACCUMULATE/AVOID unchanged at 183/344) — see `docs/DECISIONS.md` (2026-08-31, "Decision-Quality Corrective Pass"). Still `PRODUCT_CONVERGENCE_COMPLETE`; no new milestone.
63. **Core Fundamental Valuation And Peer Context V1** — **COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE** (`5e58d79f69810d6800d1f58244c421acb0e4230f`, closeout `14cc93ccc5cec97c1de865b69eba958f5f18ee7a`). Product-critical Current Research fundamental and valuation/peer/history context. Completion record is not reopened.
64. **Tactical Market Structure And Breakout V3** — **COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE** (`01ffdd57900f0a777a59fbed0365d1a53c3a25c5`, `operations-review/tactical-market-structure-breakout-v3-20260902/`). Product-critical market-structure and breakout measurements (`technical_structure_context.py` extended to v2, `market_structure_breakout_product_projection.py`). Emits confirmed swing fractal structure, BOS, CHoCH, confirmed pivot levels, breakout states, triggers, and invalidations across 1,683 tickers (942 eligible).
65. **Integrated Investment Decision Product V1** — **COMPLETE / FULL_UNIVERSE_INTEGRATED_PRODUCT_ACTIVE** (`integrated_investment_decision_product.py`, `operations-review/integrated-investment-decision-terminal-correction-20260902/`). Unifies Fundamental V2, Valuation & Peer Context, Tactical V3, Relative Volume & Participation, and Market/Sector context into a deterministic 9-posture decision engine without universal scores, target prices, or probability claims. Fully integrated into canonical post-close pipeline and `export_ai_bundle.py` with automatic same-session artifact resolution and fail-closed session identity validation. Terminal corrective pass resolved event-vs-lagged-structure prioritization (PNJ breakout onset confirmed without distribution contradiction; QNS downtrend breakout requires confirmation) and multi-axis participation/market regime contradiction filtering. Test suite 21/21 passing.
66. **Daily Integrated Decision Brief And Prospective Feedback V1** — **COMPLETE / COHERENT_PARTIAL_BY_RETAINED_EVIDENCE** (`90e3260a5596fa02c210fcc13bb3d1b28d8b329f`, `operations-review/daily-integrated-decision-brief-prospective-feedback-v1-20260902/`). The final daily research product: new `daily_integrated_decision_brief/v1` joins the integrated decision product, `next_session_decision_brief/v2` (new `posture_transition` section), sector leadership, and opportunity prioritization into one compact artifact (market/sector summary, 7 deterministic opportunity sets, canonical 11-ticker watchlist, decision transitions, what-changed-today, risk summary, prospective feedback status), wired automatically into `stocklookup.py daily` and the AI handoff manifest. `prospective_decision_outcome_measurement/v2` adds the T10 horizon and a 12-label feedback taxonomy; new `integrated_decision_prospective_feedback.py` bridges it to the integrated decision product's own already-durable per-session records without a second backtest engine. Phase 0 review found and fixed two real production defects in the preceding integration (`market_regime`/`sector_leadership` reading a key shape the real artifact never emits; `technical_structure_artifact`/`relative_volume_artifact` fed structurally incompatible legacy artifacts, making every posture come out `INSUFFICIENT_CURRENT_RESEARCH` in production) — see `docs/STATE.md` and `docs/DECISIONS.md` for full detail. A third related gap (Financial V2 has no canonical daily-materialization path in this pipeline) was found and deliberately left unfixed, flagged as follow-up. Real 2026-08-25→2026-08-28 replay: 1,683 universe, zero silent drops, posture distribution matching the established Phase-0 baseline exactly. 126 new/updated tests, all green; 0 new full-suite failures. No successor is queued.
67. **Corporate Event Canonical Data Refresh And Ledger Consolidation V1** — **COMPLETE / PARTIAL_BY_EVIDENCE** (`99d9bf4dcd55a8ab16d7c979f7ceb9d99542eee7`, `operations-review/corporate-event-canonical-data-refresh-ledger-consolidation-v1-20260905/`). Continuation of `CORPORATE_INTELLIGENCE_CATALYST_EVENT_RISK_DECISION_INTEGRATION_V1` (2026-09-05). Root-caused the frozen 2026-08-21 official corporate-event evidence with registry-level proof (`config/daily_research_session_input_registry.json` records the identical `current_official_event_context` artifact hash across every real Daily session 2026-08-24 through 2026-09-04): its only real caller is a one-off manual tool run once 2026-08-24 over one-off HNX/HOSE snapshots (re-acquisition out of this milestone's bounded no-new-provider/no-broad-scrape scope), and `canonical_post_close_pipeline._corporate_event_context()` called it with today's session, guaranteeing a session mismatch and a silent daily fallback to stale evidence, never activating `supplemental_events`. Fixed the code-path defects: session-binding to the evidence's own session (mirroring the already-correct `current_corporate_intelligence_axis` pattern), `supplemental_events` activation, and a latest-dated-snapshot resolver in `daily_session_level2_package.session_artifact_paths()`. The two raw corporate-action ledgers were confirmed genuinely distinct PIT/price-adjustment-authority modules, not the current-research event boundary — no merge. Closed 3 of 5 real "bonus issue" taxonomy spellings that had not yet converged, additive-only. Real 2026-09-04/2026-08-25 replay is byte-identical to the prior milestone's own measurement (1,507 universe, 1,103 tickers with evidence, 4,453 deduplicated events, 0 conflicted, 0 active-catalyst/risk tickers) — only the daily materialization now actually succeeds. 246 tests pass across the 7 directly-relevant suites, 0 regressions. No push, no new acquisition; the raw HNX/HOSE acquisition gap remains open, honestly reported. No successor is queued.
68. **Official Corporate Event Incremental Acquisition And Freshness V1** — **COMPLETE / PARTIAL_BY_EVIDENCE** (`23b35853dcff5aaa69e7c870368ba478553416b8`, `operations-review/official-corporate-event-incremental-acquisition-freshness-v1-20260905/`). Continuation of `CORPORATE_EVENT_CANONICAL_DATA_REFRESH_AND_LEDGER_CONSOLIDATION_V1` (2026-09-05). Converted the prior one-off manual HNX/HOSE acquisition into a repeatable, incremental, versioned, idempotent capability (new `official_corporate_event_incremental_acquisition.py`) that reuses, never replaces, the existing acquisition mechanisms: content-addressed immutable raw retention, per-attempt session manifests, and pure fixture-provable change classification (unchanged/new-version/new-surface) against the prior successful attempt. Two real bugs found by actually running the mechanism live: `hose_public_xhr_and_periodic_series_recon.py`'s `AS_OF_DATE` was permanently frozen at `2026-08-24` (fixed, defaults to today); the HNX disclosure-index endpoints have no date filter and now require 440,000+ pages to walk fully (~22x growth since 2026-08-24) — since `current_official_event_context.py` never reads that index, acquisition now skips it by default. A third defect found by the same live run: `canonical_post_close_pipeline.register_session_inputs()`'s completed-session mutation check compared `official_universe`/`event_context` as session-locked, contradicting their own pre-existing `ACCEPTED_CURRENT_ASOF_BUILD_NOT_SESSION_LOCKED` policy — fixed by excluding both from that comparison. A real, live, bounded acquisition attempt genuinely advanced `research_session` from the frozen `2026-08-21` to `2026-09-05` (1,101 tickers, 4,444 events, within 0.2% of the frozen baseline), verified flowing through Corporate Intelligence axis and Integrated Decision with zero further wiring changes. 175 tests pass across the 9 directly-relevant suites, 0 regressions. No push, no new provider; HNX disclosure-index acquisition remains out of scope for routine runs. No successor is queued.


---

## 4. Acceptance Gates for Subsequent Phases

### Opening Phase 1 (Research Evidence Layer):
- First market-wide deterministic analysis artifact generated and verified offline.
- Explicit schema declarations for all derived technical indicators.
- Vectorized cross-sectional snapshots preserve bound `TemporalField` envelopes.

### Opening Phase 2 (Fundamental & Multi-Period Accounting):
- Explicit taxonomy mappings for Banking (`BANK`), Securities (`SEC`), and Corporate (`CORP`).
- Verified audit and statement scope metadata (`consolidated` vs `parent_only`).
- Immutable page-level bounding box and OCR evidence provenance.

### Opening Phase 3 (Strategy & Backtesting):
- Unconditional fail-closed enforcement of `QUALIFIED_LIQUIDITY_INPUTS` and `POSITION_SIZING_IS_SAFE`.
- Complete separation between `PIT_AS_KNOWN` historical simulation and retrospective restatements.
- Point-in-time adjusted price series derived strictly from verified official dividend ex-date event windows (`P0-A.3E Part B` / `P3-A`).

### Current Portfolio Risk Envelope V1 (Complete Locally)

`current_portfolio_risk_envelope/v1` is an optional explicit-input daily-session extension. The next real-data gate is qualified liquidity/ADV and policy inputs before days-to-liquidate, correlation, portfolio volatility, VaR/CVaR, leverage, sizing, or execution can be evaluated. No demonstration portfolio may be promoted into prospective learning.

### Current Macro Regime V1 (Complete Locally)

The completed first macro-evidence increment retains current NSO CPI releases with raw hashes, period/publication distinction, source-specific freshness, and date-level PIT metadata. The next macro evidence gate is a governed, machine-readable official Vietnam route for policy rate, FX, credit, banking-liquidity, and government-bond observations with release/vintage metadata. That gate must not promote historical PIT, forecast, causality, allocation, sizing, or execution.

### Current Market Flow / Positioning V1 (Complete Locally / Coherent Partial)

The next real-data gate is a bounded, rate-safe full supported-universe retained session packet that includes all mapped FHSC flow endpoints and the existing DNSE foreign-flow contract. It must retain per-ticker dispositions and session/freshness boundaries; it must not promote traded value to liquidity/ADV, position sizing, execution capacity, PIT/backtest, or causal/identity claims.

### Current Market Flow / Positioning Scaleout V1 (Terminally Closed)

The scaleout attempt is terminally `COHERENT_PARTIAL_PROVIDER_LIMITED`: retained same-session evidence materially expands traded-value context but does not establish full-universe all-dimension availability. Do not start another arbitrary flow cohort. The next real-data gate, only with owner authorization, is one governed broad full-universe session packet under demonstrated provider rate/session safety; it remains a promotion-review candidate only for any later liquidity-authority program.

### Prospective Decision Outcome Measurement V1 (Complete / Coherent Partial by Evidence)

`PROSPECTIVE_DECISION_OUTCOME_MEASUREMENT_V1` is the owner-authorized post-convergence measurement contract. It evaluates only a validated durable immutable T0 case and later explicit completed-session evidence, with exact T+5/T+20/T+60 session counts, compatible retained close-price basis, close-path-only research proxies, retained T0 confirmation/invalidation boundaries, and descriptive cohorts. It is not a historical backtest and cannot fabricate a T0 case from prior workspace exports or snapshots. The local retained durable store currently has **zero** real non-fixture cases; therefore its terminal disposition is `COHERENT_PARTIAL_BY_EVIDENCE`, not an inferred performance claim. No successor is queued: outcome calibration, threshold retuning, exact execution capacity, and PIT require a new owner decision.

### Current-Session Decision Rollover and Prospective Case Capture V1 (Complete / Coherent Partial)

`CURRENT_SESSION_DECISION_ROLLOVER_AND_PROSPECTIVE_CASE_CAPTURE_V1` makes future genuine case capture deterministic: `prospective_case_admission_policy/v1` and its one-shot runner require a qualified completed session and a Workspace projection at exactly that session, then retain non-discretionary eligible stance roles in the existing durable store and initialize outcome measurement. The real 2026-08-31 gate did not qualify a current session, so the 2026-08-28 projection was intentionally not admitted late; zero cases were created. The runner is ready for a future owner invocation when a genuine current qualified session exists. No calibration, retuning, execution, or PIT successor is queued.
